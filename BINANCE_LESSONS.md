# 幣安踩坑共用清單

**版本 2026-09-26r83（r9 作廢，未採用）。這份檔案在 crypto-screener、gold-scalper、pump-dump-hunter 三個專案裡內容相同。**
發現新坑 → 在這裡加一條 → 三個 repo 都更新一份。開新對話時把這份檔案一起丟給 Claude，
它就不必重新踩一次。每一條都寫「症狀 / 原因 / 處理 / 誰踩到的」，不寫抽象原則。

---

## 1. 條件單搬到 Algo 端點（2025-12-09）

- **症狀**：下單成功，但停損單回 `-4120 Order type not supported for this endpoint. Please use the Algo Order API endpoints instead.`，倉位裸奔。
- **原因**：幣安把 STOP_MARKET / TAKE_PROFIT_MARKET / TRAILING_STOP_MARKET 全部移到 Algo 服務，舊的 `/fapi/v1/order` 直接拒絕。
- **處理**：
  - 掛單 `POST /fapi/v1/algoOrder`，要帶 `algoType=CONDITIONAL`，`stopPrice` 改叫 `triggerPrice`，`activationPrice` 改叫 `activatePrice`，回傳的是 `algoId` 不是 `orderId`。
  - 撤單 `DELETE /fapi/v1/algoOrder` 帶 `algoId`；查掛單 `GET /fapi/v1/openAlgoOrders`。
  - 查回來的型別欄位叫 `orderType`（舊端點叫 `type`），兩個都要看。
  - 先打新端點，**只有 404 或未知端點才退回舊寫法**；參數錯（-1111、-2021 之類）不能誤判成端點不存在。
- **誰踩到**：crypto-screener 先踩並修好，pump-dump-hunter 沒同步，2026-09-20 NIL、09-21 SAGA 各裸奔一次。
- **退回不能是永久的**（r12 新增）：「端點不支援」的判斷若寫進一個全程序共用的旗標，一次誤判就會讓之後**所有**條件單改走舊端點、
  全部 `-4120`，直到重啟——停損、補掛、移損全部失效。只有 HTTP 404 才能當成「端點不存在」。
  - **誰踩到（crypto-screener）**：把 `-1000`（幣安的「未知錯誤」，多半是暫時性的伺服器問題）與 `-1013`（價格／數量過濾器，參數錯）
    都判成端點不存在，並永久切換。2026-09-21 對照 r11 時由測試帶出，改成只有 404 才退回。
  - **只有 404 還不夠，退回也要有期限、要能自己改回來**（r13 新增）：
    404 也可能是一時的（閘道、維護）。退回舊端點要設期限（例如 10 分鐘後重新試 Algo）；
    期限內舊端點若回 `-4120`，代表交易所明確說要用 Algo——**立刻改回 Algo 重送這一張**，不要等期限到。
    退回期間兩個端點都可能有自己的單，「查停損還在不在」要兩邊都查；Algo 那邊查詢失敗，只有在「目前確實走舊端點」時才能忽略。
  - **誰踩到（pump-dump-hunter）**：退回條件包含 `-1013` 與字串比對 `Unknown`，並永久切換。`-1000` 碰巧沒被判中
    （幣安訊息是小寫 unknown），但原因不對。2026-09-21 對照 r12 時改成上述做法，測試在修改前 3 項失敗。
  - **退回期間 Algo 查詢失敗，停損守衛不能整個跳過**（r15 新增）：守衛通常寫成「查詢失敗就跳過這輪」（第 2 條原則 1），
    但退回期間 Algo 端點一直回 404，每一輪都「查詢失敗」，守衛就**永遠不檢查**——舊端點的停損不見了也沒人補。
    退回期間要改用舊端點的查詢結果判斷；只有兩邊都查不到才跳過。
  - **「退回期間」要怎麼知道**（r16 新增）：如果只有「掛停損單時收到 404」才進入退回，服務**重啟後**退回旗標歸零，
    在還沒掛過新停損之前，守衛查 Algo 一直 404、判定「查詢失敗、跳過」——上面那一點又發生了，只是換了觸發條件。
    → 查停損時 Algo 回 404，也要跟掛單一樣進入退回期。
  - **誰踩到（pump-dump-hunter）**（r16 新增）：r13 的判斷式本身是對的（退回期間用舊端點結果），但只有掛單會設定退回旗標。
    2026-09-21 對照 r15 時，測試模擬「沒有 Algo 服務的環境、服務剛重啟、舊端點的停損不見了」，守衛 3 輪都沒補掛。
  - **誰踩到（crypto-screener）**（r18 新增）：同上，只有掛單會進入退回；而且帳上記的端點是 algo 時連舊端點都不查。
    2026-09-21 對照 r17 時改成查詢 404 也進入退回。
  - **誰踩到（crypto-screener）**（r15 新增）：2026-09-21 對照 r14 時，404 後永久改走舊端點、舊端點回 `-4120` 也不改回，
    以及上面「守衛整個跳過」。改成 10 分鐘後重試 Algo、遇 `-4120` 立刻改回重送、退回期間兩個端點都查。

## 2. 「查不到」不等於「不存在」

- **症狀**：誤判停損單消失 → 觸發保護動作 → 誤平倉。
- **原因**：查詢 API 失敗（超時、限流、回傳格式非預期）被當成「查到空清單」。
- **處理**：查詢函式回傳 `(orders, ok)`，`ok=False` 一律跳過不動作。要動作前還要：
  1. 連續 3 輪都確認不在才動手；
  2. 補掛被拒、原因含 existing / already → 視為誤判，不動作；
  3. 預設只補掛、不強制平倉。強制平倉是事故的放大器。
- **誰踩到**：crypto-screener 誤平倉一次，教訓寫在它的 `guard_positions` 註解裡。
- **查到空清單也不一定是「沒有」**（r15 新增）：`GET /fapi/v2/positionRisk` 不帶 symbol 時回傳全部幣，
  但維護或閘門異常時可能回 200 加空清單。對帳若據此判定「全部部位都平了」，會一次把所有部位記成平倉、**所有停損一起撤掉**。
  全量表裡找不到某個部位時，先帶 symbol 逐幣再查一次；逐幣查不到（失敗）就這輪不動。
  crypto-screener 2026-09-21 對照 r14 時補上，尚未出事。
  - **不只對帳會用到全量表**（r16 新增）：凡是用「查部位」下結論的地方都要檢查——
    平倉後確認「這一側沒了」、pending 到期前確認「交易所上沒有」、送單前記基準。這些地方直接帶 symbol 逐幣查，
    不要從全量表裡找。
  - **誰踩到（pump-dump-hunter）**（r16 新增）：對帳用全量表判平倉（同 crypto-screener）；更嚴重的是**平倉確認**也用全量表——
    平倉單被拒、確認查詢又剛好回空清單，就把部位記成已平倉、撤掉停損，交易所上留下沒有停損的倉。
    pending 到期判定「未成交」也用全量表。2026-09-21 對照 r15 時全部改成逐幣查詢。
  - **逐幣查詢回空清單也是異常**（r17 新增）：帶 symbol 查 positionRisk 時，交易所一定會回這個幣的列
    （單向 1 列 `BOTH`、雙向 `LONG`/`SHORT` 2 列，數量 0 也會回）。回 200 加空清單＝查詢異常，要當成「查不到」，
    不能當成「數量 0」。改用逐幣查詢不等於安全了——判斷式要檢查「有沒有這個幣的列」，不是只看加總數量。
  - **誰踩到（gold-scalper）**（r17 新增）：本來就一律逐幣查，但查詢函式把「沒有這個幣的列」當成數量 0。
    平倉單逾時、確認查詢剛好回空清單時，會判成「這一側已經沒了」、撤掉停損結帳；pending 到期也會判成未成交。
    2026-09-21 對照 r16 時改成沒有該幣的列就回「查不到」。
  - **「沒有部位才能做 X」的判斷也一樣**（r18 新增）：撤孤兒單、刪紀錄之類的動作，前提常寫成
    `any(數量 > 0 for 列 in 回傳)` 為假就放行——空清單時 `any()` 也是假，**判成「沒有部位」就動手了**。
    同樣要先確認回傳裡有這個幣的列。
  - **誰踩到（crypto-screener）**（r18 新增）：查詢函式把沒有該幣的列當成 0（同 gold-scalper），影響平倉前確認、
    平倉後確認、送單前基準三處；撤孤兒單用上面的 `any()` 寫法，空清單時會撤。另外 pending 到期判定「未成交」用全量表（同 pump-dump-hunter）。
    2026-09-21 對照 r17 時全部修正。
  - **誰踩到（pump-dump-hunter）**（r19 新增）：r16 才把平倉前後確認、對帳、pending 到期、送單前基準四處從全量表改成逐幣查詢，
    但查詢函式在回傳前就把數量 0 的列濾掉——空清單和「真的沒部位」變得無法分辨，四處全中（正是 r17 說的「改用逐幣查詢不等於安全」）。
    2026-09-21 對照 r18 時改成：沒有該幣的列就拋例外，呼叫端一律走「查不到、這輪不動」。
  - **守衛補掛前也要確認部位還在**（r19 新增）：停損不見了就補掛，看起來天經地義；但如果部位其實已經被平掉
    （例如這一輪對帳出錯、還沒偵測到），補上去的就是一張孤兒 reduce-only 單，單向共用帳號裡還可能平到別人同側的倉（第 7、13 條）。
    → 補掛前逐幣確認自己的部位（扣基準）：查不到就這輪不動、確認沒了就不補（交給對帳）、還在才補，數量取「交易所−基準」與帳上的小者。
    這一點在「對帳跟守衛各自 try」（第 8 條 r18）之後特別重要——對帳出錯時守衛照樣會跑。
  - **不只守衛補掛，任何「掛一張新停損」都要先確認**（r20 新增）：第一次掛、**移損時掛新的那一張**、補掛，
    都是送出一張新的 reduce-only 單。移損尤其容易漏——它看起來是「更新」既有的停損，實際上是新掛一張。
    部位已被平掉、只是還沒偵測到時，價格一動就移損，就掛出孤兒單。確認要寫在「送出停損單」那一個共用的地方，不是守衛裡。
  - **誰踩到（gold-scalper）**（r20 新增）：第一次掛、移損、守衛補掛共用一個函式，但這個函式掛單前完全不查部位；
    數量用帳上的、沒扣基準。2026-09-21 對照 r19 時在這個共用函式裡補上確認：查不到這輪不動（不算掛單失敗）、
    扣基準後沒了就不掛（交給數量比對）、數量取小。啟動對帳遇空清單原本會回報「一致（空手）」，改成回報「查不到」。
  - **有基準時停損不能用 `closePosition`**（r21 新增）：`closePosition=true` 是「平掉這一側全部」，不看數量。
    同側有別人的部位（基準 > 0）時，這張停損一觸發就把**別人的部位一起平掉**——平倉單扣了基準（第 7 條 r15），停損沒扣，一樣會出事。
    → 有基準時停損改用「自己的數量＋reduce-only」（數量取交易所−基準與帳上的小者）；沒有基準時可以維持 `closePosition`
    （好處是部分出場後自動涵蓋剩餘）。這個判斷也放在「送出停損單」的共用地方。
  - **自檢／啟動對帳遇空清單的另一個後果**（r21 新增）：用全量表判斷「哪些幣沒有部位」再找孤兒條件單時，
    空清單會讓**所有部位的停損都被列成孤兒單**，畫面上旁邊還有撤單按鈕。全量表裡找不到時要逐幣確認，查不到的幣不列孤兒。
  - **誰踩到（crypto-screener）**（r21 新增）：2026-09-21 對照 r20 時——第一次掛、認領、守衛補掛、移損掛新、移損失敗掛回共 5 處
    各自直接送單，全部不確認部位、全部用 `closePosition`（有基準時會平到別人的）；自檢遇空清單把自己部位的停損列成孤兒。
    改成全部走一個 `place_stop()`：確認部位（扣基準）、查不到回「unknown」這輪不動不算失敗、沒了回「gone」交給對帳、
    有基準用數量。移損在**撤舊之前**就先確認（否則撤了才發現查不到，就裸倉了）。
    認領時用全量表那一列當確認（這一列本身就是「部位在」的正向證據；危險的是空清單被當成「沒有」，不是反過來）。
  - **誰踩到（pump-dump-hunter）**（r22 新增）：5 處各自送停損單，只有守衛補掛（r19 才加）有確認；**移損掛新的那一張完全沒確認**，
    數量也沒跟「交易所−基準」取小。沒有 `closePosition`（r3 起就是 reduce-only 帶數量），有基準時帳上數量本來就是自己的。
    2026-09-21 對照 r21 時改成一個共用的 `place_stop()`，回傳 ok／unknown／gone；手上有正向證據（剛成交的回應、交易所那一列）的
    呼叫端直接傳已知數量，不重查。移損遇 unknown 時**不算失敗**、想要的停損價留著，下一輪重試。
    自檢孤兒單 r19 已逐幣確認、查不到的列「無法確認」不列孤兒，這點本來就對。
  - **誰踩到（gold-scalper）**（r23 新增）：r20 把確認放進共用函式後，**開倉成交、認領之後掛停損也會重查**——
    手上已有成交回應／交易所那一列，卻又查一次；交易所剛成交時偶爾還沒反映，重查拿到 0 就判「部位不在」而**不掛停損**。
    2026-09-21 對照 r22 時改成這兩處直接傳已知數量；共用函式改成回傳 unknown／gone／placed 等結果，測試的前提直接檢查結果（第 16 種）。
    停損一律 reduce-only 帶數量、從沒用過 `closePosition`，r21 那點本來就對。
  - **誰踩到（crypto-screener）**（r24 新增）：2026-09-21 對照 r23 時，開倉與認領 r21 起就傳已知數量、不重查，這點本來就對。
    但移損與守衛遇 unknown／gone 時**什麼都不回傳**，測試分不出「查不到」和「沒了」（第 16 種）；改成回傳略過的原因。
  - **誰踩到（pump-dump-hunter）**（r25 新增）：開倉、認領、手動認領 r22 起就傳已知數量，本來就對；移損 r22 起會回傳 unknown／gone。
    **停損守衛**的每個出口都回 None（查詢失敗、停損還在、還沒到第 3 輪、查不到、沒了、已存在誤判、補掛失敗），
    2026-09-21 對照 r24 時改成每個出口都回傳原因，並用語法樹確認函式裡每一個 `return` 都帶值。
  - **誰踩到（gold-scalper）**（r26 新增）：掛停損、數量比對 r23 已回傳原因，但**停損守衛** 4 個出口都回 None，補掛那個出口也沒把掛單結果傳回來。
    2026-09-21 對照 r25 時改成回傳 skip／unknown／present／missing／replaced:（掛單結果）。語法樹檢查除了「每個 `return` 帶值」，
    還要檢查**函式最後不會掉出去**（最後一行不是 return 時，Python 一樣回 None）。
  - **誰踩到（crypto-screener）**（r27 新增）：2026-09-21 對照 r26 時，停損守衛 8 個出口都是裸 `return`、最後一句是 `if` 會掉出函式；
    移損「還沒到」「已移過」「待平倉」三個出口回 None。改成每個出口回傳原因，並用語法樹檢查
    （`scripts/check_returns.py`：每個 return 帶值、明寫 `return None` 也算沒帶、最後不能掉出去；附 6 組人造函式自我驗證）。
  - **誰踩到（pump-dump-hunter）**（r28 新增）：r25 只用語法樹檢查了守衛「每個 return 帶值」，沒檢查會不會掉出函式，也只檢查了守衛一個。
    2026-09-21 對照 r27 時擴大到守衛、移損、重試移損、送停損、重試平倉五個：移損**成功時**最後一句是 `if`，掉出函式回 None；
    重試移損有兩個裸 `return`、最後的 try 也會掉出去；重試平倉有一個裸 `return`。守衛與送停損本來就對。
    改成每個出口回傳原因（`scripts/check_returns.py`，照 crypto-screener 的做法附 6 組人造函式自我驗證）。
  - **誰踩到（gold-scalper）**（r29 新增）：r26 的語法樹檢查用 `n.value is None` 判斷「不帶值」，**明寫的 `return None` 抓不到**
    （它的 value 是一個值為 None 的常數節點）；也只檢查了守衛、掛停損、數量比對三個。2026-09-21 對照 r28 時補上 `return None`、
    「最後一句是沒有 else 的 if」算掉出去，並擴大到認領、重試平倉（兩者都還有裸 `return`）；附 6 組人造函式自我驗證。
    檢查本身也要有前提：每個函式真的解析到多個 `return`（第 19 種）——這項前提是這輪的測試靜態檢查抓到它缺的。
  - **誰踩到（crypto-screener）**（r30 新增）：2026-09-21 對照 r29 時，r27 的檢查本來就把明寫的 `return None` 算成不帶值、
    沒有 else 的 if 算掉出去；但前提只看**總數**（「return 總數 ≥ 函式數」），一個函式只有 1 個 return 會被別的函式撐過去。
    改成每個函式至少 2 個，並把新增的出場價函式、手動平倉、撤孤兒單納入（共 10 個函式）。
  - **誰踩到（pump-dump-hunter）**（r31 新增）：r28 的檢查本來就把明寫的 `return None` 算成不帶值、沒有 else 的 if 算掉出去，
    但前提只看「掃了幾個函式」，沒看每個函式有幾個 return。2026-09-21 對照 r30 時改成每個函式至少 2 個，
    並把手動平倉、認領、網頁交易入口納入（共 8 個函式）；這三個 r28 本來就每個出口都回傳值。
  - **誰踩到（gold-scalper）**（r32 新增）：r29 的前提本來就是每個函式至少 2 個 return。2026-09-21 對照 r31 時把手動平倉、
    網頁交易入口 5 個函式納入（網頁入口被裝飾器包過一層，要用 `__wrapped__` 取原函式來解析）；全部本來就每個出口都回傳值。
  - **誰踩到（crypto-screener）**（r33 新增）：r30 起每個函式至少 2 個 return；2026-09-21 對照 r32 時再把手動平倉外層、
    網頁交易入口納入（共 12 個函式），兩者本來就每個出口都回傳值。

## 3. 下單成功但沒記到帳 → 孤兒倉

- **症狀**：交易所有倉，程式的持倉清單是空的；之後該幣的訊號被自己擋掉，還誤判成「其他專案的倉」。
- **原因**：市價單送出後、記帳前如果拋例外（例如接著掛停損失敗），整段被外層 try 吞掉，什麼都沒寫。
- **處理**：順序必須是 **送單前先寫 pending → 成交後立刻記帳 → 最後才掛停損**。
  **送單回應逾時或 5xx 時結果不明**（r12 新增）：單可能已經成交，不能當成送單失敗而清掉 pending——要保留給下一輪對帳確認。
  只有交易所明確拒絕（4xx 帶錯誤碼）才可以清掉。crypto-screener 在 r11 時把逾時（狀態 0）當失敗並清掉 pending，已修正。下一輪對帳時，pending 裡還在交易所的倉要認領回來。另外做一個手動認領/平倉的介面，處理修正前留下的孤兒倉。
- **保留下來的 pending 要有期限，而且期間要擋同幣進場**（r13 新增）：
  1. 對帳「這一輪沒看到部位」不能立刻清掉 pending——交易所可能還沒反映。給一個期限（例如 180 秒），過了還沒有才判定未成交並通知。
     否則逾時之後，同一輪迴圈裡下一個訊號觸發的強制對帳，就會在交易所反映前把 pending 清掉。
  2. pending 期間同一個幣不能再下新單，本策略的持倉數也要把 pending 算進去。
- **認領時數量、均價一律拿交易所那一列的，並立刻掛停損**（r13 新增）：
  pending 是送單**前**寫的，裡面只有訊號資訊，沒有成交數量與均價。直接把 pending 紀錄當成部位，
  之後掛停損、移損、平倉用到數量時會出錯——而這些都在守衛的 try 裡，錯誤被吞掉、**部位永遠沒有停損**。
  另外認領回來的部位一定還沒掛停損（停損是記帳之後才掛的），不能等守衛「連續 3 輪確認不在」，要當場掛。
  - **誰踩到（pump-dump-hunter）**：認領直接複製 pending 紀錄，沒有數量；守衛補掛時 `KeyError: 'qty'` 被接住，只推播一則
    「補掛失敗：'qty'」、錯誤區沒有紀錄。2026-09-21 對照 r12 時由測試帶出（測試第一版只看錯誤區，碰巧通過，見用法第 5 點）。
- **送單前先記下這一側原有的數量**（r14 新增）：認領時「交易所那一列」可能包含送單前就有的部位
  （共用帳號、之前的孤兒倉、同側加碼）。送單前查一次、記下基準，認領數量＝現在減基準；有基準部位時均價是合併過的，
  要在通知裡講明，不能當成這張單的成交價。
  - **誰踩到（gold-scalper）**（r14 新增）：開倉回應逾時一律當送單失敗，還把「是否真的開倉」記成否——
    之後出場照規則不送真實平倉單，若其實成交就留下孤兒倉。沒有 pending 機制。2026-09-21 對照 r13 時補上：
    回應不明先查交易所，看到就認領（數量均價取交易所、當場掛停損），沒看到就記成「不明」保留 180 秒每輪確認。
    gold-scalper 每個引擎一個帳戶、一次一個部位，pending 期間引擎已有帳面部位，本來就不會再開新倉。
  - **基準查不到就不送單**（r16 新增）：送單前的基準查詢失敗時，不能當成 0 繼續送——之後認領、平倉都會把別人的部位算成自己的。
    這次不下單、記錄原因，下一根 K 棒再說。
  - **基準要跟著部位走完整個生命週期**（r16 新增）：不只認領時扣，**對帳判斷平倉、偵測數量減少、平倉前確認剩多少、
    平倉後確認沒了**，全部都要扣基準（見第 7 條 r15）。只在認領時扣，其他地方照樣會把別人的部位算進來。
  - **基準的限制**：只涵蓋「送單前就有」的部位。單向共用帳號裡，別的專案在你進場**之後**才在同一側加碼，
    交易所上是合併成一列，程式無從分辨。真正的解法是第 7 條的「上主網前每個專案開獨立子帳號」。
  - **誰踩到（pump-dump-hunter）**（r16 新增）：沒有基準。2026-09-21 對照 r15 時補上：送單前逐幣查、記進 pending 與部位紀錄，
    認領數量＝交易所−基準、有基準時用基準均價反推這張單的成交價並在通知裡講明是估計值。
  - **即時損益也要扣基準**（r18 新增）：畫面上的未實現損益、以及拿它算 R 倍數的地方（例如反向訊號時判斷持倉是否已賺到 1R），
    若直接用交易所那一列的 `unRealizedProfit`，同側有別人的部位時會把別人的損益算進來。改用「自己的數量 ×（標記價 − 自己的進場價）」。
  - **誰踩到（crypto-screener）**（r18 新增）：2026-09-21 對照 r17 時——基準查不到時以 0 計繼續送單（r14 時的寫法）；
    認領沒有用基準均價反推；即時損益沒扣基準。「全生命週期扣基準」的其他地方（對帳判平倉、偵測數量減少、平倉前後確認）r14 已做。
  - **誰踩到（pump-dump-hunter）**（r19 新增）：儀表板「帳號全部持倉」表中本策略那一列，損益直接用交易所整列的 `unRealizedProfit`。
    沒有拿即時損益算 R 的地方（R 一律用 K 棒價格與自己的進場價算）。2026-09-21 對照 r18 時補上「本策略自己的損益」欄，
    原本那欄保留並註明是整列（含同側別人的部位）。
- **注意**：對帳迴圈如果只在「帳上有持倉」時才跑，崩潰後帳上是空的、只剩 pending，認領就永遠不會發生。觸發條件要是「有持倉 **或** 有 pending」。
- **誰踩到**：pump-dump-hunter，2026-09-20。crypto-screener 2026-09-21 檢查時發現同樣的空窗（市價單到記帳之間約百行、五六次 API 呼叫），尚未出事即補上。
- **認領要在「判斷哪些部位還在」之前做**（r15 新增）：對帳常見的寫法是先從部位表算出「還在場的部位」，
  再處理 pending 認領，最後把「帳上有、但不在場」的記成平倉。剛認領進帳的部位不在先前算好的清單裡，
  **同一輪就被判成已平倉**，平倉流程還會把剛掛上的停損撤掉——交易所上留下一個沒有停損、帳上也沒有的倉。
  → 先認領，再算「還在場」。測試要走完整的對帳流程，不能只單獨呼叫認領函式（單獨呼叫測不到順序）。
- **誰踩到（crypto-screener）**（r15 新增）：2026-09-21 對照 r14 時，r13、r14 的每一點都有——pending 120 秒就靜靜丟掉、
  pending 期間同幣仍可下單且不算持倉數、送單前沒記基準數量（成交確認看到部位就當成這張單的），
  以及上面「認領後同一輪被判平倉」。最後一項是測試走完整對帳流程時才發現：上一輪的認領測試直接呼叫認領函式，所以一直通過。

## 4. 數量與價格必須照交易所精度

- **症狀**：`-1111 Precision is over the maximum defined for this asset`。
- **原因**：`round(qty)` 直接取整、停損價固定小數位，沒照 `exchangeInfo` 的 `stepSize` / `tickSize`。
- **處理**：開機讀一次 `exchangeInfo` 快取；數量無條件捨去到 `stepSize`、價格四捨五入到 `tickSize`，都用字串送出避免科學記號；低於 `minQty` / `minNotional` 提早擋下。
- **延伸**：出場位階（R、目標、移動停利啟動價）要用**取整後、實際掛出去**的停損價算。用取整前的算，R 會差一個跳動點，績效的 R 倍數跟著偏（crypto-screener 的一致性測試抓到）。

## 5. 槓桿設不上會沉默失敗

- **症狀**：保證金佔用與強平距離跟預期不同。
- **原因**：新子帳戶常被限制在 5x，小市值幣的分層本身也可能低於 10x，設定被拒卻沒處理。
- **處理**：設不上就查 `leverageBracket` 取上限、改用上限，並依實際槓桿縮量；回傳實際值與原因，不要沉默吞掉。**換子帳號上主網前一定要重測這項。**
- **順序**：設槓桿要在計算部位大小**之前**——名目上限與保證金上限都要用實際生效的倍數。crypto-screener 曾把兩者順序寫反，變數在賦值前被使用，正式網第一次下單直接拋 `UnboundLocalError`（送單前就中止，沒有留倉）。

## 6. positionRisk 權重高，打太兇會被限流

- **症狀**：`418 I'm a teapot`（429 累積後的 IP 封鎖），對帳整段失效。
- **原因**：每產生一個訊號就對帳一次。
- **處理**：對帳結果快取 20 秒，只有真的要下單前才強制重抓。查掛單優先帶 symbol（權重 1），不帶 symbol 是 40。
- **延伸**：會打高權重查詢的端點如果不需要登入（例如網頁上的自檢按鈕），要加冷卻，不能讓它被反覆觸發。

## 7. 共用模擬帳號會互相干擾

- **症狀**：A 專案的持倉讓 B 專案不敢下單；或 B 的殘留 reduce-only 單平掉 A 的倉。
- **原因**：三個專案共用同一個 demo 帳號。
- **處理**：持倉數只算自己開的，每輪跟交易所對帳；撤單一律用 `orderId` / `algoId` 精準撤，**不要用 `cancel all open orders by symbol`**；本策略的倉位上限獨立計算（例如 pump-dump-hunter 把本金上限壓在 1500 U）。上主網前每個專案開獨立子帳號。
- **雙向模式**：共用帳號若切成雙向持倉，每張單要帶 `positionSide`、且不能帶 `reduceOnly`；讀 positionRisk 時只認自己那一側。模式可能被別的專案切換，遇到 `-4061` / `-1106` 要強制重新偵測模式並重送一次。
  - 對帳也要用「幣＋方向」比對，不能只看幣名：我的多單已被停損、別的專案剛好有同幣空單時，只看幣名會誤以為自己還在場。
    **單向模式也要做**：單向時 `positionSide` 一律是 `BOTH`，方向要看 `positionAmt` 的正負；只在雙向模式過濾 `positionSide` 不夠。
  - 持倉模式若有快取，上面的「強制重新偵測」要清掉快取，否則快取期間會一直送錯參數。
    重新偵測本身也可能失敗（逾時、限流）：這時要反轉**這張被拒的單送出時的假設**——帶了 `positionSide` 就是當雙向送的，重送改單向寫法，反之亦然——
    再用結果更新快取。**不能拿快取來反轉**：快取可能從沒偵測成功過（空值），也可能在送單後被別的執行緒改過，
    用它反轉會拿同一個錯誤假設再送一次。（r8 更正：r5–r7 這裡寫「直接把快取反轉」，會誤導。）
  - **誰踩到（反轉快取）**：crypto-screener 在 r5 照原文實作成「反轉快取」，快取為空時沒有值可反轉，
    就用同一個假設重送、再被拒。2026-09-21 對照 r7 時改成依被拒那張單的參數反轉，並用測試確認修改前會失敗。
- **守衛程式只能碰自己記錄過 id 的單**：「確認停損還在」、「清掉多餘的停損單」這類守衛，不能把同幣的所有條件單都當成自己的。
  共用帳號裡別的專案同幣的停損會被當成殘留撤掉；反過來，自己的停損不見了、只剩別人的單時，也會誤判成「停損還在」。
  正確做法：只認部位上記錄過的 `algoId` / `orderId`；沒記錄 id 的舊部位才退而用方向判斷。
  - **移動停利不能算「停損還在」**：它要到啟動價才生效、通常只涵蓋部分數量。把它算進去，真正的停損不見時會被它蓋過去。
  - **守衛補掛成功要把新 id 換上帳、拿掉舊 id**。沒換的話，改成「只認 id」之後，帳上那個已不存在的舊 id 會讓每一輪都誤判停損不在。
  - **誰踩到**：pump-dump-hunter，2026-09-21 對照 r2 檢查時在自己的 `ensure_stop` 發現（前一版才引入），尚未出事即修正。
  - **誰踩到（對帳）**：gold-scalper 啟動對帳把帳戶所有 `positionAmt` 加總成淨額再比，雙向模式多空各一張會誤判空手、只剩別人的反向單會誤判仍在場。2026-09-21 對照 r3 時改成依方向比對。
  - **誰踩到（crypto-screener）**：2026-09-21 對照 r4 檢查時，四點都有：
    對帳只在雙向模式過濾方向（單向漏了正負號）；模式重新偵測失敗時快取不變；
    守衛把同幣任何 STOP_MARKET 或 TRAILING_STOP_MARKET 都當成自己的停損；補掛成功沒記新 id。均已修正，尚未出事。
  - **誰踩到（偵測失敗就放棄）**：gold-scalper 沒有持倉模式快取、每次即時查，但收到 `-4061` / `-1106` 後重新偵測若失敗，就**直接放棄重送**——效果跟「快取不變」一樣，這張單就沒送出去。2026-09-21 對照 r6 時改成：偵測失敗就把原本的假設反轉（原本帶 `positionSide` 改成單向寫法，反之亦然）再送一次。**沒有快取的專案也要檢查這個分支。**
  - **有快取的專案，第一次送單也是這個分支**（r10 新增）：快取過期後，下一張單送出前會先重新偵測；
    偵測被限流或逾時就直接拋錯，單根本沒送出去——不需要先被 `-4061` 拒絕。以 5 分鐘快取為例，
    每 5 分鐘就有一個時間窗，偵測 API 一失敗，掛停損、移損都會失敗。
    處理：送單用的偵測失敗時不能放棄——有舊值就用舊值，從沒偵測成功過就先假設單向；假設錯了會被拒，再依被拒單反轉。
    自檢那類「就是要知道現在模式」的查詢才該把偵測失敗當錯誤。
  - **「再用結果更新快取」的精確意思**（r10 補充）：重送**成功**才把快取寫成成功的那個假設；
    重送也失敗就清掉快取，不能留一個沒驗證過的值給下一張單用。
  - **誰踩到（pump-dump-hunter）**：2026-09-21 對照 r8 時全部都有——照 r5 原文反轉快取；快取為空時根本不反轉、
    重送又去偵測又失敗；重送前就把反轉值寫進快取；服務有背景迴圈與網頁手動操作兩條執行緒，錯誤發生後才讀快取；
    以及上面「第一次送單」那個分支。已修正，測試在修改前的程式上確認會失敗。
  - **誰踩到（gold-scalper，偵測失敗時平倉單沒送出）**（r11 新增）：沒有快取、每次即時偵測，但偵測失敗時用呼叫端給的預設值——
    預設是雙向。gold-live 是單向帳戶，平倉時就去找 LONG 那一列、找不到部位，回「沒有部位可以平」，**平倉單根本沒送出去**，
    真實部位留在交易所，程式當成已出場。連 `-4061` 都不會發生，反轉重送那段救不到。開倉那邊偵測失敗則直接假設「設定想要的模式」。
    2026-09-21 對照 r10 時改成：偵測失敗有舊值用舊值、沒有就假設單向；另外**平倉前本來就會查 positionRisk，它的列本身就說明了模式**
    （單向只有 `BOTH` 列，雙向是 `LONG`/`SHORT` 兩列），平倉改成以它為準，不依賴另外偵測。
    **另外兩邊請檢查**：平倉時如果是「先決定模式、再去部位表找對應那一列」，模式猜錯會不會變成「找不到部位」而不送單。
  - **確認成交的查詢也要用「幣＋方向」**（r12 新增）：送單後輪詢部位表確認成交時，單向模式同樣要看 `positionAmt` 的正負號。
    共用帳號裡別的專案同幣反向的部位，不能被當成自己的單已成交（否則會拿錯數量與均價去掛停損）。
  - **手動平倉也一樣：找不到自己那一側就不動作，不能退而取第一列**（r13 新增）：
    帳上有紀錄時只認「同一側」；那一側已經不在（可能剛被停損），就回錯誤、不送單、不動帳上紀錄，交給對帳。
    帳上沒紀錄的孤兒倉，雙向模式同幣兩側都有部位時分不出哪一側是孤兒，也不能自己挑。
  - **誰踩到（pump-dump-hunter）**：手動平倉找不到自己那一側時取 `cands[0]`。測試中帳上沒紀錄、雙向同幣兩側都有部位時，
    **真的把第一列（別的專案的 SHORT）平掉了**並回報「已平倉」；帳上有紀錄但那一側已不在時，會用別人那一列的數量送出平倉單。
    2026-09-21 對照 r12 時修正。
  - **誰踩到（crypto-screener，r10 兩點）**：2026-09-21 對照 r11 時——重送**之前**就把反轉值寫進快取、重送失敗也不清；
    以及上面「確認成交」那一點（r5 修對帳方向時漏了這個函式）。「第一次送單」分支沒有問題：網路錯誤回狀態 0 而不拋例外，
    偵測失敗時有舊值用舊值、沒有就單向。平倉不查部位表、直接送單，不會因模式猜錯而「找不到部位」。
  - **誰踩到（撤全部）**：gold-scalper 每次真實開倉切持倉模式被 `-4067` 擋下時，會撤掉**整個帳戶所有幣**的掛單再重試——當初 crypto-screener 的 KAS 條件單就是這樣被撤掉的。2026-09-21 改成不撤、直接沿用帳戶現有模式下單。
  - **不取第一列，但也不能「不查就送」**（r15 新增）：平倉不查部位表、直接送 reduceOnly 單，看起來避開了「取錯列」，
    但自己那一側已被停損時，單向共用帳號裡同側若還有別的專案的部位，這張 reduceOnly 單會**把別人的平掉**。
    送平倉單前要確認「這一側扣掉基準數量（第 3 條 r14）後，還有自己的部位」；沒有就不送、不動帳，交給對帳。
    平倉數量也要用「交易所這一側 − 基準」與帳上數量取小的。
  - **誰踩到（crypto-screener）**（r15 新增）：r12 時寫「平倉不查部位表，不會找不到部位」，只看了一半——
    2026-09-21 對照 r14 時由測試帶出上面的情況。
  - **誰踩到（pump-dump-hunter）**（r16 新增）：r13 已不取第一列，但「自己那一側」直接用交易所整列的數量——
    單向帳號裡交易所 140（自己 100＋別人 40）時手動平倉平掉 140；自己的 100 已被停損、只剩別人 40 時仍送出平倉單；
    對帳把「只剩別人的 40」判成「自己的數量從 100 減少到 40」。時間出場的平倉重試也會用交易所剩餘數量重送，
    把別人的部位一起平掉。2026-09-21 對照 r15 時全部改成扣基準。

## 8. 同方向的 closePosition 停損只能有一張

- **症狀**：移動停損時「先掛新的再撤舊的」被拒。
- **原因**：`closePosition=true` 的條件單同方向同時只能存在一張。
- **處理**：兩種做法二選一：
  - 用 `reduceOnly` + `quantity`（可以並存）→ 先掛新、再撤舊，沒有裸倉窗口。**建議這個。**
  - 仍用 `closePosition` → 只能先撤舊再掛新，掛不上要立刻把舊的掛回去，裸倉窗口約一秒。
- **誰踩到**：crypto-screener 原本「先掛新再撤舊」，新單被拒、舊單留著，移損到成本安靜失效好幾天
  （只寫進日誌）。改成第二種做法；保留 `closePosition` 是因為第一目標出一半後，它能自動涵蓋剩下的數量。
- **兩種做法都要**：新停損掛不上時，**記下「想要的停損價」並每輪重試直到成功**，只發一次告警不夠——
  下一根 K 棒不會再觸發同一個移損條件，錯過就永遠錯過。價格若已穿過想要的停損（`-2021`），直接出場。
  撤舊單失敗也要把舊 id 記下來，平倉時一起撤（接第 13 條）。
  pump-dump-hunter 2026-09-21 對照 r2 時發現同一個模式（走 reduceOnly 做法也會發生），已補上。
  crypto-screener 2026-09-21 對照 r4 時發現：移損失敗會把舊停損掛回去，但沒記「想要的停損價」，
  只有價格還在 1R 以上時下一輪才會再試；另外撤舊停損的結果完全沒檢查，撤不掉時仍去掛新的。
  已改成：觸發後記下想要的停損、之後每輪不看價格直接重試；撤舊失敗這輪不動、舊 id 留在帳上；
  `-2021` 直接市價出場；告警只在開始失敗與恢復時各發一次。
  gold-scalper 的 backstop（第 12 條）也是同一個模式：搬移掛不上只記警告，停損沒再移動就不會再試。2026-09-21 改成每輪對齊「想要的停損價」直到成功，告警只在開始失敗與恢復時各發一次。
- **告警節奏（r6 定案，三個專案統一）**：重試失敗期間要**定期提醒直到恢復**，不能只在開始失敗時發一次。
  停損卡住沒移動是持續累積風險的狀態，只發一次的告警很容易被後續訊息淹沒、被當成已處理。
  - 節奏：失敗第 1、5、30 次各發一次，之後每 120 次發一次（以每分鐘一輪計，約每 2 小時）；恢復時再發一次「已補上」。
    「之後每 120 次」精確定義為**第 30 次之後每隔 120 次**：150、270、390…（r8 補充：原文可讀成 120、240、360，三個專案要一致）。
    判斷式：`n in (1, 5, 30) or (n > 30 and (n - 30) % 120 == 0)`。
  - 每則提醒要帶：幣別、想要的停損價、目前實際停損價、第幾次、最後一次的錯誤訊息——看一則就知道現況，不用翻前面。
  - 這個節奏同樣適用於「沒有停損單且補掛失敗」（第 2 條）與「平倉後殘留單撤不掉」（第 13 條）這類需要人處理的持續狀態。
  - crypto-screener 與 gold-scalper 在 r5 時是「開始與恢復各一次」，**要改**。
    （r10 更正：r6 這裡原本寫「pump-dump-hunter 原本就是這個節奏」，是錯的。它當時算的是 `n % 120 == 0`，
    落點 120、240、360。2026-09-21 對照 r8 時改正。）
    crypto-screener 2026-09-21 已改（r8）：移損失敗、補掛失敗（第 2 條）、殘留單撤不掉（第 13 條）三處都套用，
    殘留單同樣放進引擎層級的待撤清單。其中補掛失敗原本是反方向的問題——**每輪都推**，持續失敗時洗版。
    gold-scalper 2026-09-21 已改（r7）：backstop 掛單/搬移失敗、以及平倉後殘留單撤不掉都套這個節奏。
    殘留單撤不掉時部位已經平了，重試狀態沒地方放在部位上，改放在引擎層級的「待撤清單」每輪重試。
  - **次數不等於時間**：節奏是以「失敗次數」計，實際間隔看各專案的重試迴圈多快。gold-scalper 每 15 秒一輪，
    120 次約 30 分鐘，不是 2 小時。各專案回報時請寫明自己的迴圈間隔，比較告警頻率時才不會誤會。
    crypto-screener 有部位、待認領送單或殘留單時每 20 秒一輪（`POSITION_POLL`），120 次約 40 分鐘；否則 60 秒一輪。
    pump-dump-hunter 固定每 60 秒一輪（`POLL_SEC`），120 次約 2 小時，第 150 次約在開始失敗後 2.5 小時。
  - **恢復通知不能用「失敗超過 1 次」判斷**（r8 新增）：第 1 次失敗已經告警過，若下一輪重試就成功，
    失敗次數仍是 1，用 `> 1` 當條件會讓「已補上」永遠不發——使用者收到「失敗」卻等不到「恢復」。
    正確條件是「曾經告警過」（第 1 次一定會告警，所以等於「失敗次數 ≥ 1」）。
    crypto-screener 的殘留單恢復通知在 r7 實作時犯了這個錯，對照 r7 重新檢查時發現並修正。
  - **判斷式對了還是會錯：計數和恢復放錯位置**（r10 新增）。檢查的不是條件式，而是「失敗在哪裡發生、狀態在哪裡被清掉」：
    1. **第一次失敗在計數之外**：例如移損的第一次失敗發生在出場判斷裡、只發一則通用告警，
       計數寫在重試函式裡——之後告警的「第 1 次」其實是第 2 次；殘留單在平倉當下撤不掉，計數從 0 開始又不告警，
       下一輪就撤掉時恢復條件（≥ 1）不成立，使用者從頭到尾不知道出過事。
       → 計數與告警放在**失敗發生的那個函式裡**（例如「掛停損」本身），不管誰呼叫、第幾次呼叫都走同一段。
    2. **恢復只寫在重試函式裡**：失敗狀態可能從別的路徑被清掉——移損失敗後，下一根 K 棒的追蹤停損從出場判斷那條路
       移損成功，失敗次數被默默歸零；補掛失敗後，下一輪發現停損其實在（上次逾時但交易所端成功），
       或補掛回報「已存在」，都直接返回。三種情況都收到了失敗告警、等不到恢復。
       → 恢復通知放在**失敗狀態被清掉的每一個地方**，最簡單的做法是跟計數放在同一個函式裡。
  - **已成交的動作先通知，再做後續可能失敗的步驟**（r10 新增）：例如「到 1R 減碼一半、停損移到成本」，
    減碼是市價單、已經成交；如果通知寫在移損之後，移損一失敗通知就不會發，部位少了一半你卻不知道。
  - **成交後的例外處理不能改寫「已成交」**（r11 新增，上一點的延伸）：送單和後續步驟（記錄滑點、寫資料庫、掛停損）
    包在同一個 `try` 裡時，後續任何一步丟例外，`except` 會把整筆當成送單失敗處理——通知變成「下單失敗」，
    更嚴重的是若 `except` 裡會把「是否真的開倉」記成否，之後出場時就不會送真實平倉單，**交易所留下孤兒倉（第 3 條）**。
    → 送單成功就立刻記一個「已成交」旗標，`except` 先看旗標：已成交只記錄錯誤、不改成交紀錄。
  - **誰踩到（pump-dump-hunter）**：2026-09-21 對照 r8 時，節奏、上面兩種錯法、減碼通知全部都有，已修正。
  - **誰踩到（gold-scalper）**（r11 新增）：對照 r10 時：節奏落點是 `n % 120`（120、240）；掛 backstop 時丟例外不計數；
    搬移時舊單撤不掉要等平倉才開始計數；backstop 失敗中部位被平掉，失敗狀態消失卻沒有收尾通知。
    以及上面「成交後的例外處理」——開倉那段 `except` 會把 `real_open_executed` 改成否，是真的會留孤兒倉的寫法。
    已修正，`tests/test_lessons.py` 在修改前的程式上 12 項有 9 項失敗（另外 3 項是前幾版已修好的回歸項）。
  - **平倉單送出後一定要看結果**（r12 新增，上一點的反面）：上一點是「已成交被當成失敗」，這一點是「失敗被當成已成交」。
    平倉市價單被拒（保證金、數量、模式參數、網路）時若照樣記成已平倉，接著撤掉這個部位的停損與停利——
    真實部位留在交易所，而且**沒有任何保護**。
    → 平倉單回應非成功時，再查一次部位：交易所上確實沒有了（例如同時被停損觸發）才記帳；還在就保留部位與停損、告警。
    數量不符被拒（出一部分後帳上沒更新、雙向模式不能超量）時，改用交易所上的實際數量重送一次。
    **補充（r13）**：
    - 「再查一次部位」本身也會失敗，查不到不能當成「已經沒了」（第 2 條）→ 當成沒平掉。
    - 回應逾時／5xx 但交易所其實成交了——這是最常見的「看起來失敗、其實成功」，查部位就會知道，不能直接告警「平倉失敗」。
    - **帳上紀錄要等確認平掉之後才移除**。先從帳上拿掉再送平倉單，被拒時部位就從程式眼中消失——停損沒撤、但也沒人管了。
    - 沒平掉的部位記一個「待平倉」旗標，**每輪重試**，照第 8 條的節奏告警；等待期間停損守衛照常運作。
      不能只靠「下一根 K 棒再觸發一次出場條件」——5 分 K 的策略要 5 分鐘才會再試。
    - **交易所端的停損單也要等確認平掉之後才撤**（r14 新增）：跟帳上紀錄同一個道理。先撤停損、再送平倉單，
      被拒時部位還在、停損已經沒了——剛好是最需要保護的時候。順序是：送平倉單 → 確認這一側沒了 → 撤停損 → 結帳。
      「平倉前先撤停損，順便看它是不是已經觸發」的寫法要改成：平倉單失敗時查部位，沒了才去撤（撤單回「不存在」＝已觸發）。
    - **高頻觸發的出場路徑在待平倉期間要停下來**（r14 新增）：用即時報價（bookTicker 等）觸發停損的專案，
      平倉失敗後若不擋，每一筆報價都會再送一次平倉單。待平倉期間只讓「每輪重試」那條路送單。
  - **誰踩到（pump-dump-hunter）**（r13 新增）：手動平倉先把部位從帳上移除再送單；時間出場／穿價出場／停損掛不上的緊急平倉
    都不看成交數量；緊急平倉成功時連平倉紀錄都沒寫。另外撤舊停損失敗時只記進清單、要等平倉才開始計數告警（同 gold-scalper r11），
    部位平倉時失敗狀態無聲消失、補掛連續次數以幣名為鍵跨部位殘留（同 crypto-screener r12），
    成交後寫紀錄丟例外會一路拋到迴圈外層、「✅下單」通知消失。2026-09-21 對照 r12 時修正，
    `tests/test_r12.py` 在修改前的程式上 26 項失敗，另外 5 項逐一確認：4 項程式本來就對，1 項（-1000）碰巧對、原因不對。
  - **減碼由交易所端的停利單執行時**（r12 新增）：程式不會收到成交回報，只能在對帳時發現「數量變少了」。
    不偵測的話，不只沒有通知，**損益也會算錯**——平倉時用「全部數量 × 最後出場價」：
    出一半後追蹤到更高價出場會高估，出一半後打平出場會低估。
    → 對帳時比對交易所數量與帳上數量，變少就記一筆部分出場、立即通知、更新帳上數量；平倉損益改成各次出場加總。
    **沒有交易所停利單的專案也要做**（r13 補充）：在交易所 App 手動減碼、或 ADL 自動減倉，一樣會讓帳實不符；
    不偵測的話，之後的減碼單、停損單都用帳上的舊數量送出。pump-dump-hunter 沒有交易所停利單，2026-09-21 仍補上偵測。
  - **誰踩到（crypto-screener）**（r12 新增）：2026-09-21 對照 r11 時，r10、r11 的每一點都有——
    反向訊號路徑呼叫移損失敗時第 1 次被計數但沒告警；殘留單第 1 次告警只附在背景對帳的平倉通知裡，
    手動平倉、守衛強平、移損穿價出場三條路徑都收不到；補掛回報「已存在」時沒發恢復也沒歸零；
    部位平倉時移損與補掛的失敗狀態消失、沒有收尾通知，補掛次數還留在記憶體裡，同幣下次進場會接著數；
    進場成交後任何步驟丟例外，通知都寫「下單失敗」。另外**五張平倉市價單都不看結果**（上面「平倉單送出後一定要看結果」），
    以及交易所端「2R 出一半」從未被偵測（上面「減碼由交易所端執行」）——正式網第一筆 AVAX 帳上 +3.32R，實際約 +2.61R。
    改成所有通知都在失敗發生的函式裡產生、背景迴圈統一送出。`tests/test_r11.py` 在修改前的程式上 23 項有 18 項失敗；
    另外 5 項逐一確認過是程式本來就對。**已記錄的歷史交易無法自動更正**，累積的期望值帶有上述偏差。
  - **誰踩到（gold-scalper）**（r14 新增）：2026-09-21 對照 r13 時，平倉順序完全倒過來——先把帳上紀錄結掉、
    從記憶體移除、撤掉交易所停損，**最後才送平倉單**；被拒時只發一則「同步平倉失敗」，部位從程式眼中消失、而且沒有停損。
    改成上面的順序，平倉失敗保留部位與停損、每輪重試、照節奏告警；手動強制平倉被拒時回報失敗而不是「已平倉」。
    另外補上交易所數量比對（每 4 輪約 60 秒一次，第 6 條限流）：變少記部分出場並通知，連續 3 輪為 0 走平倉流程確認。
    `tests/test_lessons.py` 在修改前的程式上 25 項有 8 項失敗。
  - **「先撤停損」不一定寫在平倉函式裡**（r15 新增）：移損的流程本身就是「撤舊 → 掛新」（第 8 條第二種做法）。
    掛新停損被拒 `-2021`（價格已穿過）時若直接送平倉單，舊停損已經為了「移動」撤掉了——送單那一刻交易所上沒有任何停損。
    → `-2021` 時先把舊停損掛回去，再送平倉單；平掉了才撤，沒平掉就記待平倉。
    檢查時要看**所有會送平倉單的路徑**在送單那一刻交易所上有沒有停損，不只看「平倉函式」的順序。
  - **誰踩到（crypto-screener）**（r15 新增）：2026-09-21 對照 r14 時——平倉失敗只告警一次、沒有每輪重試；
    上面移損 `-2021` 的路徑；待平倉期間守衛強平與移損出場仍可能各自送單。手動平倉的順序本來就對。
    `tests/test_r14.py` 在修改前的程式上 19 項有 16 項失敗，另外 3 項逐一確認是程式本來就對。
  - **停損守衛補掛遇 `-2021` 也是一條出場路徑**（r16 新增）：停損不見了、補掛時價格已穿過停損價——停損本來就該觸發了。
    只告警「補掛失敗」的話，部位沒有停損、價格又在錯的那一邊，要等人處理。→ 直接走平倉流程（一樣看結果、失敗就待平倉）。
    這條路徑送單那一刻交易所上**本來就沒有停損**（正是因為不見了才要補），沒辦法先掛回去；能做的是盡快出場。
  - **誰踩到（pump-dump-hunter）**（r16 新增）：守衛補掛遇 `-2021` 只告警。另外逐條檢查了 r14、r15 的平倉順序：
    交易所停損在確認平掉之後才撤、待平倉期間只有每輪重試那條路送單、移損是「先掛新再撤舊」所以 `-2021` 時舊停損還在，
    這三項 r13 就是對的。
  - **誰踩到（gold-scalper）**（r17 新增）：**根本沒有停損守衛**——交易所上的停損（backstop）在 App 被撤掉、或因其他原因消失，
    程式完全不會發現，只有在停損價要移動時才會碰到它。2026-09-21 對照 r16 時補上守衛：每 4 輪（約 60 秒）
    帶 symbol 查掛單（權重 1），查詢失敗不動作、連續 3 輪確認不在才當場補掛，補掛遇 `-2021` 直接走平倉流程。
    Algo 查詢回 404 時改查舊端點（每次查詢當下判斷、不存旗標，所以重啟後也一樣）。
    **另外兩邊請檢查**：守衛是不是每個部位都會跑到，還是只在某些條件下（例如移損時）才碰到停損單。
  - **守衛有寫、但不一定每輪都跑到**（r18 新增）：背景迴圈常寫成一個 `try` 包住「對帳 → 移損 → 守衛 → 重試 → 送出告警」。
    前面任何一步丟例外，**這一輪的守衛就不跑、告警也送不出去**；如果是某筆部位資料造成的持續性例外，守衛**每一輪都不跑**。
    守衛自己的迴圈也一樣：某個部位丟例外，排在後面的部位整輪不檢查。
    → 迴圈的每一步各自 `try`；守衛對每個部位各自 `try`，出錯的部位照節奏告警（第 14 條：不能被 try 吞掉）；
    送出告警放在 `finally`。測試時讓前面的步驟丟例外，確認守衛與告警照樣執行；讓出錯的部位**排在前面**。
  - **誰踩到（crypto-screener）**（r18 新增）：2026-09-21 對照 r17 時——上面兩處都是單一 `try`；守衛補掛遇 `-2021` 只告警
    （同 pump-dump-hunter r16）。守衛本身每輪對所有部位都跑，沒有 gold-scalper 那種「只有移損時才碰到」的問題。
    `tests/test_r17.py` 在修改前的程式上 12 項全部失敗。
  - **「每個部位各自 try」還不夠，同一個部位的每一步也要各自 try**（r19 新增）：守衛迴圈常寫成
    對每個部位 `try: 重試平倉 → 重試移損 → 補掛停損 → 出場判斷`。部位之間互不影響了，但同一個部位裡，
    前面的重試一出錯，**這個部位的停損守衛就跳過**——偏偏會出錯的部位最需要守衛。→ 每一步各自 try。
  - **出錯次數也要跟著部位結束清掉**：跟補掛連續次數（第 8 條 r12）同一個道理，以幣名為鍵的出錯計數不清，
    同幣下一筆進場會接著數、節奏錯位。
  - **誰踩到（pump-dump-hunter）**（r19 新增）：2026-09-21 對照 r18 時——背景迴圈外層一個大 try（掃描器出錯，整輪對帳和守衛都不跑），
    對帳和出場管理又共用一個 try；守衛迴圈每個部位各自 try，但部位內四步共用一個，而且錯誤只寫錯誤區、不推播。
    改成背景迴圈拆出可單獨呼叫的一輪 `tick()`、四步各自 try；部位內三段各自 try；出錯照節奏推播、恢復時通知。
    `tests/test_r18.py` 在修改前的程式上 16 項失敗。
  - **維護步驟出錯，也不能讓出場判斷跳過**（r20 新增）：上面講的是「前面出錯、守衛跳過」；反過來也一樣——
    如果每輪流程是「維護（對帳、守衛）→ 訊號與出場判斷」，維護丟例外時**整輪中止，程式內的停損判斷也不跑**。
    程式內停損是最主要的保護，比守衛更不能被擋。→ 維護整段包 try，出錯照節奏推播，流程繼續走到出場判斷。
  - **誰踩到（gold-scalper）**（r20 新增）：每輪開頭「撤殘留單 → 維護（認領 → 平倉重試 → 數量比對 → 守衛）」沒有任何 try，
    任何一步丟例外，後面的守衛跳過、而且整輪 tick 中止，連出場判斷都不跑。2026-09-21 對照 r19 時改成每一步各自 try，
    出錯照節奏推播、恢復時通知；以步驟名為鍵的出錯次數在部位結束時清掉並發收尾通知。
    `tests/test_lessons.py` 在修改前的程式上 44 項有 9 項失敗。
  - **通知也是一步**（r21 新增）：「對帳記了平倉 → 推播平倉通知」這種寫在對帳後面、沒有自己 try 的通知，
    一出錯（格式化時某個欄位是 None、網路例外）就中斷整輪——後面的守衛與待平倉重試（出場）都不跑。通知要跟其他步驟一樣各自 try。
  - **錯誤收進「事件」也算吞掉**：迴圈逐部位 `try`，但 except 只把錯誤包成一筆事件回傳、呼叫端只寫一行錯誤區——
    隔離做對了，錯誤卻沒人看到（第 14 條）。出錯要照節奏推播。
  - **誰踩到（crypto-screener）**（r21 新增）：2026-09-21 對照 r20 時——平倉通知與認領通知沒有自己的 try（平倉通知出錯 → 該輪守衛、
    待平倉重試全部跳過）；移損逐部位 try 但錯誤只進事件；待平倉重試、殘留單重試、對帳的兩個逐部位迴圈沒有 try
    （殘留單那個還會在出錯時把該張從清單丟掉）；背景迴圈步驟與守衛的出錯次數恢復時不歸零、部位結束時不清。
    改成共用的「部位步驟出錯」計數（步驟＋幣為鍵、照節奏推播、恢復通知、部位結束時清掉並收尾）。
    `tests/test_r20.py` 在修改前的程式上 11 項全部失敗。
  - **包 try 的時候，except 不能把「已經做完的動作」當成沒做**（r22 新增）：對帳逐部位包 try 之後，如果一筆的流程是
    「結帳（寫平倉紀錄、撤停損）→ 發通知」，通知出錯被同一個 try 接住，except 照「保守保留在帳上」處理——
    部位已經結帳、停損已經撤了，卻又被放回帳上，下一輪再結一次帳。→ 通知要自己包 try（讓組字串也在 try 裡），
    不能跟不可逆的動作共用一個 except。
  - **except 裡組告警不能再用壞掉的那筆資料**（r22 新增）：逐筆重試的 except 寫 `f"{x['symbol']} 撤不掉"`，
    剛好就是 `x['symbol']` 壞掉時進到這裡——except 裡再拋一次 `KeyError`，整段中止，後面每一筆都不處理。
    告警訊息一律用 `.get()` 組；壞掉的那筆留在清單、照節奏推播，不能被丟掉。
  - **誰踩到（pump-dump-hunter）**（r22 新增）：2026-09-21 對照 r21 時——對帳的兩個逐部位迴圈沒有 try（同 crypto-screener）；
    `telegram.send` 本身有 try，但訊息是呼叫前就組好的，格式化出錯照樣拋；殘留單重撤是上面「except 裡再拋一次」；
    整段重撤出錯只寫日誌、不推播。背景迴圈、守衛逐步驟 try 與出錯次數的清理 r19 已做。
    `tests/test_r21.py` 在修改前的程式上 15 項失敗。
  - **反過來也一樣：結帳之前出錯，不能把部位弄丟**（r23 新增）：常見寫法是先把部位從「持倉中」取走（避免兩條路同時平倉），
    再送平倉單、確認、結帳。取走之後、結帳之前任何一步丟例外（例如確認查詢本身拋錯），例外往外傳、部位也沒放回——
    **程式就忘了這個部位**，交易所上的倉沒人管，之後還可能再開新倉。
    → 以「結帳」為界包一層：界線之前出錯＝放回帳上、記待平倉、每輪重試；界線之後出錯＝不可逆、只推播。
    用「外層包裝函式＋原函式改名」做，原函式內容不用重新縮排（第 14 條 r22）。
  - **背景執行緒裡的例外沒人接**（r23 新增）：「另開執行緒送平倉單」這類 fire-and-forget 寫法，執行緒裡丟的例外
    不會回到主迴圈，不計數、不推播，只在標準錯誤印一行。執行緒的進入點要包一層，跟其他步驟一樣照節奏推播。
  - **誰踩到（gold-scalper）**（r23 新增）：2026-09-21 對照 r22 時——平倉沒有外層保護，確認前出錯部位就被弄丟、
    確認後出錯例外往外拋；快速停損（即時報價觸發）在背景執行緒送平倉，出錯沒人接；殘留單重撤沒有逐筆 try，
    告警用 `item['symbol']`（r22 說的「except 裡再拋一次」的前身）。`exit_price` 缺值時算損益會拋錯，改成用最新價補。
    `tests/test_lessons.py` 在修改前的程式上 51 項有 7 項失敗（另有 4 項只因為新增的「回傳結果」而失敗，
    拿掉那條斷言後在修改前的程式上成立）。
  - **結帳本身要排在最前面，算損益不能丟例外**（r24 新增，r22 那點的另一種樣子）：結帳函式常寫成
    「撤剩下的條件單 → 發收尾通知 → 算損益 → 寫紀錄、移出帳」。撤單是不可逆的；後面的通知或算損益一丟例外
    （某個欄位缺了、格式化時型別不對），條件單已撤、部位卻還在帳上——下一輪對帳又結一次、又丟一次，**這筆永遠結不了帳**。
    → 順序改成「算平倉紀錄（只用 `.get()`，缺欄位時損益記為未知）→ 寫紀錄、移出帳、存檔（界線）→ 撤單、通知、統計各自 try」。
  - **網頁請求也是另一條執行緒**（r24 新增，r23「背景執行緒」的另一種樣子）：手動開倉、平倉從網頁送出時，
    請求處理本身就在另一條執行緒；例外穿出去，網頁只看到連線中斷，沒有推播、使用者要平倉的意圖也沒留下。
    交易操作的入口要包一層：回錯誤給網頁、照節奏推播；平倉在結帳前出錯就記待平倉。
  - **誰踩到（crypto-screener）**（r24 新增）：2026-09-21 對照 r23 時——結帳是上面「撤單在前、結帳在後」的順序；
    手動平倉沒有外層保護（crypto-screener 不是「先取走部位」的寫法，部位不會弄丟，但例外往外傳、待平倉沒記）；
    監控執行緒（會自動下單）出錯只寫錯誤區不推播；暖機執行緒沒包；網頁交易操作沒包。except 裡組訊息有一處用 `o["type"]`。
    `tests/test_r23.py` 在修改前的程式上 12 項有 8 項失敗，另外 4 項確認是程式本來就對。
  - **誰踩到（pump-dump-hunter）**（r25 新增）：2026-09-21 對照 r24 時——
    結帳是「撤停損 → 收尾通知（`:.6g` 格式化）→ 算損益 → 寫紀錄」，資料型別不對時通知一拋，停損撤了、紀錄沒寫；
    對帳逐部位的 try（r22）接住後把它「保守保留在帳上」，下一輪再結、再拋——正是 r24 說的「永遠結不了帳」。
    平倉函式在交易所已經平掉之後用 `pos["entry"]` 算損益，缺欄位就拋。背景執行緒是 `while True: tick()`，
    `tick()` 裡每一步都有 try，但 `tick()` 本身出錯時整條執行緒結束、機器人停止運作，只在標準錯誤印一行。
    網頁的手動平倉有 try，但只把錯誤回給網頁：不推播、結帳前出錯也不記待平倉。
    pump-dump-hunter 不是「先取走部位」的寫法（平倉期間部位一直留在帳上，確認平掉才移出），r23 那點本來就對。
    修正：結帳改成「算紀錄（只用 `.get()`）→ 寫紀錄、移出帳（界線）→ 撤單、通知各自 try」；
    背景執行緒與網頁交易入口都照「外層包裝＋原函式改名」包一層。`tests/test_r24.py` 在修改前的程式上 17 項失敗。
  - **通知的格式化，測試常常根本沒跑到**（r26 新增）：交易流程的測試通常把「送通知」整個 mock 掉，
    所以「損益是 None 時 `:.2f` 會拋」這種錯，所有測試都通過、上線才炸。→ 另寫一項不 mock 通知本身、只攔最底層送出的測試，
    餵缺欄位的資料，確認訊息真的組得出來。
  - **誰踩到（gold-scalper）**（r26 新增）：2026-09-21 對照 r25 時——確認平掉之後是「撤停損 → 出錯計數收尾 → 算損益 → 寫紀錄 → 移出帳」，
    撤單在前、算損益會拋；網頁的手動測試下單、測試平倉、設定槓桿、取消掛單四個入口沒有保護（強制平倉走引擎的平倉包裝，本來就有）；
    背景迴圈 `tick()` 出錯不會讓執行緒結束，但只寫日誌不推播；平倉通知在損益未知時格式化會拋（上一點）。
    改成「算紀錄（失敗時損益記未知）→ 寫紀錄、移出帳（界線）→ 撤停損、收尾各自 try」；網頁入口用裝飾器包一層
    （`functools.wraps` 保留參數，FastAPI 讀得到）。`tests/test_lessons.py` 在修改前的程式上 58 項有 7 項失敗。
  - **`.get(鍵, 預設值)` 擋不住「鍵存在、值是 None」**（r27 新增）：r22 說「組告警用 `.get()`」，但 `t.get("opened", 0)`
    在紀錄裡有 `"opened": None` 時回的是 None，不是 0——接著相減照樣拋。r24 起結帳遇缺欄位會把值記成 None，這種紀錄一定會出現。
    → 數字先確認型別（`isinstance(v, (int, float))`）再計算或格式化；`.get(k) or 預設` 只適用於 0 也當成缺值的欄位。
  - **統計也要處理「損益未知」**（r27 新增）：勝率、期望值常寫成 `(x["pnl"] or 0) > 0` 分勝負——損益未知被算成**虧損**，
    勝率被拉低、期望值偏差。→ 損益未知的交易不算勝負，另外計數，並在通知與畫面上講明「另有 N 筆損益未知」。
  - **誰踩到（crypto-screener）**（r27 新增）：2026-09-21 對照 r26 時——平倉通知用 `t.get("closed", 0) - t.get("opened", 0)`
    （上面那點）、損益未知時標題寫「虧損平倉」；結帳時資料壞掉的端到端情境裡，使用者收到的是「⚠ 平倉通知出錯」而不是平倉通知。
    統計與分版本統計都把損益未知算成虧損；網頁把損益未知顯示成「+0.00 U」。背景迴圈外層出錯只寫錯誤區（r26 gold-scalper 同）。
    `tests/test_r26.py` 在修改前的程式上 9 項有 6 項失敗（另 3 項測的是測試基礎設施本身）。
  - **「估算」也不能把未知包裝成已知**（r28 新增）：成交價查不到時，常見寫法是退而用訊號價估算損益——
    算出來的是一個看起來確定的數字，但訊號價不是這張單的實際成交價，那個「損益」是假的已知。
    → 估算只用實際成交價；查不到就記未知，實際損益交給交易所的成交明細。成交明細查得到時以它為準，那是對的。
  - **`or 預設` 在時間戳、計數這類欄位上**（r28 補充 r27）：改寫 `.get(k, 預設)` 時很容易順手寫成 `.get(k) or 預設`——
    0 被當成缺值。pump-dump-hunter 對照 r27 時，讀完這條、改程式時照樣犯了一次（K 棒時間戳 `last_t` 是 0 時被換成預設值），
    是舊測試失敗才發現。→ 值是 None 才用預設，一律明寫 `if v is None`。
  - **誰踩到（pump-dump-hunter）**（r28 新增）：2026-09-21 對照 r27 時——偵測到數量減少時用 `rec.get("fill", rec["entry"])` 算損益，
    成交價是 None 時照樣拿 None 相減，**整個對帳那一筆出錯**；出場判斷用 `pos.get("last_t", start)`，`last_t` 是 None 時
    **每根 K 棒都拋錯、這個部位的出場判斷整個停擺**；部分出場算不出損益時寫 `pnl=est or 0`，最後整筆損益用估算加總時
    未知那段被當成 0，通知寫「損益 +0.00 U」（crypto-screener 網頁顯示的同一個症狀）。沒有勝率、期望值這類即時統計；
    已平倉表遇到未知原本顯示空白，改成顯示「未知」並在標題講明筆數。通知本來就是攔在最底層的 `telegram.send`
    （它只收組好的字串），格式化在測試裡一直有執行到，這點本來就對。`tests/test_r27.py` 在修改前的程式上 14 項失敗。
  - **風控遇到損益未知**（r29 新增）：每日虧損上限、連續虧損上限這類斷路器，把未知當 0 會低估虧損、當虧損會誤觸發。
    兩者都不對——不算進去，但**把筆數放進風控狀態**（網頁與推播看得到「另有 N 筆損益未知」），交給人判斷。
    這表示斷路器在有未知筆數時可能該觸發而沒觸發；未知筆數多時要人工查交易所成交明細。
  - **誰踩到（gold-scalper）**（r29 新增）：2026-09-21 對照 r28 時——r26 起結帳遇缺欄位會把損益記成未知，但下游全都沒跟上：
    勝率分母包含未知（等於算成虧損）；風控每日損益 `or 0.0` 當 0、連續虧損當虧損；**網頁成交清單對未知呼叫 `toFixed`，整份清單畫不出來**；
    平倉通知在真實成交價查不到時用「模擬點數 × 張數」寫成「≈ X USDT」；**網頁總損益的 USDT 是全部模擬點數 × 張數**，
    連沒真的下過單的模擬交易都算進去；App 手動減碼／ADL 用標記價估損益。全部改成只用真實成交價、查不到記未知並講明筆數。
    `tests/test_lessons.py` 在修改前的程式上 65 項有 6 項失敗。
  - **出場價「標記價，查不到再用進場價」＝損益剛好 0**（r30 新增，r28「估算」的更隱蔽版本）：它不是寫在「估算」分支裡，
    而是每一條出場路徑的一行 `px = mark_price(sym) or pos["entry"]`。標記價是偵測當下的價格、不是成交價；
    標記價查不到時退回進場價，損益就算成 0——通知寫「+0.00 U」、統計算成打平，完全看不出是假的。
    → 出場價只用實際成交價：自己送的平倉單看回應裡的成交均價；交易所端觸發的停損停利查成交明細（`userTrades`）；都沒有就記未知。
    部分出場也一樣（停利**觸發價**也不是成交價）。有基準部位（同側有別人的倉）時，成交明細分不出哪幾筆是自己的，記未知。
  - **成交明細的時間界線要用「已採用的最後一筆」，不是「偵測當下」**（r30 新增）：部分出場後，之後的出場價只該看那之後的成交。
    用偵測當下的時間當界線，前一段那筆成交的時間戳可能落在同一毫秒內，最後出場時又被算進去——出場均價被拉偏。
  - **風控遇到損益未知的另一個地方：冷卻**（r30 補充 r29）：「獲利後短冷卻、虧損後長冷卻」這類規則，未知被記成「沒贏」就套了
    虧損的冷卻、訊息寫「剛虧損平倉」。crypto-screener 的選擇：未知套較長的冷卻（只是等久一點，不影響統計），但訊息寫「損益未知」。
  - **誰踩到（crypto-screener）**（r30 新增）：2026-09-21 對照 r29 時——7 條出場路徑全部是上面那行（手動平倉、待平倉重試、
    對帳偵測、守衛兩條、移損穿價）；部分出場用停利觸發價或標記價，查不到時算出一個數字；結帳把部分出場的未知損益 `or 0` 加總；
    每日虧損上限本來就不加未知（R 倍數是 None），但沒有記筆數；冷卻把未知當虧損。自檢的 pending 逾時用 `p.get("ts") or now`，
    時間戳是 0 時被當成「剛送出」，逾時的 pending 被藏起來。`tests/test_r29.py` 在修改前的程式上 9 項有 7 項失敗
    （另 2 項：通知格式化 r26 起就沒被 mock、錯誤攔截在 `sys.stderr` 這一層涵蓋所有模組，本來就對）。
  - **平倉成交不能用 `realizedPnl ≠ 0` 篩**（r31 新增）：從成交明細挑「這筆是不是平倉」時，寫成 `realizedPnl != 0` 很直覺——
    開倉那筆的 realizedPnl 是 0。但**打平出場**（例如移到成本後在成本價被停損）那筆平倉的 realizedPnl 也剛好是 0，整筆被濾掉，
    出場價與損益變成未知、或退到估算。→ 用方向判斷（多單的平倉是 SELL），不看 realizedPnl。
  - **成交明細的界線用成交 id**（r31 補充 r30）：「已採用的最後一筆」用 id 記，不用時間——同一毫秒內可能有好幾筆成交。
  - **誰踩到（pump-dump-hunter）**（r31 新增）：2026-09-21 對照 r30 時——完整平倉的六條路徑（手動、時間、兩種穿價、緊急平倉、待平倉重試）
    都用平倉單回應的成交均價、查不到記未知，**本來就對**；交易所停損觸發那條走成交明細，也對。但：
    偵測到數量減少那條**正是 r30 那一行** `px = markPrice or entryPrice`（r28 只改了參考價那一端）；1R 減碼是程式自己送的市價單，
    回應沒讀、也沒記成部分出場，成交明細查不到時最後的估算只算剩下那一半，卻呈現成已知；成交明細用上面那個 `realizedPnl ≠ 0` 篩；
    界線是開倉時間，最後的出場價被前一段拉偏；有基準部位時也沒記未知。pending 時間戳是 None 時每輪拋錯、永遠卡著。
    沒有依輸贏調整的冷卻，也沒有每日虧損、連續虧損這類斷路器。改成共用的 `adopt_partial()`／`close_info()`：
    界線用最後採用的成交 id、方向判斷平倉成交、有基準記未知、各段損益加總（任一段未知整筆未知）。
    `tests/test_r30.py` 在修改前的程式上 10 項失敗。
  - **誰踩到（gold-scalper）**（r32 新增）：2026-09-21 對照 r31 時——**從來沒查過成交明細**。「交易所端部位已不在」
    （App 手動平倉、backstop 在服務中斷時觸發）那條路的出場價是 `標記價 or 最新價 or 停損價`，帳上的點數損益就用它算，
    看起來是確定的數字。只有 backstop 觸發時查條件單狀態的成交價，而且是在結帳之後才查、只更新 USDT 那一欄。
    數量減少（r28）已改成記未知，但查得到成交明細時也沒去查。
    改成共用的 `_closing_fills()`：界線從開倉那張單的最後一筆成交 id 起算、之後用已採用的最後一筆 id；方向挑平倉成交；
    有基準記未知；交易所端平掉時出場價只用它（帳上的點數損益也用它），查不到記未知；數量減少查得到就採用並推進界線；
    整筆 USDT 損益＝各段加總，任一段未知整筆未知。
    限制：開倉單號只存在記憶體，服務重啟後定不出界線，那筆的出場價會記未知（不會算錯，只是不知道）。
    `tests/test_lessons.py` 在修改前的程式上 71 項有 5 項失敗。
  - **pending 時間戳缺值的另一種樣子：靜靜卡住**（r33 新增，r31 pump-dump-hunter 那點的變形）：寫成
    `age = now - (ts if 是數字 else now)` 時不會拋錯——缺值被當成「0 歲」，**永遠不會到期**。這個幣永遠不能再下單、
    還一直佔一個持倉名額，而且沒有任何錯誤訊息。→ 缺值當成已經逾時，走逐幣確認後認領或判定未成交。
  - **誰踩到（crypto-screener）**（r33 新增）：2026-09-21 對照 r32 時——平倉成交本來就用方向判斷（沒有用 `realizedPnl`）；
    界線是「已採用最後一筆的時間＋1」（r30 我寫進清單的做法），同一毫秒的第二筆會被跳過，改成成交 id（`fromId`）；
    pending 時間戳缺值是上面那點。`tests/test_r32.py` 在修改前的程式上 5 項有 2 項失敗（另 1 項本來就對、2 項是工具本身的檢查）。
  - **缺值會流到下游：認領時把 None 帶進部位**（r34 新增，r33 那點的第三種樣子）：pending 缺時間戳時不拋錯、也不卡住，
    但認領時寫成 `ts=rec.get("ts", now)`——鍵存在、值是 None，認領回來的部位時間戳就是 None。之後查成交明細的界線退成 0，
    **這個幣歷史上所有的平倉成交都被算成這筆的出場**：不是未知，是算錯。→ 用到時間戳、界線的地方，一律先確認是數字。
  - **起始界線不要用「開倉時間往前幾秒」**（r34 新增，r32 gold-scalper 那點）：為了容忍時鐘誤差，常寫成「開倉時間 − 5 秒之後的平倉成交」。
    開倉前幾秒同幣的平倉（上一筆部位、別的專案）會被算進來，出場價被拉偏。→ 開倉成交後、認領當下，記下成交明細**最後一筆的 id**
    當起始界線；查不到才退回用時間。
  - **雙向模式的成交明細要看 `positionSide`**（r34 新增）：多單的平倉是 SELL，但別的專案同幣**開空單**也是 SELL。
    只用方向挑平倉成交，雙向模式下會把別人的開空算成自己的平倉。成交明細有 `positionSide` 欄位，要一起比對。
  - **誰踩到（pump-dump-hunter）**（r34 新增）：2026-09-21 對照 r33 時——r31 的修法是「先逐幣確認有沒有部位、有就認領、沒有才看時間戳，
    缺值判定逾時」，**沒有變成「缺值當 0 歲」**，這點本來就對（測試在修改前的程式上通過）。但上面三點都中：
    認領時的時間戳、起始界線用開倉時間往前 5 秒、沒看 `positionSide`。`tests/test_r33.py` 在修改前的程式上 4 項失敗。
  - **只存在記憶體的期限，重啟後就是缺值**（r35 新增，r33 那點的第四種樣子）：pending 的確認期限如果只放記憶體、資料庫只記「不明」，
    服務重啟後還原的部位**沒有期限**。如果流程寫成「有期限才去確認」，這個部位永遠不會被確認——不掛交易所停損、不比對數量、
    不跑守衛，也沒有任何訊息。→ 「不明而且沒有期限」當成已經逾時，逐幣確認後認領或判定未成交；只對實盤引擎做，純模擬引擎不能去查交易所。
  - **起始界線要在開倉當下記、存進資料庫**（r35 補充 r34）：界線如果是「平倉時才去最近 N 筆成交裡找開倉那張單」，
    持倉期間成交一多，開倉那筆就掉出查詢範圍（`userTrades` 有筆數上限），只能記未知；服務重啟後也沒有。
  - **誰踩到（gold-scalper）**（r35 新增）：2026-09-21 對照 r34 時——上面兩點都中：確認期限只存記憶體，重啟後的「不明」部位永遠卡著；
    界線平倉時才算、沒存資料庫。另外界線是空字串時舊程式直接 `int("")` 拋錯（不是退成 0，但那一步就出錯）。
    r34 的「認領時 None 退成 0」沒中：認領回來的部位沒有開倉單號，定不出界線時本來就記未知。`positionSide` 本來就有比對。
    改成：不明而沒期限當成逾時（只限實盤引擎）；開倉成交後記下開倉那張單最後一筆成交的 id、認領當下記下最後一筆 id，
    連同開倉單號存進資料庫，界線推進時也存；界線不是整數就記未知。`tests/test_lessons.py` 在修改前的程式上 79 項有 7 項失敗。
  - **用開倉時間當界線的另一個後果：交易所時鐘比本機慢**（r36 新增，r34 那點的反面）：界線是「開倉時間（本機時鐘）之後的平倉成交」時，
    交易所的時間戳如果比本機慢幾秒，平倉成交的時間戳會早於開倉時間，被篩掉——出場價記成未知。不是往前幾秒的問題，是往後。
    → 同 r34：開倉成交後用開倉那張單的單號查它的成交、記下最後一筆 id；時間只在查不到 id 時才用。
  - **存檔失敗不能被吞掉**（r36 新增，第 14 條的另一種樣子）：狀態檔是成交界線、待平倉、pending 能跨重啟的唯一依據。
    存檔寫成 `try: … except: pass` 時，磁碟滿、權限、資料無法序列化都**完全沒有訊息**——下次重啟才發現界線、待平倉全沒了。
    → 存檔失敗要寫錯誤區、照節奏推播、恢復時通知。
  - **誰踩到（crypto-screener）**（r36 新增）：2026-09-21 對照 r35 時——認領時 `"opened": pd.get("ts")`，時間戳是 0 時界線退成 0，
    測試重現**出場價 72.5**（把歷史上一筆 50.0 的平倉成交混進 95.0 這筆），是算錯不是未知；起始界線是平倉時才用開倉時間查，
    沒在開倉當下記（交易所時鐘慢 10 秒時出場價記成未知）；存檔 `except: pass`。雙向模式本來就比對 `positionSide`；
    「不明而沒期限」在 r33 已當成逾時，重啟後照樣被確認（測試用真的存檔、重新載入模組模擬重啟）。
    改成開倉用單號、認領用最近一筆記下成交 id 界線，跟著部位存進狀態檔；界線是 0 或非數字時記未知。
    `tests/test_r35.py` 在修改前的程式上 6 項有 4 項失敗（另 2 項本來就對）。
  - **讀檔的 `except: pass` 比存檔的更嚴重**（r37 新增）：存檔失敗至少記憶體裡的狀態還是對的；**讀檔**失敗時靜靜回到空白，
    持倉、成交界線、待平倉、pending 全部消失，而且之後第一次存檔就把壞檔蓋掉、連證據都沒了。直接覆蓋寫檔的話，寫到一半當機就會留下壞檔。
    → 讀檔失敗：壞檔另存一份、記進錯誤區、開機推播；存檔：先寫暫存檔再換名。
  - **存檔失敗的通知路徑會死鎖**（r37 新增）：存檔在狀態鎖裡面執行，失敗時推播；推播本身失敗時（r29 起）又回頭寫錯誤區、
    再拿同一把鎖——`threading.Lock` 不可重入，**整支程式卡住**。測試裡推播被攔截，所以測不到；用執行緒加逾時重現才抓到。
    → 狀態鎖用 `RLock`；或存檔失敗時不經過會拿鎖的路徑寫錯誤區。
  - **有 id 界線時還用時間篩**（r37 補充 r36）：r36 說「時間只在查不到 id 時才用」。pump-dump-hunter r34 雖然記了 id，
    篩選條件卻是「id 之後 **而且** 時間在開倉之後」——交易所時鐘慢時照樣被時間篩掉。檢查的是篩選條件本身，不是有沒有記 id。
  - **成交明細只查最近 N 筆：不完整的資料會算出「確定的」錯數字**（r37 補充 r35）：平倉時才找界線會變未知；
    但界線有記、查詢卻只拿最近 50 筆時，界線之後的平倉成交掉了幾筆，程式照樣用剩下的算出出場價與損益——比未知更糟。
    → 帶 `fromId`（界線＋1）往後查、分頁到拿完。
  - **誰踩到（pump-dump-hunter）**（r37 新增）：2026-09-21 對照 r36 時——pending 存在狀態檔、每輪都確認，重啟後照樣被確認
    （用真的存檔、重新載入模組測過），**本來就對**；起始界線開倉當下記、跟著部位存進狀態檔，**本來就對**。但：
    有 id 界線時還用時間篩（上面那點）；成交明細只查最近 50 筆；界線與時間戳都無效時退成 0；
    狀態檔讀取 `except: pass`、直接覆蓋寫檔、存檔失敗往外拋進交易流程；守衛撤自己的殘留停損失敗時 `except: pass`；
    加上修的時候自己引入的死鎖。`tests/test_r36.py` 在修改前的程式上 12 項斷言失敗、1 項被測程式拋錯。
  - **狀態存資料庫也一樣：讀取函式的「失敗」與「沒有」共用同一個回傳值**（r38 新增，r37 讀檔那點在資料庫上的樣子）：
    `except: return None` 跟「查無資料」回的一模一樣——資料庫短暫連不上的那次重啟，引擎以為自己空手，不管交易所上的真實部位、還可能再開新倉。
    → 讀取函式回 `(ok, 值)`；讀不到時**這個引擎暫停開新倉**、每輪重試、照節奏推播、恢復時通知；對帳講明「持倉紀錄還沒載入，無法對帳」。
    讀資料庫不要包在引擎的鎖裡面做（讀取失敗的推播路徑就在鎖裡了）。
  - **誰踩到（gold-scalper）**（r38 新增）：狀態存在 PostgreSQL，沒有狀態檔，所以暫存檔換名不適用；其他幾點都中：
    開機讀持倉紀錄 `except → return None`（跟沒有持倉一樣）；11 個寫入函式全部只寫日誌；引擎的鎖是不可重入的 `Lock`、
    開機讀資料庫在鎖裡面；成交明細只查最近 100 筆、沒有 `fromId`。篩選條件裡本來就沒有時間（只看 id 與方向）。
    改成讀取回 `(ok, 值)`、沒載入不開倉；寫入失敗照節奏推播、恢復通知（推播路徑不拿引擎的鎖）；`RLock`、讀取移到鎖外；
    界線之後帶 `fromId` 分頁查到拿完，超過頁數上限記未知。`tests/test_lessons.py` 在修改前的程式上 85 項有 6 項失敗。
  - **讀取失敗期間，存檔不能覆寫原檔**（r39 新增，r37 讀檔那點的後半）：讀檔失敗後記憶體是空白的；如果照常存檔，
    原檔被一份空白狀態蓋掉，下一次重試讀取就「成功」了——**假恢復**，持倉紀錄從此消失，連告警都停了。
    → 讀取失敗期間暫停寫原檔（壞檔也另存一份），讀到了才恢復存檔。資料庫的做法同理：沒載入時不寫會覆蓋舊資料的那幾張表。
  - **讀到一半出錯會留下半套狀態**（r39 新增）：一邊解析一邊寫進全域狀態時，第 5 個欄位出錯，前 4 個已經換成新的——
    持倉是新的、成交紀錄是舊的。→ 先全部解析到暫存，全部成功才一次換上。
  - **手動開倉不經過自動下單的閘門**（r39 新增）：「沒載入不開倉」寫在自動下單的判斷函式裡時，網頁的手動開倉直接呼叫開倉函式，照樣送單。
    → 開倉函式本身也要擋。
  - **推播失敗也不能被吞掉**（r39 新增）：推播函式對每個通道 `except: pass`，Telegram 設定錯、網路斷，所有告警**完全沒送出也沒有訊息**。
    → 寫錯誤區（不再推播、不經過任何鎖，避免遞迴與死鎖）。
  - **誰踩到（crypto-screener）**（r39 新增）：2026-09-21 對照 r38 時——讀狀態檔 `except: pass`（上面幾點都中：沒有暫停開新倉、
    手動開倉也擋不住、讀取失敗後照常存檔會蓋掉原檔、讀到一半出錯留半套）；存檔直接覆蓋寫；推播四個通道全部 `except: pass`；
    成交明細只查一頁（100 筆，r29 起「數量要湊滿」擋住了，記的是未知而不是錯數字，但仍該分頁）。
    有 id 界線時篩選條件本來就只看 id 與方向；推播失敗的路徑不拿鎖，**用執行緒加逾時驗證過不會死鎖**。
    改成：讀取回 `(ok, 錯誤)`、壞檔另存、暫停開新倉（自動與手動都擋）、每輪重試、恢復通知、讀取失敗期間不寫原檔、先解析再一次換上；
    存檔先寫暫存檔再換名；推播失敗寫錯誤區；成交明細帶 `fromId` 分頁、超過上限記未知；自檢講明「持倉紀錄還沒載入，無法對帳」。
    `tests/test_r38.py` 在修改前的程式上 8 項有 6 項失敗（另 2 項本來就對）。
  - **誰踩到（pump-dump-hunter）**（r40 新增）：2026-09-21 對照 r39 時——r37 讀檔失敗時不再靜靜回到空白，但記憶體仍是空的、
    **照常開新倉、照常對帳**（交易所上的真實部位會被當成別人的）；只記一次錯誤、沒有重試。r37 的壞檔是**改名**另存，原路徑空了，
    下一次存檔寫出空白狀態——**假恢復**，測試在修改前的程式上重現了（原檔被換成 `{"watch": [], …}`）。
    狀態檔本身是整份解析完才一次更新，沒有半套問題（本來就對）；但**參數覆蓋**是一邊迴圈一邊套用，第二個欄位格式錯時第一個已經換掉，
    實盤參數停在半套。開倉只有一個入口 `place()`，擋在那裡自動與手動都擋到；另外擋了手動**認領**（會把部位寫進還沒載入的帳）。
    推播送出失敗的路徑 r30 已寫錯誤區，但**沒設 `TG_TOKEN` 時直接 `return`**——那次部署所有告警完全沒送出也沒有紀錄。
    改成：讀取回 `(ok, 值)`、檔案不存在才是「沒有資料」；失敗時複製壞檔（原檔留著）、暫停開新倉與認領、對帳不動帳並講明、
    暫停寫原檔（這段期間的狀態寫到旁邊的 `.unloaded` 留作對照）、背景迴圈每輪重試、照節奏推播、恢復通知；
    參數覆蓋先在副本上套用、全部成功才換上；推播沒設定記進錯誤區一次、自檢列出來。`tests/test_r39.py` 在修改前的程式上 16 項失敗。
  - **比半套更隱蔽：不合法的值「跳過」、其他照套、回報成功**（r41 新增，r40 參數覆蓋那點的變形）：
    一邊迴圈一邊套用時，如果不合法的值是 `continue` 跳過而不是拋錯，根本不會停下來——參數集裡一個值打錯，
    其他欄位全部套上、畫面顯示「成功」，實盤參數停在使用者沒想要的組合，而且沒有任何人知道。
    → 先整批驗證，有一個不合法就整批不套用、回報是哪幾個欄位；只驗欄位名稱不夠，值也要驗。
  - **誰踩到（gold-scalper）**（r41 新增）：2026-09-21 對照 r40 時——全域設定更新與引擎專屬覆寫（匯入參數集也走這裡）都是上面那樣：
    值轉型失敗就 `continue`，其他照套、回報成功；匯入參數集只先檢查了欄位名稱。網頁手動測試下單直接送單、不經過引擎，
    持倉紀錄沒載入時照樣送。Telegram 沒設定時 `return`（呼叫端都不看回傳值），送出失敗只寫日誌。
    狀態存資料庫，沒有狀態檔，r39 的「讀取失敗期間覆寫原檔」與「讀到一半」不適用（持倉紀錄是一次換上；沒載入時引擎不做任何寫入）。
    改成：整批驗證、一個不合法就整批不套用；手動測試下單擋在「持倉紀錄沒載入」；推播錯誤區（只寫清單與日誌，不拿鎖、不再推播），
    沒設定記一次、`/notify/status` 與交易所自檢列出來；設定寫入資料庫失敗也照節奏推播。`tests/test_lessons.py` 在修改前的程式上 91 項有 5 項失敗。
  - **讀取失敗期間原檔「不見了」也是假恢復**（r43 新增，r40 改名那點的另一個入口）：就算壞檔是複製、原檔留著，
    讀取失敗期間原檔被人搬走或改名，下一次重試讀到「檔案不存在」——如果照開機時的規則當成「全新開始」，就恢復了開新倉，帳是空的。
    → 讀取失敗期間「不存在」仍是失敗；確定要全新開始，要放一份內容為 `{}` 的檔案。
  - **跟持倉紀錄無關的設定檔也一樣**（r43 新增）：推播設定（Telegram 聊天室）讀不到、靜靜回到預設，之後所有告警完全沒送出；
    存檔又把壞檔蓋掉。→ 設定檔讀寫比照狀態檔：檔案不存在才是「沒設定」、壞檔複製一份、讀取失敗期間不覆寫原檔、存檔失敗照節奏推播。
  - **誰踩到（crypto-screener）**（r43 新增）：2026-09-22 對照 r42 時（上次停在 r39，一併對照 r40、r41）——
    讀取失敗期間原檔不見了就恢復開新倉（上面那點）；讀取失敗期間存檔直接跳過（網頁改的設定靜靜丟掉，改成寫 `.unloaded`、回應講明只暫存）；
    手動對帳回「無異動」（改成講明無法對帳）；帳沒載入時撤孤兒單照樣撤。交易設定值錯 `continue`、超出範圍悄悄夾到邊界、
    欄位名稱打錯也回成功；自動下單設定 `on` 先打開、後面的值才丟例外（停在半套）；監控設定觀察清單先換掉、後面才丟例外。
    `notify_telegram` 每個聊天室各自 `except: pass`——r39 只在外層接例外，**測試把整個 `notify_telegram` 換掉**，測不到裡面那層
    （用法第 5 點「測試不能 mock 掉被測的那一段」又一次）；沒有任何推播管道時完全沒有紀錄；telegram.json／monitor.json 讀寫 `except: pass`。
    壞檔本來就是複製、不是改名；手動開倉與認領 r39 已擋。改成整批驗證（名稱與值都驗、一個不合法就整批不套用並列出欄位）、
    推播錯誤區（只寫清單與日誌，不拿鎖、不再推播）、沒設定記一次、自檢列出「推播管道」「設定檔」。`tests/test_r42.py` 修改前 13 項失敗。
  - **格式錯的輸入被當成「空的」**（r44 新增，r41 那點的變形）：網頁送來的表單 JSON 解析失敗時寫成 `except: form = {}`——
    而空表單的意思是「清空所有實盤覆蓋、回到預設」。使用者傳錯格式，結果是實盤參數被清掉、網頁回成功。→ 解析失敗就回錯誤，不改任何東西。
  - **誰踩到（pump-dump-hunter）**（r44 新增）：2026-09-22 對照 r43 時（上次停在 r40，一併對照 r41～r43）——
    參數表單轉換時欄位名稱打錯被 `except` 接住、**沿用上一個欄位的型別**塞進設定（打錯的名稱變成一個新參數），其他照套、網頁回成功；
    值打錯時整批失敗，但表單**已經先存進參數檔**，之後每次開機都套用失敗、跑預設值，網頁照樣回成功；表單 JSON 格式錯被當成空表單（上面那點）。
    參數集檔讀取 `except → 空的`——跟 r37 修之前的狀態檔一模一樣，之後任何一次存參數集就把壞檔蓋掉、使用者存過的參數集全部消失。
    狀態檔與參數集檔讀取失敗期間原檔不見了，都被當成全新開始。改成：整批驗證（名稱與值都驗、一個不合法就整批不存不套用並列出欄位）；
    格式錯回錯誤；參數集檔比照狀態檔（壞檔複製、讀取失敗期間不寫、照節奏推播、恢復通知）；讀取失敗期間「不存在」仍是失敗。
  - **寫入順序改了之後，後寫的那一步把先寫的欄位清成空的**（r45 新增，gold-scalper 正式網實際發生）：
    r25 把平倉改成「先送平倉單、寫出場成交價，最後才結帳」，但結帳的 `UPDATE` 本來就會一起寫出場成交價與滑點（沒傳值就寫 NULL）——
    剛寫進去的成交價被清掉。Telegram 是在清掉之前組的，寫「依真實成交價 ≈ -0.39 USDT」；網頁讀資料庫，同一筆寫「成交價查不到」。
    r25 之後每一筆真實平倉的出場成交價與滑點都被清掉，沒有任何告警。
    → 調整寫入順序時，把後寫的每一個 `UPDATE` 都看一遍：沒有要改的欄位不要寫；可能沒傳值的欄位用 `COALESCE(%s, 欄位)`。
    **資料庫被 mock 掉的測試抓不到**（mock 只記「有呼叫」）：要用 sqlite 之類真的執行 SQL，照實際順序寫兩次再讀回來比對。
  - **查不到成交價時用推估值：gold-scalper 使用者的決定（2026-09-22）**（r45 新增，跟 r28～r30「記未知」的做法不同）：
    「未知」算不進統計，使用者也不可能每筆去查。改成：USDT 用「模擬點數 × 張數」、點數用偵測當下的標記價推估，**照樣進統計**，
    但一律標示——通知寫「≈ … USDT（估算：成交價查不到，用模擬點數×張數）」，網頁寫「≈ … U 估」，總損益寫「其中 N 筆估算」，
    資料庫另存「損益是推估的」旗標。r28 那條的本意（不能把未知**混成**已知）照樣成立：推估值永遠不能寫成「依真實成交價」。
    **另外兩個專案不跟進**（使用者 2026-09-22 決定，r46）：它們的出場損益從交易所成交拿得到，沒有「查不到只能記未知」的問題，
    照 r28～r30 的「記未知」即可。將來如果也遇到大量查不到成交價，再比照這裡的做法（網頁、通知、統計三處都要標示）。
  - **誰踩到（gold-scalper，r43／r44 對照）**（r45 新增）：交易設定存資料庫，讀取失敗 `except → {}`，照樣標記「已載入」——
    正式端會用環境變數預設值交易（引擎專屬覆寫全部不見），而且載入最後的一次性遷移把「每個欄位的修改時間」全部寫回資料庫、蓋掉原本的。
    改成讀取回 `(ok, 值)`、失敗不標記已載入也不做遷移、每 15 秒重試、照節奏推播、恢復通知、沒載入不開倉、沒載入時存設定回錯誤。
    網頁表單：`values` 不是 dict 時直接 500、是空的時回成功（什麼都沒改）；匯入空的參數集也回成功。改成都回錯誤。
    `tests/test_lessons.py` 在修改前的程式上 104 項有 13 項失敗（含 5 項是這次改成推估值、預期值跟著改的）。
  - **解析得了、但不是物件的輸入也會被當成「空的」**（r47 新增，r44 那點的另一個入口）：請求內容是 `null`、`[]`、`"x"` 時 JSON 解析成功，
    r44 的「解析失敗就回錯誤」擋不到；接著 `(payload or {})` 把 `null`、`[]` 變成空的，整批驗證「沒有不合法的欄位」→ 回成功。
    → 解析後先確認是物件，不是就回錯誤；設定端點一個要改的欄位都沒有也回錯誤（r45 gold-scalper 那點）。
  - **誰踩到（crypto-screener）**（r47 新增）：2026-09-22 對照 r46 時——請求內容格式錯本來就回 400；但 `null`、`[]` 回成功、`"x"` 回 500（上面那點）；
    交易設定、自動下單、監控設定收到空的內容都回成功。切到模擬網時讀正式網帳本 `except: pass`——帳本壞掉時「正式網仍有持倉」的提醒直接消失
    （讀不到被當成「沒有持倉」，r44 參數集檔那點的同一類）；這段埋在 `main()` 裡測不到，抽成函式才能測。改成：不是物件回 400、
    空的設定回錯誤、讀不到正式網帳本要推播講明。寫入順序（r45）：結帳是一次寫成一筆紀錄，之後只補「剩下的條件單撤了沒」，**本來就對**；
    損益查不到照 r28～r30 記未知（r46 使用者決定）。`tests/test_r46.py` 在修改前的程式上 4 項斷言失敗。
  - **兩條執行緒交錯時，後寫的也會把先寫的清掉**（r48 新增，r45 寫入順序那點的執行緒版）：背景的對帳、出場管理一開始先拿一份帳本副本、
    中間查交易所好幾秒、最後把**整份副本**寫回。這幾秒之間網頁的手動平倉動了帳本，最後那次寫回就把它蓋掉——
    對帳期間手動平倉：已結帳的部位被寫回帳上，**同一筆結兩次帳**；出場管理期間手動平倉失敗：「待平倉」被清掉，之後不會再重試。
    → 動帳本的步驟（對帳、出場管理、下單、網頁交易操作）共用一把**引擎鎖**；鎖加在函式本身（裝飾器），不是加在某一個呼叫端。
    網頁那邊等鎖要有上限（拿不到就回「背景正在處理」），不能讓請求一直掛著。
  - **誰踩到（pump-dump-hunter）**（r48 新增）：2026-09-22 對照 r47 時——上面兩種交錯都中，用事件讓背景那條停在查交易所、另一條做手動平倉重現出來。
    同一條執行緒裡的寫入順序**本來就對**（每次寫入前都重新讀一次、只改自己的欄位）。
    r47 那點：`null`、`[]`、字串 r44 起就回錯誤（**本來就對**）；但**沒帶表單時預設成 `{}`**，套用到實盤就把覆蓋全部清空、回成功；
    存一個空的參數集也回成功。而「實盤回預設」按鈕本身就是送空表單——所以空表單是刻意的功能，任何 bug 讓表單沒帶到都會變成「清空全部」。
    改成：空表單一律回錯誤，回預設用明確的 `act=reset`。網頁本來就會看回應（收到錯誤先跳錯誤、不會跳「已套用」），**本來就對**。
    損益查不到照 r28～r30 記未知（r46 使用者決定），沒有推估值。`tests/test_r47.py` 在修改前的程式上 10 項斷言失敗。
  - **風控讀不到歷史，被當成「沒有交易」**（r49 新增，r47 讀不到那點發生在保護機制上）：每日虧損上限、連續虧損上限要讀最近的平倉紀錄，
    讀取失敗時如果回空清單，風控就算出「今天虧 0、連虧 0」——**斷路器永遠不會觸發**，而且正好是在系統狀況不好的時候。
    → 讀歷史的函式回 `(ok, 值)`；讀不到就**暫停真實下單**（模擬照常）、照節奏推播、恢復通知。
  - **誰踩到（gold-scalper）**（r49 新增）：2026-09-22 對照 r48 時——上面那點；以及 r48 的交錯：數量比對、停損守衛、掛停損、認領、
    重試平倉、平倉、開倉、手動平倉**都沒有引擎鎖**，只靠「平倉前先認領部位」防止重複平倉，背景查交易所的幾秒之間網頁手動平倉照樣進得來。
    gold-scalper 的帳本不是「整份副本寫回」（部位存在記憶體、資料庫逐欄更新），所以沒有「同一筆結兩次帳」；但照 r48 做法加了引擎操作鎖：
    裝飾器套在上面 8 個函式本身、可重入（平倉裡面再呼叫平倉不會卡住）、網頁手動平倉等 10 秒拿不到就回「背景正在處理」。
    測試用事件讓背景停在查交易所、另一條執行緒做手動平倉，驗證手動平倉沒有同時送單、回應講明、部位沒被動到；另一項確認 8 個函式都套了鎖。
    r47：交易與設定端點都是必填的物件型別，`null`、`[]`、`"x"`、沒帶內容在 FastAPI 那一層就回 422、進不到程式，**本來就對**——
    用測試真的打端點確認（16 個請求、程式一次都沒被呼叫），不是只看型別宣告。r48 的「空表單＝清空全部」不存在：清空覆蓋有獨立的端點。
    `tests/test_lessons.py` 在修改前的程式上 108 項有 3 項失敗。
  - **鎖只讓兩張單排隊，不會讓第二張不送**（r50 新增，r48 那點的後半）：自動開倉停在查交易所時，同一檔的手動開倉加了鎖之後會**等**，
    等到第一張做完照樣送出——手動開倉如果本身不檢查「這檔已有部位／正在等確認」，第二張照送、記帳時**蓋掉原本那筆**
    （原本的停損、停利從此沒人管）。→ 加鎖之後，每個會送單的入口都要在**拿到鎖之後**再檢查一次同一檔的狀態。
  - **每筆「損益未知」也是風控讀不到**（r50 新增，r49 那點的逐筆版）：斷路器不讀整份歷史、而是每次結帳累加當日 R，
    出場成交價查不到的那筆就不累加——成交明細查詢壞掉時每筆都是未知，每日虧損上限照樣永遠不會觸發。
    → 統計照 r28～r30 記未知（r46 使用者決定），但**斷路器**把每筆未知當成一次完整停損（-1R）來判斷：只會更早停、不會更晚，擋下時講明含幾筆未知。
  - **誰踩到（crypto-screener）**（r50 新增）：2026-09-22 對照 r49 時（上次停在 r47，一併對照 r48）——
    r48 交錯：完全沒有引擎鎖（只有精度表那把小鎖），背景對帳停在查交易所時手動平倉照樣送單；自動與手動同一檔各送一張進場單；
    手動開倉本身不檢查同一檔（上面第一點）。帳本是記憶體裡同一份字典、不是「整份副本寫回」，所以沒有「待平倉被清掉」，
    但背景手上的判斷是舊的、存檔時另一條增刪部位會讓 `json.dump` 丟例外。改成可重入的引擎鎖、裝飾器套在 19 個動帳本的函式本身；
    網頁交易操作等 10 秒拿不到回 503「背景正在處理」；狀態頁只等 2 秒，拿不到就不拿鎖讀一份（讀的途中被改動就重讀）並標記忙碌。
    r48 空表單：背景監控設定收到空的 `cfg` 會讓監控**靜靜停掉**（看到空設定就不跑）、缺 bull／bear 整輪丟例外；解除 Telegram 聊天室沒帶 id 回成功。
    都改成回錯誤。r49：斷路器讀的是記憶體裡的累計、讀取失敗時整個暫停開倉（r38），**本來就對**；但逐筆未知那點中（上面第二點）。
    `tests/test_r49.py` 在修改前的程式上 8 項斷言失敗；把裝飾器裡的鎖拿掉，三項交錯測試確實失敗。
  - **誰踩到（pump-dump-hunter）**（r51 新增）：2026-09-22 對照 r50 時——開倉只有 `place()` 一個入口，但「這檔已有部位、有 pending、持倉數已滿」
    的檢查都在呼叫端、拿鎖**之前**；`place()` 拿到鎖之後完全不檢查。同一檔呼叫兩次 `place()`，第二次照送進場單、記帳時把原本那筆蓋掉
    （引擎、停損單號都換成第二筆的），**交易所上 200、帳上 100**，第一張停損沒人管——兩條執行緒同時開同一檔時也一樣（第二條等到鎖、照樣送）。
    改成 `place()` 拿到鎖之後的第一件事再檢查一次。「交易所上這檔已有別的專案的部位」仍只在呼叫端擋：那是鎖擋不到的東西，
    `place()` 裡的基準數量（r14）本來就是為那種時間差設計的。其他會送單的入口（手動平倉、重試平倉、守衛平倉）拿到鎖後都重新讀部位，**本來就對**。
    沒有每日虧損、連續虧損這類斷路器（r30 起），r49、r50 的風控兩點不適用；風控唯一讀交易所的是餘額，讀不到沿用上次的、
    從來沒讀到用基準階（保守的預設），並記錯誤。`tests/test_r50.py` 在修改前的程式上 6 項斷言失敗。
  - **誰踩到（gold-scalper）**（r52 新增）：2026-09-22 對照 r51 時——開倉只有一個入口（背景迴圈），「已經有部位」只在呼叫端檢查；
    開倉函式拿到鎖之後完全不檢查。兩條執行緒同時開倉時第二條等到鎖、照樣送單、把原本那筆蓋掉（修改前的程式上送出兩張）。
    **網頁手動測試下單**直接送真實單、完全不看引擎有沒有部位——交易所上會多一張程式不知道的部位。
    改成：開倉函式拿到鎖之後第一件事再檢查；手動測試下單先把同帳戶每個引擎的操作鎖都拿到（等不到回「背景正在處理」）、
    通過密碼後任何一個引擎有部位就擋下，鎖一直拿到送單結束。
    r50 斷路器：連續虧損遇到損益未知是**跳過**、每日虧損是**不加**（r29 的做法）。改成當成一次完整停損：
    1R 取「設定的停損點數」與「那筆進場到停損價的距離」較大者（只會更早停），擋下時講明含幾筆未知。
    **推估的損益（r45）本來就是數字、照樣算進斷路器**（回答 pump-dump-hunter r51 留的問題）。
    `tests/test_lessons.py` 在修改前的程式上 114 項有 5 項失敗（含 1 項是 r29 的舊測試、預期值跟著 r50 改的）。
  - **拿鎖前讀出的部位「物件」，拿鎖後要確認還是帳上那一筆**（r53 新增，r51 那點的另一個入口）：不是在拿鎖前**檢查**，
    而是在拿鎖前**讀出部位**、把那個物件傳進會送單的函式（例：收到反向訊號時先讀出持有的部位，再查交易所、查衍生數據，最後才呼叫移損）。
    中間部位被平掉時，「送單前先確認交易所上還有部位」（r20）會擋住；但**平掉後同一檔又開了新倉**，交易所上有部位、檢查通過，
    函式就拿舊物件的進場價與停損單號去動**新部位**的停損——撤掉新部位的停損、改掛在舊成本價。
    → 函式拿到鎖之後第一件事：確認傳進來的部位就是帳上那一筆（同一個物件或同一個識別碼），不是就不動。
  - **誰踩到（crypto-screener）**（r53 新增）：2026-09-22 對照 r52 時（上次停在 r50，一併對照 r51）——
    r51「開倉檢查在呼叫端、拿鎖之前」：r50 已把同檔與 pending 的檢查放進開倉函式裡（拿鎖後），自動開倉的風險閘門也在拿鎖的函式裡，**本來就對**；
    網頁交易操作整段在引擎鎖裡，手動開倉走同一個開倉函式，沒有繞過引擎直接送單的入口。但中了上面那點：main 的反向訊號收緊在拿鎖前讀出部位，
    修改前的程式上「平掉後同檔重開」會撤掉新部位的停損、改掛在舊成本價。改成移損函式拿到鎖後先確認部位是帳上那一筆。
    斷路器：風控讀的是記憶體裡的累計（r38 讀不到就整個暫停），逐筆未知 r50 已改成每筆 -1R；沒有推估值（r46 決定記未知）。
    手動開倉不看持倉數上限（只有自動開倉的風險閘門看），是原本的設計。`tests/test_r52.py` 在修改前的程式上 1 項斷言失敗。
  - **交易所端的「平掉後同檔重開」，鎖擋不到**（r54 新增，r53 那點的另一半）：r53 說的是我們自己的程式重開；但部位也可能在**交易所端**
    被平掉（停損觸發），接著**別的專案或 App 在同一檔同方向開了新部位**。這時帳上還記著舊的那筆（對帳還沒跑到），
    送單前的確認（r20：交易所上這一側還有沒有部位）看到新部位、判定還在——舊部位的進場價、停損單號、數量就被拿去動別人的部位。
    引擎鎖只管得到自己程式的兩條執行緒，管不到交易所端的事。→ 送單前的部位確認同時比對**均價**：帳上成交價跟交易所這一側的均價不同，
    就是原本那筆已經沒了，當成「沒了」處理（不送任何單、照成交明細結帳）；對帳用同樣的比對。
    **限制**：新部位剛好在同一個價格開的、或本來就有基準部位（均價是合併的）時比對不出來——根本的解法仍是每個專案一個子帳號（第 7 條）。
  - **誰踩到（pump-dump-hunter）**（r54 新增）：2026-09-22 對照 r53 時，照使用者要求把每一條「讀出部位、中間有 I/O、再傳進會送單的函式」的路徑都跑
    「平掉後同檔重開」。**我們自己的程式重開：本來就被引擎鎖擋住**——出場管理、對帳、下單、網頁操作都是先拿鎖、**拿到之後**才讀帳本，
    沒有 crypto-screener 那種在鎖外先讀出部位的寫法（對照組：拿掉鎖時新部位被減碼、多一張掛在舊成本的停損、帳上被舊的蓋掉）。
    **交易所端重開：五條路徑全中**——出場判斷對別人的部位減碼一半再掛停損；守衛把舊部位的停損補掛到別人的部位上；待平倉重試與網頁手動平倉
    把別人的部位整個平掉、還記成我們的出場；對帳把別人的部位當成我們原本那筆繼續持有。改成上面那點；另加部位識別碼，
    出場管理寫回帳本前確認帳上還是同一筆（有鎖時不會發生，但不能靠呼叫端保證）。1R 減碼以前送單前不確認部位，一併補上。
    網頁沒有繞過引擎直接送單的入口（gold-scalper r52 那點不適用）；沒有斷路器（r52 斷路器那點不適用）。
    `tests/test_r53.py` 在修改前的程式上 7 項斷言失敗（全是交易所端重開那組）。
  - **平倉送單前根本沒確認部位**（r55 新增，r54 那點更早的一步）：r54 說的是「送單前的確認要比對均價」，但如果平倉路徑**根本沒有送單前的確認**，
    直接把帳上的數量送出去（交易所端算「這一側扣掉基準」再平），那均價比對加在哪裡都擋不到——別人的部位照樣被平掉。
    → 先確認每條平倉路徑（出場判斷、待平倉重試、網頁手動平倉、守衛遇 `-2021`）送單前都有一次部位確認，再把均價比對加在那個確認裡。
    代價：每次平倉多一次查詢（幾百毫秒）；查不到時照樣送單（跟原本一樣），確認到沒了才不送、照成交明細結帳。
  - **誰踩到（gold-scalper）**（r55 新增）：2026-09-22 對照 r54 時——r53：會送單的函式拿到鎖後都沒確認「傳進來的就是帳上那一筆」；
    gold-scalper 只有背景迴圈一個開倉入口、自己不會中途重開，還沒出事，但照做（掛停損、數量比對、停損守衛、認領 4 個函式，不是就回 `stale`）。
    r54：五條路徑全中，**而且平倉送單前完全沒有確認**（上面那點）；部位確認只看數量、啟動對帳也只看數量。
    改成部位確認、數量比對、平倉送單前、啟動對帳都比對均價（容許誤差 0.02 或價格的百萬分之二，取大者）。
    測試每一項都有對照組：帳上沒有成交價（均價比對失效）時，同樣的情境必須出事——這組不牽涉鎖（只有一個開倉入口），只需要讓均價比對失效。
    `tests/test_lessons.py` 在修改前的程式上 122 項有 7 項失敗。
  - **只憑「均價不同」就判定沒了，會撤掉還在場部位的停損**（r56 新增，r54 那點的反面）：帳上成交價跟交易所均價有出入的原因不只是重開——
    舊版程式記的方式不同（例如回應沒均價時用標記價、分批成交的算法）、認領時記的是當時的值。只看均價的話，**部署新版的那一刻**，
    所有記法有出入的實單持倉都會被對帳判成「已平倉」：照成交明細結帳、**撤掉它們的停損與停利**——部位還在交易所上，變成裸倉，而且帳上沒有了。
    → **兩個證據才判定重開**：均價不同，**而且**成交明細裡查到這筆的平倉成交（湊滿帳上數量）。均價不同但沒有平倉成交＝同一筆（從沒被平過），
    照常管理、照常平倉；成交明細查不到＝判斷不了，這輪不送任何單、不結帳。重開一定伴隨原本那筆的平倉成交（停損觸發、App 手動平），
    所以真的重開時不會漏；只多一次查詢，而且只在均價不同時才查。
    **gold-scalper、pump-dump-hunter 部署 r54／r55 之前要先查**：現有持倉裡有沒有帳上成交價跟交易所均價不同的（容許誤差外）——有的話，
    部署後第一輪對帳就會把它們結帳、撤停損。
  - **誰踩到（crypto-screener）**（r56 新增）：2026-09-22 對照 r55 時（上次停在 r53，一併對照 r54）——
    r55：每條平倉路徑（出場判斷的移損遇 `-2021`、待平倉重試、網頁手動平倉、守衛遇 `-2021`、守衛強平、開倉後立即平掉）都走同一個平倉函式，
    送單前一定逐幣確認部位、查不到就不送，**本來就對**；均價比對加在這一個地方就涵蓋全部平倉路徑。
    r54：五條路徑全中——移損把新停損掛到別人的部位上、守衛補掛到別人的部位上、待平倉重試與手動平倉平掉別人的部位（還記成我們的出場）、
    對帳把別人的部位當成原本那筆繼續持有；成交明細查不到時照樣送單。改成上面那點：部位確認（掛停損的唯一入口）、平倉送單前、對帳三處共用同一個判斷。
    有 8 筆實單持倉，所以沒照 r54 只看均價（上面那點）。1R 減碼是交易所條件單、程式不送部分出場，r54 pump-dump-hunter 那點不適用。
    `tests/test_r55.py` 在修改前的程式上 6 項斷言失敗。
  - **只看均價時，手動平倉會「回報已平倉、實際沒送單」**（r57 新增，r56 那點在手動平倉上的樣子）：均價有出入的持倉被判成「已經沒了」，
    平倉函式照「沒了就不送、照成交明細結帳」處理——網頁回「已平倉」、帳上刪掉，交易所上的部位原封不動、停損也被撤了。
    使用者按了平倉、以為平了，其實是一筆沒有停損、帳上也沒有的部位。比對話的「撤停損」更難發現。
  - **誰踩到（pump-dump-hunter）**（r57 新增）：2026-09-22 對照 r56 時——r54 只看均價，四條路徑都中：對帳結帳並撤停損、
    **手動平倉回「已平倉」卻沒送單**（上面那點）、守衛不補掛、成交明細查不到時照樣判定。改成兩個證據（均價不同，而且界線之後的平倉成交湊滿帳上數量）；
    查不到時部位確認回「查不到」（不送單），對帳這輪不結帳。r55：只有兩處會送減倉單——平倉函式（六條平倉路徑都經過它，r16 起送單前逐幣確認）
    與 1R 減碼（r54 補上送單前確認），**本來就對**；查不到時是**不送**（r16 的保守做法），跟 r55 寫的「查不到照樣送」不同，維持不變。
    **部署前檢查**：連不到使用者的正式環境，沒辦法直接查；自檢加一項「帳上成交價與交易所均價」，列出對不上的持倉、有沒有平倉成交、會怎麼處理。
    兩個證據的版本部署時不會誤判，這一項用來事後確認。`tests/test_r56.py` 在修改前的程式上 8 項斷言失敗；上一輪「真的重開」的 `test_r53` 照樣全部通過。
  - **判斷正確時，手動平倉的回應也要講明「這次沒有送單」**（r58 新增，r57 那點的另一半）：兩個證據都成立、原本那筆真的在交易所端平掉了，
    手動平倉照成交明細結帳是對的——但如果回應照樣寫「已以 4401 平倉」，使用者會以為是這次按的平倉平掉的、以為交易所上那張（別人的）也處理了。
    → 手動平倉走到「交易所端已平掉」時，回應寫明「交易所上這筆已經平掉（成交明細查到平倉成交），照成交明細結帳；這次沒有送平倉單」。
  - **誰踩到（gold-scalper）**（r58 新增）：2026-09-22 對照 r57 時——r55 只看均價（部位確認、數量比對、啟動對帳三處），部署後第一輪就會誤判；
    手動平倉的回應在「交易所端已平掉」時也寫「已以 {價格} 平倉」（上面那點）。改成三處共用一個判斷（均價不同、而且界線之後的平倉成交湊滿帳上數量才算重開；
    均價有出入但沒有平倉成交＝同一筆、照常管理；成交明細查不到＝判斷不了）。判斷不了時：掛停損不掛、數量比對這輪不動、
    **平倉送單前不送**、對帳寫「無法確認」。注意 gold-scalper 平倉送單前的「部位查不到」是**照樣送**（r55 的設計，跟以前一樣），
    「均價不同、成交明細查不到」才是**不送**——兩種「不知道」分開處理。自檢加「帳上成交價 vs 交易所均價」一項（照 pump-dump-hunter r57）。
    `tests/test_lessons.py` 在修改前的程式上 128 項有 6 項失敗。
  - **誰踩到（pump-dump-hunter）**（r59 新增）：2026-09-22 對照 r58 時——手動平倉遇到「交易所端已平掉」有兩種情況：
    **停損觸發、沒有重開**：手動平倉先查交易所部位（r16 起），這一側沒有自己的部位就回「已經沒有自己的部位（可能剛被停損），不送單，交給對帳處理」，
    **本來就對**；**平掉後被別人重開（兩個證據都成立）**：沒送單、照成交明細結帳都對，但回應寫「XUSDT 已平倉 @ 0.9，損益 -10.14 U」，
    交易所上那張別人的部位完全沒提——正是 r58 那點。平倉函式改成記下這次實際送了幾張單，一張都沒送時回應寫明
    「交易所上這筆已經平掉（成交明細查到平倉成交），照成交明細結帳；這次沒有送平倉單。交易所上現在那張是別的部位，沒有動它」。
    `tests/test_r58.py` 在修改前的程式上 1 項斷言失敗。
  - **回應寫「查到平倉成交」之前，先確認真的查到了**（r60 新增）：手動平倉走到「交易所端已平掉」時，出場價可能來自成交明細，
    也可能是成交明細查不到、用偵測當下的價格推估（gold-scalper r45）。回應如果一律寫「成交明細查到平倉成交，照成交明細結帳」，
    推估的那種就是把「不知道」寫成「知道」。→ 回應依實際來源寫：查到了寫照成交明細結帳；查不到寫出場價是推估的。
  - **誰踩到（gold-scalper）**（r60 新增）：2026-09-22 對照 r59 時——r58 補的回應一律寫「成交明細查到平倉成交」，不分停損觸發沒重開、
    平掉後被別人重開，也不分成交明細查到還是查不到（上面那點）。改成三種情況分開寫：
    停損觸發、沒重開 → 「交易所上這筆已經平掉（可能是停損觸發）…這次沒有送平倉單」；平掉後被別人重開 → 另加「交易所上現在那張是別的部位（均價不同），沒有動它」；
    成交明細查不到 → 寫「出場價用偵測當下的價格推估（估算）」。`tests/test_lessons.py` 在修改前的程式上 132 項有 2 項失敗。
  - **對帳結帳的原因與通知也要照實際情況寫，不只手動平倉的回應**（r61 新增，r58～r60 那幾點在對帳路徑上的樣子）：
    交易所端重開時，實際替原本那筆結帳的常常是**對帳**（下一輪偵測到），不是手動平倉。對帳的結帳原因如果照舊寫「交易所出場（停損或停利觸發）」，
    通知裡看不出交易所上現在那張是別的部位、程式沒動它；出場價查不到時也只寫原因、不講記未知。
    → 對帳替被重開的那筆結帳時，原因加「交易所上現在同一側那張是別的部位（均價不同），沒有動它」；出場價查不到時加「出場價成交明細查不到，記未知」
    （推估的專案寫「推估」）。平倉通知用的就是這個原因。
  - **誰踩到（crypto-screener）**（r61 新增）：2026-09-22 對照 r60 時（上次停在 r58，一併對照 r59）——
    手動平倉遇到交易所端已平掉時**不自己結帳**（交給對帳，第 7 條 r13），回應在兩種情況都寫明不送單：
    停損觸發沒重開寫「這一側已經沒有自己的部位，不送平倉單，交給對帳處理」；被別人重開寫「……查到這筆的平倉成交：原本那筆已經沒了，
    現在這一側是別人重開的，不送平倉單、交給對帳」——「查到平倉成交」只在重開判定成立時才寫，而判定本身要求成交明細湊滿數量，**寫的都是真的查到**；
    成交明細查不到時寫「判斷不了……不送平倉單」。出場價從不推估（r46 決定記未知），平倉通知在損益未知時寫「請到幣安核對」。以上**本來就對**。
    中了上面那點：對帳的結帳原因兩種情況都只寫「交易所出場（停損或停利觸發）」。`tests/test_r60.py` 在修改前的程式上 2 項斷言失敗。
  - **誰踩到（pump-dump-hunter）**（r62 新增）：2026-09-22 對照 r61 時（上次停在 r59，一併對照 r60）——
    r60：手動平倉沒送單時，回應一律寫「成交明細**查到**平倉成交，照成交明細結帳」。會走到「沒送單」的不只被重開：手動平倉讀部位表時部位還在、
    送單前確認的那一刻停損剛好觸發——這時成交明細也查不到的話，回應照樣寫「查到」，出場價其實是未知的。改成照實際情況寫：
    被重開（判定本身要求查到平倉成交）寫「查到這筆的平倉成交」；沒重開寫「可能是停損觸發」；出場價有值才寫「照成交明細結帳 @」，
    沒有就寫「成交明細查不到，出場價與損益記未知」（本專案不推估，r46）。
    r61：對帳替被重開的那筆結帳時，結帳原因寫死「停損單」、通知是「停損單觸發出場」，「別的部位」只在另一則警告裡；出場價查不到時只寫「損益未知」。
    改成結帳原因本身寫明「交易所上現在同一側那張是別的部位（均價…），沒有動它」，平倉通知用這個原因；出場價查不到寫「出場價成交明細查不到，記未知」。
    另一則警告的內容已併進平倉通知，拿掉。`tests/test_r61.py` 在修改前的程式上 5 項斷言失敗。
  - **通知的說明放錯欄位，印出來的情境就是錯的**（r63 新增）：通知函式如果只有一個「沒下真實單的原因」欄位（本來給風控斷路器用），
    交易所端已平掉的說明塞進去，印出來會是「（已暫停真實下單，僅記錄模擬單）原因：……」——使用者會以為風控擋下了這次平倉。
    → 「交易所端已平倉、這次沒有送單」要有自己的欄位與開頭字樣，不能借用風控那一欄。
  - **誰踩到（gold-scalper）**（r63 新增）：2026-09-22 對照 r62 時——r61：對帳（數量比對連 3 輪判定部位不在）替原本那筆結帳時，
    結帳原因一律是「交易所端部位已不在」，看不出交易所上那張是別的部位；出場價推估的說明只在通知的另一段，而那一段放在風控的欄位裡，
    印成「已暫停真實下單」（上面那點）。說明文字也寫死「平倉單回應失敗或逾時」，但這時送單前確認就知道沒了、根本沒送單。
    改成：結帳原因加「交易所上現在同一側那張是別的部位（均價不同），沒有動它」「出場價成交明細查不到，用偵測當下的價格推估」；
    通知另開「交易所端已平倉」欄位；送單前確認就知道沒了時寫「送單前確認：交易所這一側已經沒有這一筆（可能是停損觸發），這次沒有送平倉單」。
    r62：手動平倉按下時部位還在、送單前確認那一刻才發現沒了——r60 起回應就照實寫（「可能是停損觸發」、查不到寫推估），**本來就對**。
    `tests/test_lessons.py` 在修改前的程式上 135 項有 3 項失敗。
  - **誰踩到（crypto-screener）**（r64 新增）：2026-09-22 對照 r63 時（上次停在 r61，一併對照 r62）——
    r62：手動平倉不自己結帳（交給對帳），「按平倉時還在、送單前確認那一刻停損剛觸發」回應寫「這一側已經沒有自己的部位，不送平倉單，交給對帳處理」，
    沒有寫錯的東西（不寫「查到」），但沒講明是送單前確認才發現、也沒提可能是停損觸發；改成「送單前確認：交易所這一側已經沒有自己的部位（可能是停損剛觸發）」。
    出場價由對帳查成交明細，查不到記未知（r46）。待平倉重試遇到同一情況也不送、留給對帳，**本來就對**。
    r63：平倉通知的「原因」就是結帳原因，r61 起「別的部位、沒有動它」「出場價記未知」都在這一行；唯一的風控欄位（⚠ 當日停損上限）另起一行、
    只在斷路器擋下時出現，**本來就對**——補三項測試鎖住（重開、出場價未知、正常出場）。`tests/test_r63.py` 在修改前的程式上 1 項斷言失敗。
  - **誰踩到（pump-dump-hunter）**（r65 新增）：2026-09-22 對照 r64 時——沒有斷路器、通知裡沒有任何「暫停真實下單」之類的風控欄位（r30 起）；
    平倉通知（🏁）的可變部分只有結帳原因那一句，r62 起「別的部位、沒有動它」「出場價成交明細查不到，記未知」都寫在那一句；
    手動平倉的說明是回應自己的 msg，**本來就對**。補 `tests/test_r64.py` 鎖住三種出場（正常停損、被重開、出場價未知）與手動平倉回應：
    說明都在通知本身、沒有風控字樣。在修改前的程式上全部通過。

## 9. K 線最後一根是「進行中」的

- **症狀**：實盤訊號跟回測對不上；條件在剛開盤幾秒的空棒上判斷，量能類條件幾乎不可能成立。
- **原因**：`/fapi/v1/klines` 回傳的最後一根是未收盤的棒。
- **處理**：過濾掉 `開盤時間 + 週期 > 現在` 的棒；輪詢對齊整分後 2 秒，棒一收完就判斷。
- **不適用的情況**：crypto-screener 不用幣安 klines，訊號來自 CoinGecko `market_chart` 的時間點快照，量能是 24 小時滾動量，沒有「空棒」問題。但它的 60 分均線把最後一個即時價當一根算，三刀流狀態可能在一小時內翻動——這是既有行為，改動等於換策略，要走策略版本分桶。

## 10. 伺服器時間是 UTC，判讀會差 8 小時

- **症狀**：以為一筆單掛了 8 小時，其實才 2 小時。
- **原因**：容器預設 UTC。
- **處理**：程式啟動最前面 `os.environ["TZ"]="CST-8"; time.tzset()`（POSIX 寫法，不需要 tzdata）。例外：日 K 的日期是 UTC 切日，那個欄位保持 UTC 才不會標錯天。
- **先判斷架構**：如果後端一律存 epoch 毫秒或帶時區的 ISO 字串、前端用 `toLocale*` 轉本地時間（gold-scalper、crypto-screener 都是），就不需要設 `TZ`。這種架構下殘留的 `gmtime()` 多半是刻意的 UTC 切日（例如每日下單上限），不要改。

## 11. 回測有的出場規則，實盤要一條不漏

- **症狀**：實盤只掛初始停損，減碼、保本、追蹤、時間出場、冷卻全沒做，實盤結果跟回測完全不是同一套策略。
- **處理**：實盤出場管理逐根收盤棒重現回測邏輯，並寫一支一致性測試：同一組 K 線丟給回測和實盤管理，**出場原因與出場根數必須完全一致**。改其中一邊就跑這支測試。
- **沒有回測引擎的專案**：一致性測試改鎖「同一組數字被算了好幾次」的地方。crypto-screener 的影子追蹤曾把目標寫死成 2R，實盤用的是可調的 `tp1R`——改參數後兩邊就會脫鉤。
- **誰踩到**：pump-dump-hunter，2026-09-21 才補上。

## 12. 沒掛交易所停損時，服務中斷 = 倉位裸奔

- **症狀**：策略本身邏輯正常（回測、實盤判斷都對），但停損/移動停損完全是「程式內比對現價」實作，沒有在交易所掛任何條件單。服務重啟、斷線、或主機掛掉的那段時間，部位在交易所端沒有任何保護，可能大幅虧損才被發現。
- **原因**：一開始圖簡單，出場邏輯只靠程式自己算 `sl_price` 再比對現價、觸發後送市價單；這樣做行情判斷靈活（可以疊 ATR 動態停損、追蹤停損、結構停利），但沒有交易所端的備援。
- **處理**：真實開倉成功後，額外在交易所掛一張 `reduceOnly` 的 `STOP_MARKET` 條件單當「backstop」，觸發價用當下的 `sl_price`；程式內的判斷仍是主要出場依據，backstop 只在服務掛掉時墊底。三個時機都要處理：
  1. **移動停損更新時**：backstop 要跟著搬——用第 8 條建議的「先掛新的、再撤舊的」，不要先撤再掛，不然搬動的瞬間裸奔。
  2. **平倉時**（不管出場原因是程式判斷、手動平倉、還是任何路徑）：一定要撤掉這張 backstop，否則留下一個沒有對應部位的孤兒 reduceOnly 掛單。
  3. **平倉前要先確認 backstop 是否已經自己觸發**：撤單時如果發現這張單已經不存在（查無此單），代表它已經先一步把部位平掉了（例如服務中斷過一段時間）——這時不能再送一次真實平倉單（交易所根本沒部位可平），改成查這張單的實際成交價，讓損益紀錄準確。
  掛不上 backstop 不該讓這筆交易整個失敗——它是保險，不是主要防線，掛不上就記警告、繼續走原本流程。
- **誰踩到**：gold-scalper 原本完全沒有交易所停損，2026-09-21 補上。
- **不適用**：crypto-screener 的主要出場本來就是交易所條件單（停損、停利、移動停利），不需要另掛 backstop；但它踩到的是這條第 2 點的反面，見第 13 條。

## 13. 一張條件單觸發平倉後，同一部位的其他條件單會留下來

- **症狀**：部位已經平了，交易所上還掛著它的停利、移動停利或停損。下次同一個幣再進場時，
  新的 `closePosition` 停損被拒（第 8 條：同方向只能一張），接著觸發「沒有停損就不留倉」而立刻平倉；
  共用帳號時，殘留的 reduce-only 單還可能動到別的專案同一個幣的倉（第 7 條）。
- **原因**：出場是交易所端的條件單自己觸發的（停損、停利、移動停利），程式只在對帳時發現「部位不見了」，
  記了帳卻沒去撤剩下的單。
- **哪一型會留下（實測，未經幣安文件確認）**：`closePosition=true` 的停損在部位歸零後**查不到**，
  推測幣安會自動取消；帶 `quantity` 的 `reduceOnly` 單（部分停利、移動停利）則會留下。
  所以停損觸發後，停利與移動停利是主要的孤兒來源。不要依賴自動取消，一律自己撤。
- **處理**：
  - 每張條件單的 `algoId` / `orderId` 都記在部位上；**任何**平倉路徑（交易所觸發、手動、守衛）
    記帳時一律用記錄的 id 精準撤剩下的單。
  - 撤單回 `-2011 Unknown order`（已觸發或已撤）視為正常，不是錯誤；其他失敗要推播，請人手動處理。
  - 自檢加一項「孤兒條件單」：查全部掛單（不帶 symbol，權重 40，只在自檢時查），列出沒有對應部位的。
  - 第 12 條第 2 點是同一件事的另一面：那條講「程式平倉要撤 backstop」，這條講「交易所平倉要撤其他單」。
- **誰踩到**：crypto-screener，2026-09-21 自檢在模擬網查到 2 張沒有對應部位的條件單（共用帳號，來源未確認），已撤。
  正式網第一筆 AVAX 移動停利出場後，原本推測成本價停損會殘留，自檢查證**沒有**——這也是上面「closePosition 會被自動取消」推測的依據。
- **其他兩個專案要檢查**：pump-dump-hunter 若同時掛了停損與停利，停損觸發後停利是否有撤；
  gold-scalper 的 backstop 若自己觸發（第 12 條第 3 點），同一部位是否還有其他交易所掛單。

## 14. 改寫一個函式時，把旁邊的東西一起刪掉

- **症狀**：某個保護機制（撤單、停損守衛）看起來在運作，實際上每次都拋 `NameError`，被外層 try 吞掉，
  變成「撤單失敗」「補掛失敗」這種看起來像交易所問題的告警，或只在錯誤區留一行。
- **原因**：用「從 A 函式開頭取代到 B 函式開頭」的方式整段改寫時，夾在中間的小函式或模組層級宣告被一起刪掉。
  守衛類程式通常包在 try 裡（這是對的），所以程式錯誤跟交易所錯誤被一視同仁地處理。
- **處理**：
  - 改寫後、部署前跑靜態檢查：`python -m pyflakes app/`（或該專案的程式目錄）。**有 `undefined name` 就不能部署。**
  - 行為測試的斷言要檢查「失敗的原因」，不只是「有沒有失敗」：例如測「撤單失敗會告警」，要確認告警裡的錯誤是注入的那一個，
    不是 `NameError`。
- **誰踩到（pump-dump-hunter）**：2026-09-21 兩次。
  1. 對照 r8 時（作廢的 r9 版本）刪掉了 `_missing = {}`，停損守衛整個失效。
  2. 對照 r12 時改寫 Algo 退回邏輯，刪掉了判斷「已撤單」的 `gone()`，**所有撤單都會失敗**。
     測「移損時撤舊單失敗要告警」的測試照樣通過——因為撤單確實失敗了，只是原因是 `NameError` 而不是注入的 503。
     是看到告警內容寫著 `name 'gone' is not defined` 才發現。兩次都在部署前抓到。
- **另一種形式：改寫沒有生效，也沒有報錯**（r15 新增）：用字串取代（`str.replace`、`sed`）修改程式時，
  比對字串的縮排或內容跟檔案差一點，取代就**什麼都不做、也不報錯**。常見於被改的程式先前被包進 `try` 多了一層縮排。
  pyflakes 抓不到（程式本身沒有錯，只是沒改到）。
  - **處理**：取代一律用「必須恰好命中 N 次，否則中止」的函式（crypto-screener 的 `scripts/patch.py`）；
    一輪修改結束後，逐條用搜尋確認每一處預期的修改都在程式裡，不能只靠「測試通過」推論。
  - **誰踩到（crypto-screener）**：2026-09-21 對照 r14 時，開倉兩條平倉路徑與部位紀錄共 3 處取代沒有生效，
    是基準數量的測試失敗才發現。
  - **pump-dump-hunter 檢查自己的改寫方式**（r16 新增）：r13 以前用的取代函式只 `assert` 字串存在，**不檢查命中幾次**
    （`str.replace` 會把所有出現處都換掉）；改測試檔、HANDOFF 用的是完全沒檢查的 `.replace`，改版本號用 `sed -i`——
    都會靜靜略過。回頭用搜尋逐條確認 r13 那輪 17 處預期修改都在程式裡，沒有漏。
    這輪起改用 `scripts/patch.py`（必須恰好命中 N 次，否則中止），每輪結束逐條搜尋確認。
    2026-09-21 對照 r18 時實際擋下一次：儀表板那一處的比對字串多寫了 `${...}`，工具中止並列出比對字串，
    改正後重跑。換成 `str.replace` 的話，後端改好了、畫面卻沒有新欄位，測試也不會發現（測試不看畫面）。
  - **整段縮排包 try 之後，要比對改動只有縮排和新增的 try**（r22 新增）：把一大段程式縮排包進 try，
    手動改很容易漏一行縮排（改變控制流程）或改到內容。pump-dump-hunter 對照 r21 時用程式比對前後：
    被改的 55 行全部只差縮排，新增的只有 try／except 那幾行。
    crypto-screener 對照 r23 時，手動平倉的外層保護照 r23 的「外層包裝＋原函式改名」做，原內容沒有重新縮排。

---

## 15. 市價單回應預設只有「收到了」，不是「成交了」（r42 新增）

- **症狀**：下單回傳成功，但回應是 `status: NEW`、`executedQty: 0`、沒有 `avgPrice`。程式當成已成交，成交價卻是空的——
  執行品質、真實損益都算不出來。**平倉時更危險**：「回傳成功」被當成已經平掉，接著撤交易所停損、結帳，不會再查部位；
  萬一那張單其實沒成交，就是一張沒有停損、程式也不管的部位。
- **原因**：期貨 `POST /fapi/v1/order` 的 `newOrderRespType` 預設是 `ACK`，只保證「交易所收到了」。
- **處理**：
  - 送單帶 `newOrderRespType=RESULT`，市價單的回應直接帶成交狀態、成交量、均價。
  - 「成功」的條件是 `executedQty > 0`，不是 HTTP 200。沒確認就再查訂單幾次（`GET /fapi/v1/order`）；
    還是沒確認就**當成結果不明**，走第 3 條的路：開倉查部位、看得到就認領；平倉查部位、確認這一側沒了才撤停損結帳。
  - 真實數量用交易所確認的 `executedQty`，不是送出的數量（部分成交時兩者不同）。
  - 均價缺漏時，用那張單號在成交明細（`userTrades`）裡的成交算加權均價——這是實際成交價，不是估算；查不到記未知。
- **誰踩到**：gold-scalper，正式網 2026-09-22 00:00:06 開倉（XAUUSDT 0.1 張，雙向模式 LONG）。舊程式送單沒帶回應類型，
  沒拿到均價時只等 0.5 秒重查一次，查不到也照樣回報成功；開倉通知寫「無法計算執行品質：回應裡沒有 avgPrice」。
  部位與交易所停損都在，只是進場成交價沒記到。`tests/test_lessons.py` 用當時的原始回應重現，在修改前的程式上 5 項失敗。
- **其他兩個專案要檢查**：送市價單有沒有帶 `RESULT`；「成功」看不看 `executedQty`；平倉單的「成功」是不是直接導向撤停損、結帳；
  真實數量是送出的還是成交的。這項沒辦法在交易所自檢裡檢查（要真的送單），要靠測試：用一個 `status: NEW` 的回應跑開倉與平倉。
- **誰踩到（crypto-screener）**（r43 新增）：2026-09-22 對照 r42 時——開倉單有帶 `RESULT`，但**平倉單沒帶**，而且平倉只要 HTTP 200
  就回報平掉：手動平倉、待平倉重試、守衛補掛遇 `-2021`、守衛強制平倉、移損遇 `-2021` 五條路徑全部接著撤停損、結帳——
  就是上面「最危險的一點」，還沒在實單發生。開倉本來就用部位增加量確認數量，**本來就對**；但交易所明確說沒成交（EXPIRED、成交 0）時
  仍留著 pending 等 3 分鐘。同側有別人的部位時，均價缺漏就用標記價當進場價（估算）。
  改成：兩種市價單都帶 `RESULT`；不是最終狀態就用單號查，查幾次仍不是就**撤掉那張單**再查一次；平倉只在「FILLED 且成交量足夠」或
  「再查部位確認這一側沒了」才算平掉，其他一律待平倉、停損不動；開倉 EXPIRED 0 直接丟 pending；均價缺漏用單號查成交明細，查不到記未知。
  `tests/test_r42.py` 用 `status: NEW` 的五種情境（其實已成交、稍後成交、沒成交、部分成交、一直 NEW）跑開倉與平倉，修改前 10 項失敗（含自檢的判讀項目）。
- **補充：卡在 NEW 的單要撤掉**（r43 新增）：查了幾次仍不是最終狀態就放著不管，之後才成交的數量就沒人知道；
  平倉單還會跟下一輪重試的平倉單重疊。→ 撤單，再查一次拿最終成交量。
- **補充：部分成交的平倉，出場價不能只用最後一張單的均價**（r43 新增）：一半成交 → 待平倉 → 重試平掉剩下的，
  結帳時若用「最後一張平倉單的均價」，只算到後半段。→ 標記「部分成交過」（跟著部位存檔），出場價改用成交明細把兩段加權。
- **補充：改帶 RESULT 之後冒出來的坑**（r43 新增）：以前 ACK 沒有均價，「沒結帳的平倉」（停損掛不上立刻平掉、進場瞬間穿過停損）
  不會留下成交價；改帶 RESULT 之後有了，存在「給下一次結帳用」的暫存裡卻沒人取走——**下一筆同一個幣的部位**結帳時拿它當出場價。
  → 沒有結帳的平倉路徑要清掉暫存。修舊坑時自己引入的，只有測試抓得到（在舊程式上它反而「通過」）。
- **補充：模擬交易所的市價單一律回 FILLED**（r43 新增，用法第 5 點「退化值」）：不管有沒有帶 `RESULT` 都回成交結果，
  所以「平倉沒帶 RESULT」在任何測試裡都看不出來。→ 模擬交易所沒帶 `RESULT` 時回 ACK（NEW、成交 0、均價 0），並支援查單、撤單。
- **誰踩到（pump-dump-hunter）**（r44 新增）：2026-09-22 對照 r43 時——所有市價單都走同一個送單函式，**本來就帶 `RESULT`**；
  平倉送出後**不看回應**、一律再查部位確認這一側沒了才撤停損結帳（r12 起的設計），六條平倉路徑都經過它，**「最危險的一點」本來就對**；
  沒有「給下一次結帳用」的成交價暫存，r43 那個坑不適用。但：開倉寫成 `executedQty or 送出的數量`——回 NEW 成交 0 時照送出的數量記帳、
  還掛了停損，**帳上多出一筆交易所上不存在的部位**；EXPIRED 也照記；1R 減碼寫成 `executedQty or 一半`，沒成交也把帳上數量減半，
  之後的停損只保護一半；卡在 NEW 的單沒撤；平倉分段成交後，成交明細查不到時的估算拿最後一張單的均價乘整筆數量。
  改成共用的 `confirm_fill()`：不是 FILLED 就用單號查、查幾次仍不是最終狀態就撤單再查；開倉成交 0 直接丟 pending、查不到最終狀態當結果不明；
  1R 減碼沒成交帳不動（真的成交了由對帳偵測）；平倉分段成交過就標記、估算不用最後一張單的均價。
  **模擬交易所的市價單一律回 FILLED，而且回應裡沒有訂單號**——第 15 條的測試在這之前全部是空跑。改成：沒帶 `RESULT` 回 ACK；
  五種成交情境（立刻成交、查單時才成交、一直 NEW、成交 40% 後卡住、EXPIRED）；支援查單、撤單；成交明細帶訂單號。
  測試跑手動平倉、待平倉重試、守衛補掛遇 `-2021` 三條平倉路徑。`tests/test_r43.py` 在修改前的程式上 19 項斷言失敗、2 項被測程式拋錯。
- **誰踩到（gold-scalper）**（r45 新增，回答 pump-dump-hunter r44 留的問題）：r42 的緊急修正讓「成功」要求 `executedQty > 0`，
  但**平倉只成交一部分時照樣當成平掉**，接著撤交易所停損、結帳，剩下的部位沒有停損；交易所回 EXPIRED 成交 0 時當成「結果不明」等 3 分鐘；
  卡在 NEW 的單沒撤。開倉的數量本來就用成交量（r42），沒有程式自己送的部分出場。
  改成：不是最終狀態就查單、查幾次仍不是就撤單再查；最終狀態成交 0 直接回「沒成交」（不是結果不明）；
  平倉成交量不足時剩下的記待平倉、停損保留、每輪重試，最後結帳用成交明細把各段加權。
- **誰踩到（crypto-screener）**（r47 新增，對照 pump-dump-hunter r44、gold-scalper r45）：r43 起開倉與平倉共用同一個確認函式，
  「成交 40% 後卡在 PARTIALLY_FILLED」會撤單再查、平倉剩下的記待平倉、出場價用成交明細加權，**本來就對**；
  沒有程式自己送的部分出場（1R 減碼是交易所條件單）。模擬交易所的市價單本來就有訂單號、成交明細帶訂單號。
  補了「成交 40% 後卡住」這種情境與開倉、平倉兩項測試。
- **帶了 `RESULT`、回應也是 FILLED，均價照樣可能沒有；當下查不到不能就此放棄**（r71 新增）：
  - **症狀**：平倉市價單回應 `status: FILLED`、`executedQty: 1.000`，但 `avgPrice`、`cumQuote` 兩個欄位**整個不存在**（不是 `"0"`）。
    `updateTime`、`cumQty` 都在，是 `POST /fapi/v1/order` 帶 `RESULT` 的回應。程式查不到成交價，通知寫「估算」，
    還把整包原始回應貼進 Telegram。同一天的進場、前一筆出場回應都有均價——**時有時無**。
  - **原因**：確切原因不知道（幣安沒有文件說 RESULT 會缺這兩個欄位）。看得到的是時間差：送單回應、訂單查詢、成交明細（`userTrades`）
    三個來源更新的時間點不同，**成交明細是非同步寫入的**，剛成交的一兩秒內常常還查不到這張單號、或只出現其中幾筆。
    舊程式三個補救都擠在 1 秒內、各試一次：0.5 秒後查一次訂單 → 沒有就照原回應；立刻查一次成交明細 → 沒有就放棄。
    快速行情（停損出場）時比較容易碰上。
  - **處理**：
    1. 沒有 `avgPrice` 但有 `cumQuote` 時，**成交額 ÷ 成交量**就是實際均價（U 本位 1 張 = 1 單位標的），不是估算。
    2. 送單流程裡查訂單要查**幾次**（例如 3 次、間隔 0.5 秒），不是 1 次；每次結果寫日誌（舊程式重查失敗時什麼都不記）。
       次數要有上限——這時單已經成交，不能為了一個價格卡住平倉流程。
    3. 用成交明細算均價時，**同一張單號的數量加總要湊滿 `executedQty`**。只出現前幾筆就拿來算，得到的是前半段的價格，
       而且會被當成「真實成交價」——比估算更糟（估算至少有標記）。沒湊滿就當成還查不到。
    4. 還是查不到：先照舊結帳、發通知（標估算／未知），另外**排背景補登**——隔幾秒到兩分鐘再查幾次（gold-scalper 用 3、10、30、90 秒），
       查到就補寫資料庫（出場／進場成交價、執行品質）、進場的回填到還開著的部位、補發一則通知（附原本的估算值對照）；
       一直查不到推一則「補登失敗」附單號。背景執行緒要自己接住例外並推播（第 14 條）；需要的東西在排程當下抄下來，不讀之後會變的部位。
    5. 通知只附單號，原始回應寫日誌。
  - **誰踩到（gold-scalper）**：正式網 2026-09-25 22:46，XAUUSDT 15分K 空單移動停損出場，訂單 17233421715，1 張。
    帳戶餘額 987.94 → 1019.66（+31.72），通知估算 +35.44，差額約等於來回手續費——損益沒有算錯很多，但執行品質沒記到、網頁這筆是估算。
    `tests/test_lessons.py` 的 `LiveFilledNoAvgPrice` 用當時的原始回應照抄重現（成交明細的出現綁在「背景等待過後」的時間，
    不用第幾次查詢計數，測錯方式 18），加上通知格式共 8 項，修改前的程式上 8 項全部失敗。
    其中「成交明細只出現 0.4/1 張」那項在舊程式上**算出了 +42.88 USDT、標成依真實成交價**——第 3 點的坑本來就在，這次才被測出來。
    背景執行緒在測試裡不真的開（換掉 `_start_backfill_thread`、收下來由測試自己跑），不會在測試結束後還去打網路。
  - **誰踩到（pump-dump-hunter）**（r72 新增）：2026-09-25 對照 r71 時——五項中四項中：沒有 `cumQuote` 備援；FILLED 沒均價時**一次都沒查訂單**
    （查單的迴圈只在「還沒成交」時跑，FILLED 就直接查一次成交明細、沒寫日誌就結束）；用單號算均價沒檢查湊滿；
    **對帳與部分出場用的「界線之後的平倉成交」也沒檢查湊滿**——只先出現四成時照樣結帳，損益只算到四成、標成實際成交，範圍比單號那處大；
    沒有延後再查。通知本來就沒貼原始回應。改成：均價依序看 `avgPrice` → `cumQuote ÷ executedQty` → 查訂單 3 次（間隔 0.5 秒、每次寫日誌）
    → 成交明細（要湊滿）；兩處成交明細都要湊滿，沒湊滿當成還查不到；查不到排背景補登（3、10、30、90 秒），查到補寫已平倉紀錄或還開著的部位、
    補發通知附原本是未知，一直查不到推「補登失敗」附單號。模擬交易所的成交額以前一直是 "0"（退化值），一併改成照實累計；
    加上「回應沒有均價欄位」「成交明細稍後才出現／只先出現一部分」兩種情境，開倉、手動平倉、交易所端停損觸發都跑。
    `tests/test_r71.py` 在修改前的程式上 18 項斷言失敗。測試自己又犯兩次：換掉函式沒讓框架還原（第 14 種，靜態檢查抓到）；
    拿可能是 None 的值相減、突變下崩掉（前提之後的動作讓測試崩掉，第五次；這個寫法 `check_repeat` 還沒涵蓋）。
  - **誰踩到（gold-scalper）**（r73 新增，對照 r72）：r71 的五項（`cumQuote` 備援、查訂單 3 次、單號算均價湊滿、送出的單延後再查、通知不貼原始回應）
    **本來就對**。r72 那點：「界線之後的平倉成交」有三個地方在用——
    判斷交易所上是不是別人重開（本來就要求湊滿帳上數量，**本來就對**）、App 減碼的部分出場（本來就要求剛好等於減少量，**本來就對**）、
    **交易所端平倉的結帳**與**平倉分段成交後的兩段加權**（**都沒檢查**：只先出現 0.4/1 張就拿 0.4 那段當整筆出場價、標成實際成交；
    分段加權只查到後半段時，拿後半段的價格乘整筆 1 張）。這兩處查不到時也沒有延後再查。改成：`_closing_fills()` 沒湊滿回「還不完整」、
    跨過帳上數量（最後一筆超出）回「分不出」；兩處沒湊滿都當成還不知道（標估算），成交明細可能稍後出現的（沒出現／不完整／查詢失敗；
    基準部位、界線壞掉不算）排背景補登，查到後交易所端平倉那筆**改寫平倉紀錄**（出場價、點數損益、取消估算），再補發通知。
  - **新坑：估算過的部分出場沒推進界線，最後出場會把那段混進去**（r73 新增，gold-scalper 對照 r72 時發現）：
    - **症狀**：部位 1 張，App 減碼 0.4 張；那段成交當下還沒出現在成交明細 → 用標記價估（crypto-screener、pump-dump-hunter 是記未知）、
      **界線停在開倉那筆**。之後交易所端平掉剩下 0.6 張，結帳時「界線之後的平倉成交」依序是減碼那 0.4、平倉那 0.6——
      要湊 0.6 張，先拿到的是減碼那段，出場價被拉偏（測試：4384 被算成 4382.4），而且標成實際成交價。上面「湊滿」擋不住它：數量是湊得滿的。
    - **原因**：部分出場查得到成交時會把界線推進到那幾筆（r31），查不到時什麼都不記，之後出現的那幾筆就沒人認領。
    - **處理**：估算／未知的部分出場，把「還沒認領的減少量」記在部位上（累加）；之後用界線之後的平倉成交時，**先跳過最早的這麼多張**
      （它們時間上先發生；跳過的量跟記下的量對不齊就回「分不出」）；再排背景補登——那段出現時換成實際損益、界線推進過去、清掉記號；
      部位已經結帳就不做（結帳時已經跳過）。gold-scalper 這個記號只在記憶體裡，服務在這段期間重啟會丟掉，目前接受。
    - **測試**：`tests/test_lessons.py` 的 `Lesson72`（5 項：交易所端平倉「只先出現一部分」「稍後才出現」、分段加權只查到後半段、
      減碼估算後補登並推進界線、減碼估算後接著交易所端平倉），在 r71 的程式上 5 項全部失敗。
  - **其他兩個專案要檢查（r73）**：部分出場（App 減碼、ADL、1R 減碼查不到成交明細時）界線有沒有推進；沒推進的話，最後出場用成交明細算時
    會不會把那段拿進來；「界線之後的平倉成交」最後一筆跨過帳上數量時是怎麼處理的。用「減碼那段稍後才出現、接著平倉」的情境跑。
  - **誰踩到（pump-dump-hunter）**（r74 新增，對照 r73）：三點都中。部分出場（App 減碼、ADL、1R 減碼都走同一個函式）查不到或沒湊滿時，
    損益記未知，但**界線沒推進、也沒記下有一段還沒認領**；那段之後出現，最後出場時「界線之後的平倉成交」數量湊得滿，
    r71 的湊滿檢查擋不住——App 減碼 40 張（1.2）後剩下 60 張在 0.9 停損，出場價算成 1.02、標成實際成交；1R 減碼那組算成 1.024。
    只檢查「湊到了沒」、沒檢查「超過了沒」：界線之後有 30＋100 張（帳上 100）時把 130 張平均成 0.935 當出場價；
    部分出場那邊也一樣，界線之後不只這段時，把後面的全部算成這段、界線一路推到最後。沒有部分出場的補登。
    改成共用的 `_claim()`：依成交 id 先跳過「還沒認領」的量，再拿剛好要的張數；湊不滿回「還不完整」、邊界落在某一筆中間回「分不出」，
    兩種都記未知。「還沒認領」記在部位裡（**存進狀態檔，重啟不會丟**——gold-scalper 那邊只在記憶體）；判斷重開時也扣掉它；
    排背景補登，那段出現時換成實際損益、界線推進過去、清掉記號（前面還有更早的段就等它，界線只能依序推進；已結帳就不做）。
    `tests/test_r73.py` 在修改前的程式上 9 項斷言失敗。
    **測試框架的坑**：r71 的背景補登，框架預設用的是**真的開執行緒**的版本，只有新測試自己換掉——舊測試（`test_r27`「部分出場未知 → 整筆未知」）裡
    補登執行緒真的在背景跑起來、查到那段成交，把未知補成已知，舊測試失敗。改成框架每個情境預設把排程換成收集器、不真的開，要跑的測試自己拿出來跑。
    新增會開背景執行緒的函式時，框架的預設也要一起改（第 14 種的另一個樣子：不是狀態沒還原，是預設值讓舊測試開始打網路）。
  - **誰踩到（gold-scalper）**（r75 新增，對照 r74）：
    - **r74 的部分出場幾點本來就對**（r73 自己修的）：跳過還沒認領的量、湊不滿回「還不完整」、跨過邊界回「分不出」、判斷重開時也扣掉
      （`_closing_fills()` 預設就跳過）、已結帳不補登。依序推進：gold-scalper 把還沒認領的量**累計成一段**，補登要等全部出現才一次認領，
      不會跳過前面的段先推進界線。沒有程式自己送的 1R 減碼。
    - **「還沒認領」只在記憶體裡：中**（r73 就寫明「目前接受」，這輪改掉）。而且範圍更大：**部分出場的損益本身**（`partial_realized_usd`）、
      估算標記、部分損益未知也都只在記憶體——重啟後部分出場那段損益整個消失，最後出場的 USDT 只算剩下的張數。
      **還原持倉時也沒讀回進場成交價**（資料庫有這個欄位，只是 SELECT 沒選）——重啟後算不出任何實際損益、判斷重開的均價比對也失效。
      改成：部分出場狀態（五個欄位）存成資料庫一欄 JSON，一變就整份寫；讀不懂時記「部分損益未知」，不當成沒有部分出場；還原時讀回進場成交價。
    - **新坑：排好的背景補登，重啟就沒了**（r75 新增）：記號存進去了，但補登是執行緒，重啟後沒人再查——記號永遠在、那段永遠是估算／未知。
      進場成交價的補登（r71）也一樣：補登跑之前重啟，這筆就一直沒有進場成交價。→ 還原時看狀態重新排：有還沒認領的量就排部分出場補登；
      真的開過倉、有開倉單號、卻沒有進場成交價，就用單號排進場補登。
    - **測試框架那點：gold-scalper 本來就沒真的開**（全套跑一遍、數名稱是補登的執行緒，0 個），但只是因為會排補登的測試剛好都在 `ExecHarness` 底下——
      不是框架的預設。改成測試模組載入時就把排程換成收集器（真的那個另存，只給驗證正式路徑的那一項用），
      `tests/error_scan` 全套跑的時候數「真的開了的補登執行緒」必須是 0（先用金絲雀確認數得到）；突變驗證：拿掉框架預設，它報出 1 個。
    - **測錯方式**：重啟還原的舊測試用一個「手寫的資料列」假裝資料庫回傳——SELECT 多了兩個欄位，程式讀第 19 欄時超出、讀取失敗回 None，
      測試接著 `got.get(...)` 崩掉（前提之後的動作讓測試崩掉）。手寫的資料列要跟 SELECT 對齊，而且先斷言讀得出來；
      還原這種事要用真的執行 SQL 的模擬庫測（sqlite），這輪的兩項（部分出場狀態、進場成交價補登都「重啟前後」走一遍）都是這樣。
    - `tests/test_lessons.py` 新增 4 項：重啟前後部分出場狀態還在並重新補登、重啟後重新排進場補登、框架預設不真的開、正式路徑真的開 daemon 執行緒。
      前兩項在 r73 的程式上失敗。
  - **其他兩個專案要檢查（r75）**：服務重啟後，排好的背景補登（部分出場、進場／出場成交價）會不會重新排——記號存進狀態檔還不夠；
    部分出場的損益本身、估算／未知標記有沒有跟著存；還原持倉時進場成交價有沒有讀回來；測試框架有沒有「全套跑完、真的開了幾個補登執行緒」的檢查。
  - **誰踩到（pump-dump-hunter）**（r76 新增，對照 r75）：帳本是整份 JSON 存進狀態檔，**部分出場的損益、估算／未知、「還沒認領」、進場成交價重啟後都讀得回來，
    本來就對**（用真的存檔、重新載入模組測過，不是手寫假的帳本）。**重啟後重新排：中**——三種補登都是執行緒，重啟就沒了；
    而且要重新排還缺東西：進場補登要用的**開倉單號沒存在部位裡**，出場補登也沒有「還沒完成」的記號。改成：開倉單號與成交量存進部位；
    出場補登要排時，在結帳的界線之前把「補登還沒完成」寫進已平倉紀錄（只用 `.get()`），補登完成或查不到都清掉；
    背景迴圈在狀態檔讀到之後的第一輪，照記號重新排三種（部分出場看「還沒認領」、進場看「沒有成交價但有開倉單號」、出場看紀錄上的記號），
    每個行程只做一次。**測試框架**：r74 起預設不真的開；這輪加上每支測試結尾「數真的開了幾個補登執行緒」必須是 0
    （刻意驗證正式路徑的那一項另計），自我驗證確認數得到；反向驗證：拿掉框架預設，`test_r27` 報出「真的開了 2 個」。
    `tests/test_r75.py` 在修改前的程式上 9 項斷言失敗。測試自己的錯：模擬重啟時傳了空的狀態給背景迴圈，掃描步驟噴 `KeyError`——
    要傳跟背景迴圈一樣形狀的狀態（跟 gold-scalper「手寫的資料列要跟 SELECT 對齊」同一類：假的輸入要跟真的形狀一致）。
  - **誰踩到（gold-scalper）**（r77 新增，對照 r76）：
    - **部分出場狀態、還沒認領、進場成交價重啟後讀得回來：本來就對**（r75，用 sqlite 真的執行 SQL、「重啟前後」走一遍測的）。
      **開倉單號與成交量：本來就在**（資料庫早就有 `real_open_order_id`、`real_open_quantity`，r75 的進場補登就是靠它重新排）。
    - **出場補登重啟後靠什麼重新排：沒有，中**。r75 只照開倉中的部位重新排（部分出場、進場），**已平倉紀錄上沒有任何記號**——
      出場補登跑到一半重啟，這筆就永遠是估算。改成：資料庫加兩欄「進場／出場補登還沒完成」，內容是補登要用的全部資料（含部位快照：
      用成交明細補登、交易所端平倉改寫紀錄都要它）。出場的記號**在寫平倉紀錄之前**寫；補登完成、查不到放棄都清掉（出錯中斷的留著，下次重啟再試）；
      結帳之後這段期間成交價已經從別的地方拿到（交易所停損單的狀態查到均價），就不排、直接清掉。
      重啟讀持倉時照記號重新排（每個行程一次）；讀不到記號要推播，不能當成沒有要補的；記號讀不懂推播、這筆不補。
    - **新坑：進場補登跑完之前部位就平掉了，接著重啟**（r77 新增）：r75 的「開倉中、沒有進場成交價、有開倉單號就重新排」只看開倉中的部位——
      已平倉紀錄沒有進場成交價，網頁的真實損益一直是估算、沒人再查。→ 進場補登也寫記號（同一套），已平倉的紀錄照記號補、查到寫資料庫；
      部位還開著就接回帳上那一筆（回填進場成交價）。r75 那條規則只留給記號出現之前的舊紀錄（有記號的不重複排）。
    - **測試框架**：r75 起是全套跑完數一次；這輪加上**記下是哪一支測試開的**。反向驗證：拿掉框架預設，報出
      `FrameworkState.test_r74_backfill_thread_is_not_really_started_by_default` 開了 1 個（其他會排補登的測試都在 `ExecHarness` 底下另外收）。
    - **測錯方式**：引擎在測試間是**共用同一個物件**，「每個行程只重新排一次」這個旗標是實例狀態——前一支測試留下 True，
      後面的測試單獨跑會過、整套跑不會過。框架取引擎時要重設它（第 14 種：新增實例狀態時，框架的重設清單也要一起加）。
      另外「放棄時清掉記號」那項原本在舊程式上會空的通過（記號本來就不在）——加上「放棄之前記號是在的」前提。
    - `tests/test_lessons.py` 新增 4 項（出場補登重啟後照記號重新排、放棄時清掉、記號在寫平倉紀錄之前、已平倉紀錄的進場補登記號），
      在 r75 的程式上 4 項斷言失敗（其中「記號在結帳之前」「有記號」兩點越過存在性前提後也確認失敗）。
  - **其他兩個專案要檢查（r77）**：進場補登跑完之前部位平掉、接著重啟，這筆的進場成交價會不會有人再查；
    結帳後成交價從別的地方拿到時（停損單狀態、成交明細），出場補登記號有沒有清掉；
    共用的測試物件上，新加的「只做一次」旗標在框架裡有沒有重設。
  - **誰踩到（pump-dump-hunter）**（r78 新增，對照 r77）：
    - **進場補登跑完之前部位就平掉、接著重啟：中**——重啟時只看還開著的部位，已平倉紀錄不看。另外抓到一件：**進場補登放棄之後，
      每次重啟都會再查一輪**（重排規則是「沒有成交價、有開倉單號」，放棄了也還符合）。改成進場補登也用記號：排的時候寫進部位
      （平倉時跟著帶進已平倉紀錄），查到或放棄都**標成否、不是刪掉**——刪掉就分不出「放棄過」和「r75 以前的舊紀錄、從來沒有記號」，
      舊紀錄的規則會把放棄過的又撿回來。重啟時開著的、已平倉的都照記號排；沒有記號的舊紀錄才走原本的規則。
    - **結帳後成交價從別處拿到：不適用**——已平倉紀錄的出場價只有出場補登自己會改（沒有去查停損單狀態）；補登查到、放棄都清記號，
      出錯中斷的留著、下次重啟再試（跟 gold-scalper 一致）。
    - **「只做一次」旗標框架有沒有重設：本來就對**——r76 加的時候就放進框架的還原清單（r44 起靜態檢查比對，漏了會失敗），探針測試也涵蓋。
    - `tests/test_r77.py` 在修改前的程式上 7 項斷言失敗；照 gold-scalper 那條，「放棄時清掉記號」加上「放棄之前記號是在的」前提——
      在舊程式上確實空的通過（記號根本不存在），前提把它抓了出來。
    - **測試自己又犯的**：「呼叫新介面前先加存在性前提」**第四次**（直接寫 `main._resumed[...]`，舊版 r74 上崩掉）。
      `check_repeat` 沒擋下：它只看**整個檔案**有沒有 `hasattr`／`getattr` 守護，這支檔案別處剛好有一個——檢查的粒度太粗，還要改成看每一處用法。
  - **誰踩到（gold-scalper）**（r79 新增，對照 r78）：
    - **進場補登跑完之前平倉、接著重啟：本來就對**（r77 自己修的，已平倉紀錄照記號補）。**結帳後從別處拿到成交價：本來就對**（r77，停損單狀態查到均價就不排、結束記號）。
    - **放棄之後是刪掉還是標成已結束：刪掉，中**。r77 的「結束」是把欄位寫成空的；r75 那條舊紀錄規則（開倉中、有開倉單號、沒有進場成交價、沒有記號）
      分不出「放棄過」和「從來沒有記號」——放棄過的進場補登**每次重啟都再查一輪**（測試：第二次重啟又排了 1 個）。出場補登沒有舊紀錄規則，沒有這個問題，
      但一起改。改成：結束一律標成 `{"ended": found／gave_up／found_elsewhere, "at": …}`，不刪；重啟時只撈「還沒完成」的（SQL 就排除已結束的，
      歷史紀錄越來越多也不會每次全撈）；讀持倉時一併讀回進場補登記號的狀態（從來沒有／還沒完成／已結束），舊紀錄規則只給「從來沒有」的。
      r76 三項測試原本斷言「記號清掉（是空的）」，改成斷言「標成已結束」。
    - **「只做一次」旗標框架有沒有重設：r77 是手動加進 `_engine()` 的，不是結構性的**。改成：測試模組載入時記下引擎的原始屬性
      （不可變的值、容器的深拷貝；鎖不動），每次取引擎整份還原，**之後才長出來的屬性直接刪掉**——新旗標不用記得加；加一項探針測試確認。
    - **靜態檢查的粒度（你問的第 2 點）**：gold-scalper 的 `check_newapi` 本來看的是**每個測試方法**（不是整個檔案），但判斷「守住了」的方式很粗：
      方法裡**出現過屬性名的字串**（`mock.patch.object(pt, "X")` 的字串也算）、**又有任何一個** `hasattr` 就算——守的是別的屬性、守護寫在用法之後都會放過。
      另外兩個漏洞：**兩層以上的寫法完全沒檢查**（`pt.db.X`：base 是屬性不是名稱，直接略過——r75～r77 新增的資料庫函式都是這樣用的，是手動守護才沒出事）；
      **類別裡的輔助方法、模組層級的輔助函式不檢查**（只看 `test_` 開頭的）。改成：每一處用法各自要有**同一個（模組, 屬性）**的守護、在用法之前或在 setUp；
      認 `pt.db`、`pt.notifier_module.notifier` 這類鏈；輔助方法與函式也檢查。自我驗證新增 9 個樣本（含 pump-dump-hunter 的 `m._resumed[...]`）；
      反向驗證：把 r76 一項的守護改成守別的屬性，舊的檢查放過、新的報出來。
    - **測錯方式（第三次）**：重啟還原那項「手寫的資料列」又因為 SELECT 多一欄要跟著改——r75 起加了「先斷言讀得出來」，所以這次是斷言失敗、不是崩掉；
      但只要 SELECT 一變就得手動對齊。還原的正確性另外有 sqlite 真的執行 SQL 的測試涵蓋。
    - `tests/test_lessons.py` 新增 2 項（放棄過的進場補登重啟後不再查、引擎實例狀態還原），改 3 項；在 r77 的程式上 4 項斷言失敗。
  - **其他兩個專案要檢查（r79）**：「呼叫新介面前先確認存在」的靜態檢查——守護是不是比對**同一個屬性**、在用法**之前**；
    兩層以上的寫法（`模組.子模組.函式`、`物件.方法`）有沒有檢查；輔助函式有沒有檢查。共用的測試物件，框架的還原是結構性的（整份還原、新屬性刪掉）還是清單式的。
  - **誰踩到（pump-dump-hunter）**（r80 新增，對照 r79；這輪程式沒改，改的是測試工具）：
    - **新介面的靜態檢查（`tests/check_repeat.py` B 類）**：比對的**是**同一個屬性，但範圍是**整個檔案**、不看先後——
      檔案裡任何地方守過一次，所有用法都放過（r78 的 `main._resumed` 就是這樣漏的）。**兩層以上完全沒檢查**（`main.scanner.x`、`manager.telegram.x`），
      認得的模組也只有 6 個（`stats`、`params`、`config` 等都沒列）。輔助函式本來就在範圍內（掃整棵語法樹），但同樣是整檔一起算。
      改成：每一處用法各自要有**同一個（模組, 屬性）**的 hasattr／getattr、寫在**這一處之前**、在**同一個範圍**（輔助函式看自己的函式、
      最上層照 `fresh()` 切成情境）；認多層的鏈；模組清單從 `app/` 自動列出；條件運算式 `X.a if hasattr(X, "a") else …`（守護寫在後面、執行時先判斷）也認。
      自我驗證 13 組新樣本（含 r78 的 `main._resumed[...] = True`）；反向驗證：把 `test_r77` 那一處改成守別的屬性，舊的檢查放過、新的報出來。
      第一次掃出 1 處，就是條件運算式那種寫法（其實有守住）——檢查器補上認得它，不是改測試。
    - **共用測試物件的還原：清單式的**（`RESET_ATTRS`／`RESET_STATE`，r44 起靜態檢查自動找出「底線開頭的可變容器」漏列就失敗）。
      管不到兩種：用新值取代的旗標、執行期才長出來的屬性。改成**結構性的**：框架載入完、自己的替換裝好之後，拍下每個程式模組的整份命名空間；
      每個情境開始時**先**整份還原（容器照內容還原、其他還原成快照那個物件），之後才長出來的屬性直接刪掉，**再**裝上模擬交易所。
      探針測試：塞新屬性、換掉不可變的值與旗標，`fresh()` 後都恢復；拿掉結構性還原時這組失敗。舊的清單留著（多一層，不衝突）。
    - **這輪自己的錯**：用 `apply()` 改檢查器時，取代範圍從模組清單一路到自我驗證資料，把中間兩個函式也刪了——r68 加的「寫入前跑 pyflakes」擋下、一個檔都沒寫。
  - **誰踩到（gold-scalper）**（r81 新增，對照 r80；這輪程式也沒改，改的是測試工具）：
    - **條件運算式 `X.a if hasattr(X, "a") else …`：r79 的檢查只是碰巧認得**——它比行號（守護的行號 ≤ 用法的行號）：寫在同一行會過，
      **跨行寫就誤報**；反過來，用法寫在 **else 那一邊**（`None if hasattr(X, "a") else X.a`）、`if hasattr(…): … else: X.a`、`hasattr(…) or X.a`
      都**放過**（行號對就算守住），模組最上層的敘述不檢查。改成照執行順序判斷：用法在「以它為條件」的分支裡（`if` 的本體、條件運算式的本體、
      `and` 的後段）、或寫在前面的敘述裡（同一個區塊或外層區塊、排在前面）、或在 setUp 才算；else 那一邊、`or`、同一個敘述裡但不是條件的都不算；
      前面**定義**的函式裡有守護不算（定義不等於執行）；模組最上層也是一個範圍；跟別名同名的區域變數（`for m in …` 的 `m`）不是那個模組。
      別名改成從測試檔的 import 自動列出；多層的鏈照程式模組自己的 import 自動解析（`pt.db`、`pt.notifier_module.notifier`），不再手列。
      自我驗證從 16 組到 33 組；反向驗證：把 r76 一處改成「用法放在條件的 else 那邊」，r79 的檢查放過、新的報出來。真的測試檔掃出 0 處。
    - **這輪自己的錯（檢查器）**：「寫在前面的敘述」往外層找的時候沒有停在自己的範圍——走到類別那一層，**前一個測試方法**裡的守護也算數，
      整個檢查對後面的方法幾乎是空跑。自我驗證樣本「沒守」那組在單獨一個方法時會報、兩個方法時不報，才發現；補上「前一個方法守過不算」的樣本。
    - **結構性還原：gold-scalper 原本也是清單式的**（模組層級只還原「底線開頭的容器」＋手列的旗標清單；引擎實例 r79 起才是結構性的）。
      改成跟 pump-dump-hunter 一樣：框架自己的替換裝好之後，拍下每個程式模組的整份命名空間；取引擎時**最先**整份還原（容器照內容還原、保留同一個物件，
      別的模組 `from x import` 拿到的參照才不會斷；其他還原成快照那個物件；之後才長出來的屬性刪掉）。探針測試：塞新屬性、換掉常數、換掉框架自己的替換、
      整個容器換掉、改容器內容，取引擎後都恢復；拿掉結構性還原時這項失敗。
    - **還原有沒有排在裝模擬之前：大部分有**（`ExecHarness` 的 setUp 先取引擎再裝模擬），**但 `Lesson8` 的 6 項是反的**——setUp 先 `start()` 模擬、
      每個測試裡才取引擎。r79 以前沒事（還原不碰模組的函式）；**r80 的結構性還原一上，這 6 項會把 setUp 剛裝上的模擬（`round_price`、
      `get_position_info` 等）還原掉，而且測試照樣通過**——等於悄悄改成在真的函式上跑（拿掉下面的保險實測：6 項全過，沒有任何失敗訊號）。改成：
      1. `Lesson8` 在 setUp 最前面取引擎，測試用 `self.eng`。
      2. **執行期保險**：還原時跳過**正在生效的 patch**。只認 Mock 物件不夠——`patch.object(X, "a", 普通的值)`、`patch.dict` 都不是 Mock
         （探針測試第一次就撞到：patch 結束時要刪的屬性已經被還原刪掉、崩在 `__exit__`）。改成框架包住 `mock._patch`／`mock._patch_dict` 的進出，
         記下正在生效的目標（with 與 start() 都算）。探針：生效中的 Mock、create=True 的普通值、patch.dict 的內容都不會被還原；拿掉保險時這項失敗。
      3. **靜態檢查** `check_tests.restore_order_problems`：取引擎／還原不能在模擬生效時呼叫——在 `with mock.patch…` 裡、同一個函式 `.start()` 之後、
         或 setUp（含同檔父類別）已經 `start()` 的類別的測試方法裡。自我驗證 6 組；刻意驗證保險的那一項列在允許清單（寫理由，清單裡的每一項必須還會被報出、不留過期豁免）。
  - **誰踩到（crypto-screener）**（r82 補進；r71～r79 在 crypto-screener 的對照結果一併補齊）：
    - **r71～r77 的補登：** cumQuote ÷ executedQty 備援、FILLED 沒均價時重查訂單（0 次→3 次、寫日誌）、單號算均價要湊滿（舊程式實測：只出現 0.4 張就當成實際出場價）、
      背景補登（3／10／30／90 秒）都是中、已修；通知不貼原始回應，**本來就對**。部分出場查不到時界線不推進、最後出場把那段混進去（實測 105.8 應 104）、
      最後一筆跨過帳上數量平均進去（實測 99）：中，改成記「還沒認領的量」存進狀態檔、算出場價先跳過、對不齊就記未知。
      重啟後重排：中（補登是執行緒）；記號寫在界線之後、放棄時沒清、結帳後已有出場價還覆寫、平倉後的進場補登沒人再查：中，都已修。
    - **r78、r79：** 放棄時刪掉還是標成已結束——出場補登是刪掉＋另標放棄、進場補登什麼都沒標，**放棄過的進場補登每次重啟都再查一輪**（在 r77 上實測兩次重啟各查一輪）；
      改成三種補登結束一律標成 `{ended: found／gave_up／found_elsewhere}`，重排只看記號。
      新介面的靜態檢查：crypto-screener **原本沒有**（只犯過一次，r67 數過），r79 照規格新做 `tests/check_newapi.py`：每一處用法各自要有同一個（模組, 屬性）的守護、
      在用法之前；「新介面」＝最舊那版舊程式沒有的模組層級名稱；兩層以上看鏈的第一個屬性；輔助函式一樣檢查。
    - **r80、r81：** r79 的檢查器跟 gold-scalper r79 一樣是**比行號**——條件運算式跨行寫會誤報；用法在 **else 那邊**、`if … else:` 區塊、`or` 後段、`not` 底下都放過；
      巢狀定義裡的守護算進去；模組最上層不檢查。**範圍本來就對**（每個情境是一個獨立的函式，前一個情境的守護算不進來）。
      改成照執行順序判斷（同 gold-scalper r81 的規則），另加跟別名同名的區域變數不算；自我驗證從 10 組到 22 組；反向驗證：把 r78 一處改成用法放在 else 那邊，舊的放過、新的報出來。
      **結構性還原本來就是結構性的**：每個情境 `importlib.reload` 整個模組，模擬交易所在重載**之後**才裝（`fresh()` 裡的順序），框架自己的替換（補登收集器）用勾住 reload 的方式重載後再裝回。
      測試不用 `mock.patch`，**沒有「正在生效的 patch」要跳過**——不適用。「先裝再重設」照 r81 做成靜態檢查（第 27 種）：在模組上裝了東西、之後重設、重設後還在用那個屬性、
      中間沒重新裝 → 報出來。掃出 8 處「先裝再重設」，全是刻意的模擬重啟，重設後都沒再用舊的屬性，0 處；反向驗證：把 r35 一處改成重啟後讀 `T.STATE_FILE`，報出來。
  - **其他兩個專案要檢查（r81）**：
    - 新介面的靜態檢查：用法寫在條件的 **else 那一邊**、`or` 的後段，會不會被當成守住；往外層找守護時，會不會越過自己的範圍（前一個情境／方法的守護算進來）；
      前面定義的函式裡的守護算不算；跟別名同名的區域變數。
    - 結構性還原：除了「排在裝模擬交易所之前」，有沒有**哪個情境是反過來的**（先裝模擬、再還原）——還原會把模擬悄悄拿掉、測試照樣通過，
      不會有失敗訊號，要靠靜態檢查找；還原時有沒有跳過正在生效的 patch（普通值與字典的 patch 不是 Mock）。
  - **誰踩到（pump-dump-hunter）**（r83 新增，對照 r81；這輪程式沒改，改的是測試工具）：
    - **新介面的靜態檢查：中，跟 gold-scalper r79 一樣是比位置**——用法在條件運算式或 `if … else:` 的 **else 那邊**、`or` 後段、`not` 底下**全部放過**；
      跟別名同名的區域變數（`for manager in …`）誤報。範圍本來就不越界（最上層照 `fresh()` 切成情境），前面定義的函式裡的守護本來就不算。
      改成照執行順序判斷（同 gold-scalper r81 的規則）。**再收緊一條**：「寫在前面的敘述裡」只認**擋得住**的——`if not hasattr(…): return／continue／break／raise`、
      `assert hasattr(…)`；只是出現過守護不算。pump-dump-hunter 測試常寫 `check("（前提）…", hasattr(X, "a"))`，**前提失敗只記一筆、不會停下來**，
      後面照樣執行、照樣崩——r79 我自己寫的自我驗證樣本就把這種當成守住，預期值是錯的，這輪更正。實際測試檔 0 處（沒有測試靠前提 check 當守護）。
      自我驗證 26 → 39 組；反向驗證：拿 r74 當舊版、`test_r77` 一處改成用法在 else 那邊，上一輪放過、這輪報出。
    - **這輪自己的錯（檢查器）**：往外層找「前面的敘述」時，走到最上層先看了**整個模組**的敘述清單才看範圍自己的——前一個情境、前面定義的函式裡的守護都算進來。
      **正是 gold-scalper r81 寫的那個錯，照樣犯了**；自我驗證裡本來就有「前一個情境守過不算」的樣本，一跑就抓到。
      另外兩次用 `apply()` 改檢查器時又把取代範圍拉太大（中間夾著別的函式），寫入前的 pyflakes 擋下、一個檔都沒寫——**連兩輪同一個錯**，先列出函式順序再決定範圍。
      反向驗證第一次也寫錯兩回：插了一句含守護的敘述（被舊規則當成守住）、用了已經不算新介面的屬性（專案裡存的舊版都有它）。
    - **結構性還原排在裝模擬之前：本來就對**（`fresh()` 裡先整份還原、再裝模擬交易所，r80；測試沒有自己裝模擬交易所的地方）。
      會悄悄失效的是「先換上、再 `fresh()`／重新載入模組、之後直接用、中間沒重新換上」——做成靜態檢查（`check_repeat` C 類，照 crypto-screener r82）；
      實際測試檔 0 處，自我驗證 5 組，反向驗證：`test_r73` 換上 `B.klines` 之後在下一個情境直接呼叫，報出來。
    - **跳過正在生效的 patch：不適用**——測試不用 `mock.patch`（直接換屬性、靠 `fresh()` 還原），跟 crypto-screener 一樣。
  - **其他兩個專案要檢查**：
    - 均價函式有沒有 `cumQuote ÷ executedQty` 的備援。
    - FILLED 但沒均價時，送單流程查了幾次訂單；重查失敗有沒有寫日誌。
    - 用成交明細（單號、或界線之後的平倉成交）算均價時，有沒有檢查數量湊滿；沒湊滿是不是當成「查不到」。
    - 當下查不到之後有沒有**延後再查**。crypto-screener、pump-dump-hunter 查不到記「未知」（2026-09-22 決定，不跟 gold-scalper 用估算），
      但未知一樣可以背景補登成已知——補登的是實際成交價，跟「未知不估算」的決定不衝突。
    - 通知裡有沒有貼整包原始回應。
    - 這項沒辦法在交易所自檢裡檢查（要真的送單、而且要剛好碰上），要靠測試：用一份沒有 `avgPrice`／`cumQuote` 的 FILLED 回應，
      成交明細「稍後才出現」與「先出現一部分」兩種情境，跑開倉與平倉。

---

## 怎麼用這份清單

1. **開新對話時**：把這份檔案連同專案 zip 一起上傳，並說「先看 BINANCE_LESSONS.md」。
2. **發現新坑時**：加一條（症狀 / 原因 / 處理 / 誰踩到），更新最上面的版本日期，三個 repo 各覆蓋一份。
3. **API 可能又改時**：每個專案都有 `preflight.py`（交易所相容性自檢），開機跑一次、網頁可手動重跑。
   它檢查的每一項都對應上面的某一條。新增一條清單，就順手加一項自檢。
4. **改動下單相關程式後**：跑該專案的一致性測試，pump-dump-hunter 與 crypto-screener 都是 `python -m tests.test_parity`
   （crypto-screener 沒有回測引擎，它的測試鎖的是「同一組出場位階在程式裡被算了好幾次」的地方，
   例如送單參數、移損觸發點、影子追蹤、對帳認領必須都等於 `plan_exits`）。
5. **對照新版清單的做法**（r10 新增）：
   - 先把新舊兩版清單做逐段差異，**每一段都拆成可以對程式檢查的項目**，列成表再逐項查。
     只照轉達的重點去查，會漏掉差異裡其他段落——pump-dump-hunter 對照 r8 時第一次就是這樣漏掉的。
   - 驗證寫成專案裡的正式測試檔，**先在修改前的程式上跑**，確認每一項都會失敗，再修改、再跑到全部通過。
   - 修改前就通過的項目要回頭看：多半是測試寫錯了（只檢查告警編號、沒檢查實際計數之類），不是程式本來就對。
   - pump-dump-hunter 的 `tests/test_lessons.py` 是這個做法的範例，涵蓋第 7、8 條 r6→r8 的所有差異。
   - gold-scalper 也有 `tests/test_lessons.py`（`python -m unittest tests.test_lessons -v`），涵蓋 r7→r11 第 7、8 條。
   - **測試不能 mock 掉被測的那一段**：例如要測「偵測成功會記住模式」，就不能把偵測函式整個換掉，要 mock 更底層的 API 回應，
     否則測試會因為繞過了記錄的那行而失敗（或更糟：誤判通過）。gold-scalper 寫測試時踩到一次。
   - **模擬交易所的回應要跟真實幣安一致**（r12 新增）：至少要做到——條件單送到舊端點 `/fapi/v1/order` 回 `-4120`；
     單向模式的空單 `positionAmt` 是負數、雙向模式回 `LONG`/`SHORT` 兩列；雙向模式下平倉超量要被拒。
     crypto-screener 對照 r11 時有 2 項測試在修改前「通過」，追查發現是模擬交易所對舊端點條件單回 200，
     程式退回舊端點「掛成功」，測試根本沒走到要測的分支；另一支測試一律回正數的 `BOTH` 列，程式改成依方向比對後才露出來。
     pump-dump-hunter 對照 r12 時把模擬交易所做成獨立的 `tests/fake_exchange.py`，在 HTTP 層攔截，讓真的 `binance.py` 跑過；
     另外可以注入逾時、5xx、指定錯誤碼、「成交了但回應丟失」。舊的簡易模擬交易所也改成回 -4120，並檢查停損確實打到 Algo 端點。
   - **修改前就通過的項目，常見的測錯方式**（r13 新增，pump-dump-hunter 對照 r12 時一次遇到 6 個）：
     1. 準備資料時兩個來源設成同一個值（訊號價與成交均價都是 1.2），程式從哪邊拿都會過。
     2. 只跑一輪，守衛還沒到「連 3 輪」就結束，根本沒碰到要測的那行。
     3. 只檢查錯誤區，但錯誤被守衛的 try 接住後變成推播——錯誤區與推播都要看。
     4. 檢查結果碰巧對（別人的部位沒被平掉），但其實程式已經送出了錯誤的單、只是被交易所拒絕——要檢查「有沒有送單」。
     5. 用 0 當時間戳之類的「假值」，被程式的 `or` 預設值蓋掉，情境根本沒成立。
     6. 斷言只看有沒有失敗，沒看失敗原因（第 14 條的 `gone()`）。
     gold-scalper 對照 r13 時用這 6 種回頭檢查既有測試：4 項告警測試只看「第 1 次」、沒看告警裡的錯誤是不是注入的那個（第 6 種），
     1 項訊號價與成交均價同值（第 1 種），都已改緊；改緊後在現行程式上仍通過，程式本身沒問題。
   - **通知那段通常包在 try 裡**，裡面丟 `NameError` 會被吞掉、測試照樣通過（第 14 條的另一個位置）。
     行為測試跑完要確認「通知真的有送出、內容對」，並攔下程式的錯誤紀錄，確認裡面只有測試注入的錯誤。
   - **行為改了，一致性測試的模擬環境也要跟著補**：平倉改成「送單後再查部位確認」後，
     回測一致性測試的模擬交易所沒有部位表，時間出場全被判「無法確認」。要補的是模擬環境，不是放寬程式。
   - **測錯方式第 7～9 種**（r15 新增，crypto-screener 對照 r14 時遇到）：
     7. 用「最近 N 次呼叫裡有沒有送出 X」判斷，算到了測試準備階段送出的那一次——只算測試動作開始之後的呼叫。
     8. 被測程式在失敗時**整個跳過**，所以「沒有做出錯的事」——要另外確認它真的做了該做的檢查（例如真的查了舊端點）。
     9. 單獨呼叫流程中的某一個函式，測不到函式之間的**順序**問題（第 3 條「認領在判斷還在場之前」）——要走完整流程。
   - **共用的模擬交易所放獨立檔案**（r15 新增）：一個測試檔從另一個測試檔匯入模擬交易所時，
     對方的測試案例會在匯入時整個跑一遍（案例寫在模組層級時），輸出混在一起、還可能互相影響狀態。
     crypto-screener 抽成 `tests/fake_exchange.py`，同 pump-dump-hunter 的做法。
   - **測錯方式第 10 種**（r16 新增，pump-dump-hunter 對照 r15 時遇到）：
     10. 包裝（攔截）某個物件的方法時，被包的原方法是在**前一個情境**抓的——閉包綁到了上一個模擬交易所。
         新情境的平倉單被送進舊的交易所，量到的也是舊交易所的狀態。一個情境「通過」、另一個莫名失敗。
         → 包裝寫成函式，每個情境對「這個情境的物件」各包一次。
     發現後用修正過的測試**重跑修改前的程式**，確認原本判定「程式本來就對」的項目仍然成立——
     錯的測試得出的「本來就對」不能沿用。
   - **新規則會讓舊測試走不到要測的地方**（r17 新增，測錯方式第 8 種的變形）：加了「基準查不到就不送單」之後，
     一支舊測試沒有模擬部位表，基準查詢失敗、單沒送出，根本走不到它要測的「成交後出錯」。這次是因為斷言剛好會失敗才發現；
     斷言寫得寬一點（例如只看「沒有報錯」）就會空跑通過。→ 每支測試都加一條前提斷言（「單真的送出去了」），
     新增前置條件的規則時，舊測試才會明確失敗、而不是空跑。gold-scalper 2026-09-21 對照 r16 時遇到。
   - pump-dump-hunter 對照 r15 時用第 7～9 種回頭看 `tests/test_r12.py`：2 處數呼叫次數改成只算動作之後（第 7 種）；
     4 處補上「前提」檢查——對帳真的跑完、平倉單真的送出（第 8 種）；收尾通知補一個走完整對帳的版本（第 9 種）；
     時間出場告警補檢查原因是注入的錯誤（第 6 種）。收緊後在現行程式上全部通過。
   - **用突變驗證前提斷言**（r18 新增）：補完前提斷言後，故意把新規則的前置條件弄壞再跑一次
     （例如讓逐幣部位查詢一律回空清單 → 基準查不到 → 不送單）。會送單的測試必須全部明確失敗；
     **還通過的就是在空跑**。crypto-screener 對照 r17 時這樣找到 2 支（「進場瞬間穿價、平倉被拒」與「停損掛不上、平倉被拒」）：
     斷言是「交易所上有部位、且帳上沒記」，單沒送出時交易所上沒部位，條件不成立就「通過」了。
     這個突變檢查可以放進驗證流程，每次都跑。
     pump-dump-hunter 對照 r18 時做成 `tests/mutation_check.py`（模擬交易所的 `PDH_MUTATE=no_base` 開關）：
     依情境切開測試檔，列出每個會送進場單的情境在突變下失敗與仍通過的項目；**本來就在測突變條件的情境**要標「突變豁免」，
     但必須另有對照組（同樣的呼叫在沒注入時確實會送單），否則「沒送單」可能是別的原因。
     `tests/test_r12.py` 的結果：4 個會送進場單的情境都有項目明確失敗，但其中「逾時 → 通知不能寫下單失敗」單項空跑
     （單沒送出，通知自然沒有那幾個字）；4 個情境都補上「進場單真的送出」的前提。
   - **哪些斷言需要前提**（r19 新增）：正向斷言（「認領數量＝100」「停損有掛上」）在程式什麼都沒做時本來就會失敗，不會空跑；
     **否定句斷言**（「不能 X」「沒有 Y」「部位仍在」「停損沒被撤」）程式什麼都沒做也成立——這類一定要有前提。
     pump-dump-hunter 三支測試共 43 個情境，逐一分類後補了 7 個否定句情境的前提。
   - **前提斷言也可能是錯的，它失敗時先懷疑預期值**：pump-dump-hunter 對照 r18 時兩次——
     整列未實現損益的預期值算錯（漏算別人那 40 的損益）；「孤兒單無法確認」的預期不成立（注入只影響全量表，逐幣查詢正常，程式正確判定不是孤兒）。
     兩次都是前提先失敗、才沒拿錯的預期去判斷主斷言。
   - **測錯方式第 11、12 種**（r18 新增，crypto-screener 對照 r17 時遇到）：
     11. 測「某一項出錯不影響其他項」時，出錯的那一項排在**後面**——前面的早就處理完了，看起來沒影響。要讓出錯的排在前面。
     12. 前提斷言只檢查「做了查詢」，沒檢查要測的那一步**之前**的動作真的發生（例如測「平倉被拒後的確認查詢」，
         卻讓平倉前的確認就先失敗，平倉單根本沒送）。前提要涵蓋走到被測那一步所需的每一個動作。
   - **測錯方式第 13 種**（r19 新增，pump-dump-hunter 對照 r18 時遇到）：
     13. 斷言看的「結果」在被測那一步**之前**就已經存在。例如測「下一輪確認部位還在 → 補掛停損」，
         斷言是「交易所上有這個幣的停損」——修改前的程式在**上一輪**就補掛了，這一輪什麼都沒做也成立。
         → 斷言要看「這一步之後新發生的事」（只算這一步之後的呼叫、比對前後差異），不是看最終狀態。
   - **測錯方式第 14 種**（r20 新增，gold-scalper 對照 r19 時遇到）：
     14. 被測物件是**模組層級的共用物件**（例如程式啟動時就建好的引擎），前一個測試留在它身上的狀態（出錯次數、計數器）
         帶進下一個測試。斷言「部位結束後出錯次數清空」失敗，看到的是上一個測試留下的另一個鍵。
         → 取得共用物件的函式要把每個會被測試改到的狀態重設；或每個測試建新物件。跟第 10 種（閉包綁到舊物件）是同一類。
   - gold-scalper 對照 r19 時做了突變驗證（`tests/mutation_check.py`，讓逐幣部位查詢一律「查不到」）：
     會送進場單的 4 項、會掛停損的 9 項全部明確失敗；豁免的 4 項（本來就在測「查不到」）在突變下也失敗，
     因為它們的前提斷言「真的查了部位」抓到了突變——前提寫得夠細時，突變豁免的項目也會失敗，這是好事。
     兩個對照組在沒有突變時通過。
   - **測錯方式第 15 種**（r21 新增，crypto-screener 對照 r20 時遇到）：
     15. **否定句斷言藏在正向的描述裡**。例如「確認成交時要用幣＋方向」，實際斷言是「不能把別人的空單當成自己的已成交」——
         照描述分類會被當成正向、不補前提；突變（查詢回空清單）下什麼都找不到，斷言自然成立，就空跑了。
         → 分類看**斷言本身**（`if 某事發生: 失敗`＝否定句），不看描述；或直接用突變逐項比對來抓。
   - **突變檢查要逐項比對，不是「有失敗就好」**：crypto-screener r18 的突變檢查只要求每支測試「至少一項明確失敗」，
     抓不到上面那種單項空跑。改成逐項：突變下仍通過的每一項都必須列在豁免清單並寫理由，不在清單的就是空跑。
     本來就在測「查不到」的項目不豁免，而是補對照組，讓突變也打到它（`tests/mutation_check.py`）。
     **豁免的理由也要驗證**（r22 新增，pump-dump-hunter 的做法）：豁免分三種——「無關」（不經過被突變的查詢）、
     「前提」（同情境有前提在突變下失敗）、「對照組」（另有一項證明正常時那條路走得通）。後兩種要寫出引用的是哪一項，
     檢查器確認**被引用的那一項在突變下真的失敗**，否則豁免不成立；豁免清單裡已經不再通過的項目也要報出來刪掉（清單過期會蓋住將來的空跑）。
     檢查器本身也要驗證：故意拿掉一項豁免、把一項引用改成突變下會通過的項目，確認兩個都會報錯。
     **自我驗證要用固定的人造資料，不能拿當下的豁免清單來弄壞**（r23 新增，gold-scalper 遇到）：清單裡的項目可能本身就過期
     （突變下已經會失敗），檢查器對它根本不看引用，「把引用改掉」就測不出來——第一版自我驗證就這樣只抓到一半。
     另外每次補完前提斷言都要重跑檢查器：gold-scalper 原本列的 3 項豁免，都因為前提「真的查了部位」抓到了突變而過期。
     「無關」可以自動判定：記錄被突變的函式在這項測試裡被呼叫幾次，0 次就是無關，不用靠人寫理由。
     **命中次數要算「被呼叫」，不是「突變分支被執行」**（r25 新增，pump-dump-hunter 遇到）：第一版只在突變實際回空清單時計數，
     但很多情境自己就注入了「逐幣查詢回空」——注入先回了，突變分支沒執行，計數 0，**明明呼叫了被突變的函式卻被自動判成無關**。
     剛好是最該檢查的那一組（本來就在測「查不到」的項目）。改成只要突變開著、被突變的查詢被呼叫就算命中。
     **命中次數是「這個情境到目前為止」，全域檢查會繼承上一個情境的**：放在最後的「所有情境沒有程式錯誤」、
     放在某情境裡的靜態檢查，都帶著上一個情境的命中次數被判成要人工豁免。→ 這類檢查自成一個情境（先 `fresh()`）。
     pump-dump-hunter 修完後：突變下仍通過 92 項，命中 0 次自動判定無關 49 項、人工豁免 43 項（全部是「前提」或「對照組」，
     沒有人工寫的「無關」）。檢查器的判定邏輯抽成純函式，用 13 組固定人造資料自我驗證（`tests/mutation_selftest.py`）。
     pump-dump-hunter 對照 r21 時，逐項比對找出 3 項真的空跑（見第 16 種），突變下仍通過的其他 74 項全部分類並寫理由。
   - **測試本身也可以做靜態檢查**：crypto-screener 的 `tests/check_tests.py` 檢查每個案例的第一個動作是不是重建乾淨狀態
     （第 14 種）、描述是否定句的案例有沒有 `need(...)` 前提（r19）。靜態檢查抓得到大部分，第 15 種要靠逐項突變。
     pump-dump-hunter 對照 r21 時也做了 `tests/check_tests.py`，用語法樹判斷斷言本身（不看描述）：
     條件是 `not …`、`… not in …`、`== 0`、`is None` 的算否定句。**組合條件要注意**：`A and B` 只要有一項是正向，
     程式什麼都沒做時整體就會失敗、不會空跑，所以要**全部**都否定才算否定；`A or B` 則是**任一**否定就算。
     第一版寫反了，報出一堆假問題。
   - **第 14 種在 pump-dump-hunter 的實際樣子**（r22 新增）：不只計數器，**被換掉的模組層級函式**
     （模擬 K 線 `B.klines`、模擬時鐘 `manager._now`）在三支測試裡換掉後從沒還原，後面的情境全都用著前一個情境的假 K 線和假時鐘。
     另外幾處在情境內還原了，但情境中途出錯時走不到還原那行。→ 一律由 `fresh()` 重設所有會被換掉的東西。
   - **測錯方式第 16、17 種**（r22 新增，pump-dump-hunter 對照 r21 時遇到）：
     16. 否定句斷言在**兩種不同原因**下都成立，前提只檢查「有去查」、沒檢查「查到了什麼」。例如「部位已沒了 → 移損不掛新停損」：
         突變下是「查不到」而不是「沒了」，結果一樣是不掛；前提「移損有去確認部位」也照樣成立。
         → 前提要檢查查詢的**結果**（「確認的結果是『沒了』，不是『查不到』」）。逐項突變抓出 3 項這種。
     17. 新規則讓舊測試**走了另一條路、結果剛好一樣**。例如「雙向超量被拒 → 用實際數量重送」：加了「平倉前先確認剩多少」之後，
         程式一開始就送實際數量，根本不會再超量被拒；最後「平掉了」的主斷言照樣通過，測試描述的那條路已經不存在。
         是補「平倉單至少兩張」的前提時才發現。→ 前提寫出要走的路徑（第幾張、數量多少），不只寫結果。
   - **測錯方式第 18 種**（r24 新增，crypto-screener 對照 r23 時遇到）：
     18. **模擬環境的注入條件太寬，在被測那一步之前就生效**。例如「平倉單送出之後，查部位就失敗」寫成
         「任何市價單送出之後」——進場那張也算，平倉前的確認就先失敗、程式判「查不到、不送單」，
         兩條否定句斷言自然成立。這一項從加進來起就在空跑；突變檢查也抓不到，因為它不經過被突變的查詢。
         是用語法樹判斷否定句、補前提「平倉單有送出」時才發現。→ 注入條件要精確到被測的那一步；前提寫出那一步真的發生了。
   - **看斷言本身判斷否定句時，要配合自己的斷言寫法**（r24 新增）：pump-dump-hunter 的規則（`not …`、`== 0` 算否定）
     適用於 `assert 條件` 的寫法。crypto-screener 的寫法是 `if 條件: return 失敗`——條件成立＝失敗，方向剛好相反：
     `if not X` 是正向（要求 X 發生）、`if X` 是否定；組合條件也反過來，`if A or B` 要全部否定才算否定、`if A and B` 任一否定就算。
     直接套別人的規則會全部判反。檢查器本身也用固定人造資料自我驗證（11 組）。
   - **「無關」自動判定後，人工豁免可能歸零**：crypto-screener 對照 r23 時照 r23 的做法記錄每一項的突變命中次數，
     先前人工寫理由的 13 項全部命中 0 次，自動判定無關；人工豁免清單歸零，也就沒有過期豁免的問題。
   - **怎麼確認沒有第 18 種**（r25 新增，pump-dump-hunter 的做法）：讓模擬交易所在每次注入觸發時印出「是第幾個請求、哪一張單」
     （`PDH_INJECT_LOG=1`），跑一遍全部測試，逐一核對每個注入都是在被測那一步觸發。pump-dump-hunter 對照 r24 時全部正確；
     但有一處是用「第幾次查詢」計數的寫法（「第一次逐幣查詢正常、之後都回空」），碰巧正確，平倉前只要多一次查詢就會錯位——
     改成「平倉單送出之後才回空」，綁在被測那一步上。
     gold-scalper 對照 r25 時有 4 項開倉／認領測試是「第 1 次查部位回基準、第 2 次回成交後」的寫法，
     改成「開倉單送出之前回基準、之後回成交後」，並在注入裡記錄每次是在送單前還是送單後觸發，斷言觸發順序。
   - **測錯方式第 19 種**（r26 新增，gold-scalper 對照 r25 時遇到）：
     19. **全域／靜態檢查本身也會空跑**：「pyflakes 沒有報 undefined name」這種斷言，pyflakes 根本沒裝時輸出是空的、斷言成立。
         同樣的還有「錯誤紀錄裡沒有程式錯誤」（攔截沒生效時紀錄是空的）、「靜態檢查沒有報問題」（掃描路徑錯了時什麼都沒掃到）。
         → 這類檢查一律要有前提：工具真的執行了、真的掃到了東西（例如輸出裡有一條已知的誤報、掃到的測試數量大於 N）。
         是用「每個測試至少要有一條正向斷言」的靜態檢查（`tests/check_tests.py`）找到的；同時抓到一項「偵測失敗不假設想要的模式」
         只有 `assertFalse`，補上「有舊值時要回舊值」這條正向斷言。
   - **patch 工具在腳本中途中止時，前面的修改已經生效**（r25 新增）：一個腳本依序改好幾個檔，第三處比對不到而中止——
     前兩處已經寫進檔案，後面的都沒改。重跑前要先確認哪些已生效，否則重跑會在已改過的地方比對不到（或重複修改）。
     pump-dump-hunter 對照 r24 時遇到一次（全域檢查那段的原文跟預期不同），逐檔確認後只重跑沒生效的部分。
     **更好的做法：先全部比對、全部命中才一次寫入**（r26 新增，gold-scalper 的 `scripts/patch.py` 的 `apply()`）：
     一批修改（可跨多個檔、同一個檔多處依序套用）先在記憶體裡逐一比對，任何一處對不上就中止、**一個檔都不寫**，
     就不會有「改了一半」要去確認的狀態。工具本身要驗證一次：故意讓第二處比對不到，確認第一處的檔案沒被改動。
     **但只保護得到「同一次 apply」裡的修改**：gold-scalper 對照 r25 時，清單的 apply 中止了（一個檔都沒寫），
     同一個指令後面另外用 `sed` 改的 VERSION 卻照樣生效，差點交出「清單 r25、VERSION r26」的不一致版本。
     → 版本號也放進同一次 apply，或者每一步檢查上一步的結果再往下走。
     另外兩個小坑：拿「從檔案擷取的一段原文」當比對字串時，替換內容要保留原文結尾的換行，否則下一行會黏上來（py_compile 抓得到）；
     逐條搜尋確認修改時，預期次數要算上同一字串在**其他函式**裡的出現次數，搜尋結果跟預期不同時先懷疑預期值。
     crypto-screener 對照 r26 時照做：`apply()` 附自我驗證（第二處比對不到時第一個檔不被動到），這輪中止兩次
     （一次是預期的上下文錯了、一次是同一段在另一個函式也有），兩次都一個檔都沒寫；清單與版本號放在同一次 apply。
     pump-dump-hunter 對照 r27 時照做（`scripts/patch.py` 的 `apply()`，5 組自我驗證：第二處對不到時第一個檔不動、
     結尾換行不一致時中止）；這輪清單、版本號、HANDOFF 放在同一次 apply。第一次套用就中止了（清單有三處比對字串的縮排
     是照「前面印出來的樣子」抄的，但那次輸出前面還有自己加的縮排），**三個檔都沒寫**、版本號也沒被單獨改掉；
     改成從檔案讀出整行當比對字串後重新套用。
   - **第 19 種在 crypto-screener 的樣子**（r27 新增）：
     - **某一支測試的攔截根本沒裝**：5 支測試檔各自複製了一份案例框架，其中 test_r11 那份沒有攔截錯誤輸出——
       「沒有程式錯誤被吞掉」在它的 23 項上從來沒檢查過。→ 案例框架收成一份共用的（`tests/harness.py`），並用人造案例自我驗證。
     - `verify.sh` 的 pyflakes 沒裝時只印警告、照樣通過；區域變數檢查只印「通過」、看不出掃了幾個函式。
       → 沒裝就失敗；先掃一個已知有錯的檔（金絲雀），必須報出來；印出並檢查掃到的數量。
   - **測試基礎設施本身的測試，不該被突變檢查當成「程式的測試」**（r27 新增）：測「注入綁定」「命中計數」這類模擬交易所本身的項目，
     會自己送被突變的查詢，命中大於 0 又通過，被判成空跑。→ 這類項目執行時關掉突變環境、或事後清掉自己的命中紀錄，
     讓它們自動歸為「無關」；不要為了它們放寬檢查器。
   - **「讀模擬交易所」不等於「測基礎設施」**（r28 新增，pump-dump-hunter 對照 r27 時）：43 項人工豁免逐項看，
     條件有讀模擬交易所狀態的不少（例如「別人的 40 留著」），但那個狀態是**程式送的單造成的**，測的是程式的行為。
     真正只確認測試環境本身的只有 1 項。做法：斷言加 `infra=True` 標記，框架印〔命中0〕自動歸為無關；
     靜態檢查確認 infra 項的條件只讀模擬交易所、不碰程式，防止拿這個標記繞過檢查。
   - **收成共用框架時，別用 `import *`**（r28 新增，第 19 種）：pump-dump-hunter 把五支測試收成 `tests/harness.py` 時
     先用了 `from tests.harness import *`——pyflakes 在這五個檔上**完全無法偵測未定義名稱**（它自己會說 unable to detect）。
     靜態檢查靜靜失效。改成明列名稱後，pyflakes 馬上抓到一個潛伏的錯：某支測試用舊框架的 `ORIG["record_close"]`，
     新框架的鍵格式不同，那行執行時一定 `KeyError`，只是那條分支沒被走到。驗證腳本也把「unable to detect」明確算成失敗。
   - **共用框架收起來之後，漏掉的那份才會現形**：pump-dump-hunter 的 test_r12（50 多項）結尾**根本沒有**
     「沒有程式錯誤被吞掉」的全域檢查（同 crypto-screener 的 test_r11）；收進框架、由 `finish()` 統一做之後才補上。
     遷移前後逐支比對斷言數，確認只多了這 2 項全域檢查、其他一項沒少。
   - **框架與檢查器的自我驗證，第一次跑就抓到它們自己的錯**（r28 新增）：框架的全域前提寫成「斷言數 > 1」，
     真的測試都有幾十項所以一直通過，只有一項斷言的人造案例才露出來（應為 ≥ 1）。突變檢查器加了「至少解析到 5 項」的新規則後，
     它自己的 3 組人造資料全部被判「解析太少」——r17 說的「新規則讓舊測試走不到」，發生在檢查器自己身上；門檻改成參數，
     自我驗證另外補一組驗證這條新規則。
   - **程式變穩之後，舊測試的前提可能不成立了**（r28 新增，r17 的另一種樣子）：一項測試靠「餵 `exit="n/a"` 讓通知格式化出錯」
     來驗證通知出錯被隔離；程式改成先檢查型別後，壞資料不再讓格式化出錯，這項就測不到它要測的東西了。
     改成讓通知**送出**時失敗，並補前提「通知真的失敗了」。
     gold-scalper 對照 r28 時逐項看過：靠壞資料觸發例外的只有「待撤清單有一筆欄位缺漏」，補了前提「那一筆真的走到例外處理」
     （不是走一般的撤單失敗）；其他都是直接讓被 mock 的函式丟例外，不受程式變穩影響。
   - **測錯方式第 20 種**（r29 新增，gold-scalper 對照 r28 時遇到）：
     20. **共用框架的 setUp 把被測的東西 mock 掉了**：「平倉通知不能用模擬點數估 USDT」這項放進了交易流程的共用框架，
         而那個框架在 setUp 裡把整個 `notify_trade_event` 換成收集器——通知的格式化根本沒執行。
         斷言「通知裡沒有 -0.20 USDT」會空跑通過；是前提「通知真的組出來、送出了」失敗才發現。
         → 測格式化、測某個函式內部行為的項目，放在不 mock 那個函式的情境；前提要檢查「那個函式真的執行了」。
   - **錯誤掃描只攔了一個模組的 logger**（r29 新增，第 19 種在 gold-scalper 的樣子）：「沒有程式錯誤被吞掉」的全域檢查
     只攔引擎的 logger，網頁入口、下單層寫的錯誤從來沒被掃過。改成攔所有 logger（`logging.Logger.error`），
     前提是「至少攔到兩個不同模組的錯誤」。gold-scalper 的完整驗證收成 `scripts/verify.py`，第一次跑就報出一項靜態檢查失敗
     （上面那個回傳原因檢查缺前提）。
   - **彼此獨立的檢查不要塞在同一個情境裡**：命中次數是「這個情境到目前為止」，放在移損情境後面的兩個「沒有待辦時回傳原因」
     繼承了移損的命中次數，被突變檢查判成空跑。它們本來就不查部位，移到自己的情境就自動歸為無關。
   - **完整驗證收成一個指令**：pump-dump-hunter 的 `scripts/verify.py` 依序跑 pyflakes（沒裝就失敗、先掃金絲雀）、
     所有行為測試（每支要印「全部通過」且斷言數達標）、框架／靜態檢查／回傳原因／改寫工具／突變檢查器各自的自我驗證、逐項突變檢查。
     第一次跑就如實報出三件事（未修的程式、上面那個 import *、一個計數方式不適用的測試），不會因為某一步沒執行而印「通過」。
   - **crypto-screener 對照 r29 時的幾點**（r30 新增）：
     - **模擬交易所要讓「成交價」和「標記價」不一樣**：市價單成交價加一點滑價、部位均價＝成交價。兩者一樣時，
       測試分不出程式用的是哪一個——「出場價用成交價」這條斷言會空跑通過。
     - **程式變穩之後失效的舊測試**（r28）：r23 有兩項靠「`wantStop` 是字串 → 收尾通知格式化時拋錯」來測「結帳之後出錯」，
       r24 起通知先檢查型別、不再拋錯，兩項照樣通過但測不到它描述的那條路。改成讓結帳之後的一步（每日統計）確實失敗，
       並補前提「那一步真的出錯了」。其他靠壞資料觸發例外的 5 項逐一確認仍會觸發、且都有斷言證明例外發生。
     - **沒有用 logging 的專案**：錯誤是各模組直接寫 `sys.stderr`，攔截點是框架裡的 `sys.stderr`，本來就涵蓋所有模組；
       前提照 r29 做——在框架底下讓 trader 與 main 各走一次真的出錯路徑，確認兩個都攔得到（`harness.selftest_modules()`）。
     - **基礎設施標記**（r28）：`case(..., infra=True)`，框架執行後清掉它的突變命中；靜態檢查確認 infra 項在 `fresh()` 之後
       不碰 `T.`、`M.`（附人造資料自我驗證）。取代了 r27 用的「關環境變數、手動清命中」兩個權宜寫法。
   - **pump-dump-hunter 對照 r30 時的幾點**（r31 新增）：
     - **模擬交易所讓成交價 ≠ 標記價之後，一項舊測試「通過的原因」露出來了**：r27 的「成交明細查不到時講明損益未知」一直通過，
       是因為舊的模擬交易所平倉損益剛好 0，而程式用 `realizedPnl ≠ 0` 篩平倉成交把它濾掉了——**兩個錯湊成對的結果**。
       改完模擬交易所後這項失敗，才發現它根本沒讓成交明細查不到。→ 模擬環境的預設值不要是退化值（0、成交價＝標記價、手續費 0），
       退化值會讓程式的錯剛好被抵銷。這次同時加了滑價、加權均價、開倉成交、手續費、遞增成交 id。
     - **測試資料要自洽**：一項「打平出場」測試把模擬交易所的均價改成 1.0，程式帳上記的成交價卻是 1.001；
       程式濾掉那筆成交後退到估算，拿 1.001 算出一個負數，碰巧滿足「損益 < 0」。改成開倉時就不加滑價，
       並直接比對成交明細算出的值（-0.04，估算會是 0.0）。
   - **測錯方式第 21 種**（r31 新增）：21. **在一堆輸出裡找關鍵字，被別的輸出滿足**。「平倉通知講明損益未知」寫成
     「任何一則通知含『損益未知』」——這輪新增的「數量減少」通知也寫「這段損益未知」，平倉通知沒講明也照樣通過。
     是逐項突變檢查報它空跑才發現。→ 斷言指定是哪一則（開頭符號、類型），不在全部輸出裡找字。
   - **錯誤掃描的前提放進每支測試**：r29 說前提是「至少攔到兩個不同模組的錯誤」。pump-dump-hunter 的做法是框架的 `finish()`
     每次都先讓 manager、main、presets 各走一次真的出錯路徑，確認攔得到才做全域掃描；掃描範圍加上 `sys.stderr`（traceback 都在那）。
     找到三處只 `print` 的錯誤：套用實盤參數失敗、Telegram 送出失敗、網頁請求出錯，錯誤區與推播都看不到，已補上。
   - **`apply()` 這輪擋下一次**：對帳那段在 r22 被整段縮排包進 try，照舊縮排寫的比對字串對不上，六處修改一處都沒寫。
     改成用一個小函式從檔案讀出連續幾行原文當比對字串（`exact()`），不再手抄縮排。
   - **gold-scalper 對照 r31 時的幾點**（r32 新增）：
     - **第 21 種有 19 處**：幾乎所有告警斷言都是「任何一則告警含某字」。改成只看第一行（標題）、恰好一則（`one(alerts, 標題)`）。
       改寫後在修改前的程式上逐項重跑：18 處照樣通過（程式本來就發了對的那一則），**1 處失敗**——「搬移停損時舊單撤不掉」
       一直被「別的告警也含『第 1 次』和 -1001」湊巧滿足，它真正該看的是「多餘的停損單撤不掉」那一則。
     - **測錯方式第 22 種**（r32 新增）：22. **測試依賴共用框架看不見的預設值**。共用框架加了「成交明細預設查不到」之後，
       「數量減少時損益記未知」這項照樣通過——但它得到「未知」是因為框架的預設，不是測試自己的設定。框架預設哪天改成查得到，
       它就會莫名失敗（或更糟：換個斷言寫法就空跑）。→ 測試要用到的條件都在測試裡明寫，並用第 16 種檢查「未知」的原因。
       找法：改框架預設值時，列出所有走進那條路的舊測試（這次是用函式包裝記錄每項測試的呼叫結果），逐項看它依不依賴那個值。
     - **stderr 攔截也要金絲雀**：錯誤掃描加攔 `sys.stderr` 之後，來源清單裡沒有 stderr——分不出是真的沒 traceback 還是沒攔到。
       金絲雀是故意讓一條背景執行緒丟例外。**金絲雀要用另一個緩衝區**：traceback 的 `Exception in thread`、`Traceback` 兩行
       不含注入標記（標記在最後一行的訊息裡），跟測試期間的輸出混在一起就被當成程式錯誤。
     - `apply()` 的「結尾換行不一致就中止」會擋下**整段刪除**（替換字串是空的），這輪擋下一次、一個檔都沒寫；改成替換字串為空時不檢查。
   - **crypto-screener 對照 r32 時的幾點**（r33 新增）：
     - **第 22 種最常見的樣子：「把部位歸零」模擬觸發，不會留下成交紀錄**。這種測試走到結帳時，出場價因為「查不到成交」而是未知——
       不是測試自己設定的原因。照 r32 的找法，讓模擬交易所記錄每一項測試查成交明細的結果：15 項走進這條路，
       8 項是歸零沒留成交；逐項看，6 項的斷言跟損益無關，**2 項依賴它**——「缺 R 時 R 倍數記為未知」其實是出場價未知造成的、
       「壞資料讓損益變未知」其實壞資料根本沒走到。改成用 `trigger()` 留下真的成交，前提證明未知的原因（第 16 種）。
     - **第 21 種 30 處，0 處湊巧**：全部改成指定標題（`titled(告警, 完整標題)`）後在修改前的程式上逐項重跑，30 處照樣通過——
       程式本來就發了對的那一則。gold-scalper 是 19 處裡 1 處湊巧，兩邊的結果都值得記：沒抓到不代表不用改。
     - **模擬環境去掉退化值**（r31）：成交帶遞增的成交 id、非零手續費、平倉那筆的 `realizedPnl`（打平出場時剛好 0）；
       加碼時部位均價改成加權均價（原本直接蓋成最後一筆）；可以讓所有成交落在同一毫秒，專門測界線。
     - **`apply()` 的整段刪除**：r30 我加的換行檢查也會擋下整段刪除（同 gold-scalper），改成替換字串為空時不檢查，補自我驗證；
       另加 `exact(檔, 起始行, 行數)` 從檔案讀出原文當比對字串。這輪的清單修改就是用它取錨點。
     - **stderr 金絲雀**：背景執行緒丟例外、用另一個緩衝區接，確認 traceback 真的寫進被攔截的 `sys.stderr`。
   - **pump-dump-hunter 對照 r33 時的幾點**（r34 新增）：
     - **第 22 種的稽核結果**：讓模擬交易所記下「每次查成交明細時，這個幣的部位有沒有被直接改過數量」（`untraced()`），
       每項斷言印出來。3 個情境走進這條路，逐項看：4 項斷言跟損益無關（平倉數量、收尾通知、計數歸零、帳上數量），
       **2 項依賴它**——「成交價是 None 時算部分出場損益不拋」其實根本沒進計算（沒成交就直接記未知）；
       「部分出場損益未知 → 記成 None」描述的原因是成交價未知，實際的未知完全來自沒留成交。
       改成用 `fx.trigger()` 讓交易所真的成交，未知的原因由測試自己明寫（讓成交明細查不到），前提證明原因。
     - **第 21 種 15 處，0 處湊巧**：改成 `one(標題, 必須含的字)`——開頭是這個標題的推播恰好一則。比對方法：改寫前、改寫後兩版，
       分別在「這輪修改前的程式」（修正都已在裡面，對的那則確實有發）與「各測試原本修正之前的舊程式」上跑。
       前者 15 處改寫前後都通過，後者都失敗。否定句（「沒有任何一則寫 X」）本來就該看全部推播，6 處不改。
       r31 那輪已抓到並修掉 1 處湊巧，所以這輪是 0。
     - **在舊版程式上重跑，會把共用框架的第 14 種翻出來**：`test_r12` 在 r10 程式上中途崩掉——r10 的 Algo 退回是永久旗標 `_algo_ok`，
       原本 `test_r12` 自己的框架有重設它，收進共用框架時漏了。只有在舊程式上跑才會發作。框架改成遇到舊程式時略過不存在的東西、
       有舊旗標就重設。
     - **stderr 金絲雀用另一個緩衝區、含背景執行緒**：框架的 `selftest_modules()` 讓 manager、main、presets 各走一次出錯路徑、
       再讓一條背景執行緒丟例外，全部寫進一個獨立的緩衝區檢查；每支測試的 `finish()` 前提改成「至少兩個模組、而且攔得到執行緒」。
       自我驗證另外確認金絲雀的 traceback 沒有混進正式掃描。
     - **突變下測試本身崩掉，靠「至少解析到 N 項」才發現**：突變讓開倉單沒送出，測試接著呼叫 `fx.trigger()` 去平一個不存在的部位，
       測試直接拋錯、後面的項目全部沒跑。正常情況不會發生。→ 前提之後依賴那個前提的動作包進 `run()`，讓測試跑完、如實報出失敗。
     - `apply()` 的換行檢查這輪先改成「替換字串為空時不檢查」並補自我驗證（這輪沒遇到，照 r32 預先修）。
       另外一次是我寫的套用腳本本身有 f-string 語法錯誤，腳本在 `apply()` 之前就停了——一個檔都沒寫。
   - **gold-scalper 對照 r34 時的幾點**（r35 新增）：
     - **突變下測試本身崩掉，在 unittest 裡的樣子**：unittest 每項各自獨立，一項崩掉不會讓後面的項目不跑（跟 pump-dump-hunter 的腳本式測試不同），
       但**那一項後面的斷言全部沒檢查**，失敗訊息只剩一個 `TypeError`。找到 1 項：直接讀 `mock.call_args[0][1]`，突變讓那個呼叫沒發生，
       `call_args` 是 None。再掃「先索引、沒先確認有東西」的寫法，共 7 項測試，全部補一條前提（「真的掛了停損」「結帳了、通知有送出」）；
       突變檢查器改成自動報出「測試程式本身拋錯」的項目（看 traceback 最後一層是不是在測試檔）。
     - **在舊版程式上重跑**：框架裡重設模組層級狀態的地方改成「有才重設」（`hasattr`），在舊版程式上不會因為那個東西還不存在就崩。
       拿修正後的測試跑修改前的程式，結果要分成「斷言失敗」「被測程式拋錯」「框架／測試崩掉」三類，這輪框架崩掉 0 項。
     - **只有否定句的新測試，補了正向前提之後才變成真的測試**：「純模擬引擎不去查交易所」在修改前的程式上本來是通過的
       （舊程式根本不確認，「沒查」自然成立）；補上前提「真的先判斷了是不是實盤引擎」之後，它在修改前的程式上會失敗。
     - **`apply()` 寫入前先編譯**：兩輪都遇到 `apply()` 全部命中、寫進去之後 `py_compile` 才抓到語法錯（兩行黏在一起；
       把 f-string 包成條件式後跟下一段隱式串接少了 `+`）。改成寫入前把每個 `.py` 在記憶體裡 `compile()`，語法錯就一個檔都不寫，附自我驗證。
   - **crypto-screener 對照 r35 時的幾點**（r36 新增）：
     - **先索引、沒先確認有東西：15 處**。寫了一個靜態檢查（`tests/check_indexing.py`，6 組人造資料自我驗證）：對清單取 `[0]`／`[-1]` 之前，
       同一個案例裡要先有 `need`／`if not`／`len(...)` 提到那個清單；排除不會空的寫法（迴圈變數取值、帶非空預設值、常數清單、同一行短路保護）。
       自動在每一處前面插一行 `need(清單, …)`。**插入腳本自己犯了這個錯**：巢狀取值（`ex.fills[-1]["price"]`）抓錯一層，
       插成 `need(ex.fills[-1], …)`——前提那一行自己就會崩。檢查器下一輪就抓到了。這一輪我新寫的測試也有兩處把索引寫在前提條件裡。
       → **前提本身也要先確認有東西**（`need(x and x[-1]["a"] …)`、`need(len(x) >= 2 and …)`）。
     - **框架把失敗分成四類**：前提不成立／被測程式拋錯／測試本身崩掉（traceback 最後一層在測試檔）／斷言失敗；
       突變檢查器把「突變下測試本身崩掉」單獨報出來。兩處都附人造資料自我驗證。這輪突變下崩掉 0 項。
     - **在舊版程式上重跑**：修正後的全部測試跑 r33 版程式，失敗 4 項全部是斷言失敗，框架崩掉 0 項。
       限制：手上只有 r33 這一個舊版，更早的版本已被後面的輸出覆蓋——**舊版程式要留著**，才能做這種比對。
     - **只有前提、看不到行為的測試**：「界線在開倉當下記」原本把「有沒有記 id」寫成前提，修改前的程式只顯示「前提不成立」，
       看不到行為上的錯（出場價被篩成未知）。改成先斷言出場價、最後才斷言有沒有記 id。前提是用來排除「沒走到」，不是用來放要測的東西。
     - `apply()` 寫入前先 `compile()`（同 gold-scalper），附自我驗證。這輪中止兩次都是比對次數不對（開倉與認領有 3 行一模一樣、
       插入前提的錨點在同一檔出現兩次），兩次都一個檔都沒寫；插入腳本改成錨點往前加行、直到在檔案裡唯一為止。
   - **pump-dump-hunter 對照 r36 時的幾點**（r37 新增）：
     - **先索引、沒先確認有東西**：照 crypto-screener 的規則做了 `tests/check_indexing.py`（含前提本身，7 組人造資料自我驗證）。
       掃出 15 處，10 處是誤報（`one()` 固定回傳兩個元素、給剛建好的部位**賦值**），4 處是真的（先取 `fx.trades[-1]`、`bars[-1]`），
       已補確認。規則要排除固定長度的回傳值與賦值目標，否則誤報多到沒人看。
     - **突變檢查器報「測試本身崩掉」，第一版誤報了全部 9 支**：框架的金絲雀故意讓背景執行緒丟例外，traceback 最後一層在
       `tests/harness.py`；測試在突變下有項目失敗、結束碼非 0，兩者湊起來就被判成崩掉。→ 真的崩掉 = **沒印出最後的總結**。
     - **在舊版程式上重跑，框架第一次全部崩掉**：拿現在的測試跑手上最舊的 r10，9 支都崩在框架——`finish()` 的金絲雀用到 r19 才有的函式。
       r35 說「框架重設狀態的地方有才重設」，**金絲雀也一樣**。修完後 r10 上 9 支框架崩掉 0，全部是斷言失敗或被測程式拋錯。
       工具做成 `tests/rerun_old.py <舊版目錄>`，分三類報告；完整驗證可以用 `PDH_PREV` 指定上一版一起跑。
     - **舊版程式要留著**：這輪用來比對的舊版都是對話裡的暫存目錄，下一次對話就不在了。→ 每次部署前在 git 打標籤
       （例如 `lessons-r37`），要比對時用 `git worktree add ../pdh-r37 lessons-r37` 取出。
     - **測錯方式第 23 種**（r37 新增）：23. **兩邊都可能是 None 的相等比較**。「重啟後界線還在」寫成 `m1 == m0`、
       「出場價含每一筆成交」寫成 `exit == 預期值`——突變下開倉沒成功，兩邊都是 None，相等成立。這其實是藏起來的否定句，
       靜態檢查的否定句規則（`not`、`== 0`、`is None`）抓不到，是逐項突變抓到的。→ 相等比較先要求預期值不是 None。
     - **測錯方式第 24 種**（r37 新增）：24. **測試借用被測程式的輔助函式下判斷**。一項斷言用程式裡的 `manager.num()` 判斷
       「是不是數字」：舊版沒有這個函式就崩掉；就算有，`num()` 本身錯了，測試也會跟著錯、照樣通過。→ 判斷自己寫。
     - **「只有前提、看不到行為」2 處**：「時間戳缺值 → 認領回來」「開倉當下記下起始界線」是要測的行為本身，卻寫成前提。改成一般斷言。
       另有一條前提寫成常數 `True`（「狀態檔真的壞了」），等於沒有前提，改成重新載入前確認那個檔真的解析不了。
     - **`apply()` 寫入前編譯，這輪擋下一次**：把註解寫進 `check(` 同一行，後面的引數全被註解掉；兩個檔都沒寫。
       另一次是我自己寫的突變檢查器自我驗證「先用後定義」——pyflakes 不報模組層級的先用後定義，是完整驗證裡「每支都要印出總結」擋下的。
   - **gold-scalper 對照 r37 時的幾點**（r38 新增）：
     - **先索引、沒先確認：做成正式靜態檢查**（`tests/check_indexing.py`，9 組人造資料自我驗證，含「前提本身就索引」）。
       掃出 10 處：4 處是真的（其中 1 處是**前提那一行自己就索引**），6 處誤報。照 pump-dump-hunter「規則要排除固定長度」，
       這邊的誤報是另外兩種樣子：**同一行的短路保護**（`x and x[-1]`）、**從函式呼叫算出來的值**（`ast.parse(原文).body[0]`，
       不是測試累積的清單）；固定長度的回傳值改成在賦值那行標 `# 固定長度`，看得到、也審得到。
     - **新測試直接呼叫這輪才加的方法，在舊版上會被分成「測試崩掉」**：原因是對的（功能不存在），但跟真正的框架問題混在一起。
       → 呼叫新方法前加前提「程式有這個方法」（`hasattr`），在舊版上變成斷言失敗。這輪修改前的程式上框架／測試崩掉 0 項。
     - **第 14 種又來一次：新的引擎狀態沒加進共用框架的重設**：「讀不到持倉紀錄」的測試把共用引擎的「已載入」設成否，
       沒還原，後面兩項直接開倉的舊測試被新規則「沒載入不開倉」擋下——是前提「單真的送出去」失敗才發現。
       → **每新增一個引擎狀態，同時加進取得共用引擎時的重設**。
     - **程式判斷的順序變了，舊前提就不成立**：「界線不是數字」原本的前提是「真的查了成交明細」，改成在查詢之前就判定之後，
       這個前提不再成立。改成檢查判定的原因（第 16 種），不檢查「有沒有查」。
     - **第 23、24 種掃描**：相等比較的預期值不是常數的 4 處，都是測試自己剛建的字典或迴圈常數，不可能是 None；
       斷言裡呼叫程式函式的 3 處，呼叫的就是那項測試要測的函式本身。兩種都 0 處要改。
     - **舊版要靠 git 標籤**：gold-scalper 的 zip 裡沒有 `.git`，標籤只能在 GitHub 那邊打。工具做成 `tests/rerun_old.py <舊版目錄>`，
       `GS_PREV=<舊版目錄> python scripts/verify.py` 會一起跑；README 寫了部署前打 `lessons-rNN` 標籤的步驟。
   - **crypto-screener 對照 r38 時的幾點**（r39 新增）：
     - **新的狀態不用另外加進重設**：crypto-screener 的共用框架每個案例都重新載入被測模組（`importlib.reload`），模組層級的狀態全部重建；
       靜態檢查確認每個案例的第一個動作都是 `fresh()`。所以「新增的引擎狀態漏加進重設」這類第 14 種不會發生——代價是每個案例都要重建。
     - **模擬交易所的成交明細不管 `limit`、一律回全部**——又一個退化值（r31）。「只查一頁」的錯永遠測不出來。改成照 `limit` 截斷（預設 500）。
     - **第 23 種 9 處、0 處要改**（全是數量、數字、檔案內容）；**第 24 種 1 處**：一項測試用程式的 `round_step()` 算「應該出一半的數量」，
       改成讀交易所實際收到的那張停利單數量。
     - **索引檢查的排除規則**：照 gold-scalper 加上 `# 固定長度` 標記；但「從函式呼叫算出來的值」**不整類排除**——
       crypto-screener 上一輪抓到的 `工具輸出.strip().splitlines()[-1]` 就是函式呼叫的結果，工具沒輸出時真的是空的。
       兩邊的誤報樣子不同，排除規則要照自己的誤報訂，不能直接照抄。
     - **舊版程式存在專案裡**：zip 裡沒有 `.git`，所以把每輪修改前的後端程式（只有 `.py`，約 300KB）存進 `tests/legacy/rNN/`，
       `tests/run_on_legacy.py rNN` 用現在的測試跑它、分四類報告；完整驗證對 r33、r36 兩版都跑，「測試本身崩掉」要是 0。
       這輪兩版都是 0；呼叫這輪新介面的測試在舊版上都落在「斷言失敗」——因為新測試先用既有介面斷言行為、最後才檢查新介面。
   - **pump-dump-hunter 對照 r39 時的幾點**（r40 新增）：
     - **第 14 種延伸到磁碟上的檔案**：共用框架每個情境重設記憶體裡的狀態，但**狀態檔也是共用的**——前一個情境故意把狀態檔弄壞，
       後面所有情境重新載入時都會停在「還沒載入、不能開倉」。框架的 `fresh()` 同時清掉狀態檔、重設「已載入」與讀取失敗計數。
       照 r38 的做法，新增這些狀態的同一次修改就加進重設，沒有等舊測試失敗才發現。
     - **呼叫新介面前先加 `hasattr` 前提**：`test_r39` 用到這輪新增的「已載入」「重試載入」，在舊版上 16～17 項全部落在「斷言失敗」，
       測試／框架崩掉 0。
     - **模擬交易所的另一個退化值**：真的 `positionRisk`（不帶 symbol）會回所有交易過的幣、數量 0 的列也在；模擬交易所只回有部位的列。
       程式哪裡忘了濾掉 0，測試永遠看不出來。改成跟真的一樣之後全部測試照樣通過——程式每處都有濾，**本來就對**，但現在是真的驗證過。
     - **舊版程式存進專案**（照 crypto-screener）：zip 沒有 `.git`，把上一輪與這輪修改前的後端程式存進 `tests/legacy/r34`、`r37`
       （各約 210KB，只留最近兩版）；`tests/rerun_old.py legacy:rNN` 用現在的專案當底、把 `app/` 換成舊版；完整驗證對每一版都跑，
       「測試／框架崩掉」要是 0。這輪 r34 上 29 項斷言失敗、1 項被測程式拋錯；r37 上 16 項斷言失敗；兩版都崩掉 0。
     - **索引檢查的排除規則**：pump-dump-hunter 的誤報是 `one()`／`run()` 固定兩個元素與賦值目標，只排除這兩種具名的，
       不整類排除函式呼叫的結果（照 crypto-screener 的提醒）。這輪新測試 0 處。
   - **gold-scalper 對照 r40 時的幾點**（r41 新增）：
     - **第 14 種在沒有狀態檔的專案**：狀態都在資料庫、測試時資料庫是關的，所以沒有「前一個情境留在磁碟上」；
       要重設的是模組層級的記憶體狀態（資料庫寫入失敗計數、網頁入口出錯計數、推播錯誤區與「沒設定已記過」旗標、記住的持倉模式），
       收成一個 `_reset_module_state()`，每個情境取共用引擎時呼叫；舊版沒有的東西就略過。
     - **舊版程式存進專案**（照 crypto-screener／pump-dump-hunter）：`tests/legacy/r38/app/`（只存 `.py`），`tests/rerun_old.py legacy:r38`、
       `scripts/verify.py` 自動對 `tests/legacy/` 下每一版都跑。**第一次跑就抓到一項崩掉**：「設定一次套用」直接用了這輪才加的例外類別，
       舊版沒有——r38 才記進清單的「呼叫新介面前先加存在性前提」，這輪自己又漏了一次，靠存在專案裡的舊版才抓到。修完後崩掉 0。
     - **索引檢查的排除規則，上一輪整類排除了函式呼叫的結果**（正是 crypto-screener 提醒的那種），而且有兩處：
       摘要那條規則、以及檢查器最前面一條更早的「被索引的東西本身是呼叫就跳過」。照 crypto-screener 的例子加一組自我驗證
       （`r.stdout.strip().splitlines()[-1]` 必須被報出），第一次跑就失敗、才找到那條更早的規則。改成只具名排除 `ast.parse(原文).body[0]`；
       改完在實際測試上 0 處新增。
     - **全量部位表模擬成跟真的一樣**（所有交易過的幣、數量 0 的列、雙向兩側）：孤兒單檢查照樣通過，**本來就對**，現在是真的驗證過。
     - **改寫工具的兩個新坑**：（1）照「濾掉說明文字與空行後印出來的樣子」抄錨點，`apply()` 中止、一個檔都沒寫（同 pump-dump-hunter r34）；
       （2）新函式插在 `@property` 與 `def is_enabled` 中間，裝飾器就套到新函式上——**語法合法、編譯檢查抓不到**，是行為測試抓到的。
       插入新函式時，錨點要選在裝飾器**之前**。
   - **crypto-screener 對照 r42 時的幾點**（r43 新增）：
     - **附檔版本先核對**：這輪第一次收到的清單其實是 r41（檔頭與章節都沒有第 15 條），卻照著轉達的說明做了一整輪才講。
       → 收到清單先看最上面的版本號與章節，跟轉達的版本不符就**立刻問**，不要先做。
     - **呼叫端 5 處、測試只測 1 處不夠**：第 15 條的「平倉成功 → 撤停損結帳」有 5 個呼叫端；測試至少跑手動平倉、待平倉重試、守衛三條，
       其餘兩條共用同一個平倉函式的結果判斷。
     - **舊版程式改成只留最近兩版**（照 pump-dump-hunter）：`tests/legacy/r36`、`r39`，`verify.sh` 自動跑 `tests/legacy/` 下每一版。
       這輪 r39 上 23 項斷言失敗，測試本身崩掉 0。
     - **改寫工具加上裝飾器檢查**（照 gold-scalper r41）：錨點緊接在 `@裝飾器` 後面、而且替換內容多出 `def`／`class` 時，`apply()` 中止、一個檔都不寫；
       附自我驗證（修改被裝飾函式的內容不會被誤擋）。
     - **全量部位表**：模擬交易所本來就回所有交易過的幣、數量 0 的列、雙向兩側，**本來就對**，加一項 infra 測試鎖住。
   - **pump-dump-hunter 對照 r43 時的幾點**（r44 新增）：
     - **框架的重設又漏了：改成自動比對，不靠記得**。這輪新增了參數集檔的讀取失敗計數、Telegram 的「沒設定已記過」旗標，
       兩個都漏加進 `fresh()`——前者讓框架的金絲雀在後面的情境崩掉才發現。r38 才寫進清單的規則，照樣漏了兩次。
       改成框架匯入時把每個模組層級狀態的初始值存一份、`fresh()` 一律還原；靜態檢查掃 `app/` 裡所有底線開頭的模組層級 dict／list／set，
       不在框架清單裡（或沒寫理由豁免）就失敗。第一次跑找出 5 個沒重設的（包含上面兩個，另外 3 個一直都沒重設，只是沒測試踩到）。
       靜態檢查本身也驗證過：從清單拿掉一個，確實會報出來。
     - **測錯方式第 25 種**（r44 新增）：25. **`all(...)` 對空清單成立**。「卡住的平倉單都撤掉了」寫成 `all(狀態 != NEW for 每張平倉單)`、
       「所有市價單都帶 RESULT」寫成 `all(…for 每張市價單)`——突變下根本沒送單，清單是空的，`all()` 是 True。跟第 23 種同一類，
       是藏起來的否定句。逐項突變抓到 4 處；靜態檢查的否定句規則加上 `all(...)`（先要求清單非空才算正向）。
     - **舊版程式上重跑，這輪又抓到一項崩掉**：測試「讀取失敗期間把原檔搬走」直接 `os.remove(原檔)`——r37 讀取失敗時自己就把原檔改名了，
       原檔不在，測試拋錯。前提之後依賴它的動作，在舊版上也要能跑完（r35）。
     - **兩支用簡化模擬 `binance` 模組的舊測試**（`test_lessons`、`test_parity`）遇到新函式 `confirm_fill` 時**明確失敗**，不是空跑——
       r17「新規則讓舊測試走不到」的正確樣子。補上它們的模擬。
     - **兩條「要測的行為被寫成前提」**又出現：「稍後才成交」的前提寫成「之後真的成交了」（要靠程式去查單才會發生）；
       平倉的前提假設只送一張單（程式本來就會用剩餘數量再送一次）。都是修改前的程式跑出來才發現。
     - **改寫工具加上裝飾器檢查**（照 gold-scalper r41、crypto-screener r43），自我驗證含「修改被裝飾函式的內容不誤擋」。
       這輪 `apply()` 還擋下一次自己的錨點重複（`import sys` 在檔頭與檔尾各一次），一個檔都沒寫。
     - **gold-scalper 那點（緊急修正後平倉路徑是否也改用 executedQty）要在 gold-scalper 自己對照**：這輪只收到 pump-dump-hunter。
   - **gold-scalper 對照 r44 時的幾點**（r45 新增）：
     - **框架的重設改成自動**（照 pump-dump-hunter）：匯入時把 7 個模組所有底線開頭的模組層級 dict／list／set 存一份初始值，每個情境一律還原；
       另加一項測試：往每一個都塞一個探針值、重設後必須全部消失。物件屬性與純量另列在 `_reset_named_state()`。
     - **第 20 種又兩次**：新測試放進交易流程的共用框架，框架把 `close_paper_trade` 整個 mock 掉，「結帳不覆寫成交價」根本沒執行到——
       前提「讀得到這筆」失敗才發現。資料庫、通知格式化這類要測內部行為的，一律放在不 mock 它的情境。
     - **sqlite 當測試資料庫時，時間欄位要回 datetime**：PostgreSQL 回 datetime、sqlite 回字串，程式呼叫 `.isoformat()` 就失敗、整筆讀不出來。
       模擬環境不能比真的寬鬆，也不能比真的嚴格（r31 退化值的另一面）。
     - **舊版上崩掉：新測試直接讀字典的新鍵**（`s["estimated"]`，舊版的統計沒有這個鍵）。索引檢查只看 `[0]`／`[-1]`，字典鍵整類納入會誤報太多，
       所以沒有加進靜態檢查；做法是讀新介面的結果一律 `.get()`，並先加存在性前提。
     - **第 25 種**：靜態檢查把 `assertTrue(all(...))` 算成否定句，附兩組自我驗證（先確認清單非空的就算正向）。現有測試 0 處。
     - **改寫工具加上裝飾器檢查**（r41 gold-scalper 踩到、crypto-screener／pump-dump-hunter 先做了，這邊補回來），自我驗證 7 項。
       這輪 `apply()` 又中止兩次、一個檔都沒寫：一次是關鍵字引數名稱記錯（`qty=` 其實是 `quantity=`），一次是錨點在兩個函式裡各出現一次。
   - **crypto-screener 對照 r46 時的幾點**（r47 新增）：
     - **框架的重設**：每個情境都重新載入模組（`importlib.reload`），模組層級狀態本來就全部還原；照 pump-dump-hunter／gold-scalper 加一項探針測試鎖住
       （往 trader、main 所有底線開頭的模組層級 dict／list／set 塞探針，重設後必須全部消失）。**沒有重新載入的模組**（自檢模組登記的檢查清單）要另外處理——
       main 重新載入時先移除舊的登記再加，不會重複。
     - **第 25 種**：靜態檢查的「否定句」本來就把 `if any(...)` 算進去，但 `if not all(...)` 被當成正向——補上，附兩組自我驗證。現有測試 0 處。
     - **埋在 `main()` 裡的邏輯測不到**：切換網路時的帳本檢查只在啟動時跑，抽成函式後才寫得出測試；抽的時候另加一項「原本的行為不能掉」。
     - **前提寫成「只送兩張平倉單」**：r43 的部分成交測試前提是「平倉成交正好兩筆」，程式本來就可能多送一次；改成「至少兩筆」（同 pump-dump-hunter r44 那點）。
     - **舊版程式**：`tests/legacy/` 改留 r39、r43。r43 上 4 項斷言失敗，r39 上 29 項，測試本身崩掉都是 0。
   - **pump-dump-hunter 對照 r47 時的幾點**（r48 新增）：
     - **這輪沒有附程式**：上傳資料夾裡有一個 `pump-dump-hunter.zip`，檢查版本號是上一輪的 r40（比清單早兩小時上傳），不是現在的程式。
       以上一輪交出去的 r44 為準對照，並在回覆裡講明。→ 資料夾裡找到的檔案**先看版本**，不能假設是這次附的。
     - **鎖第一次只加在呼叫端**：先加在背景迴圈的 `tick()` 那一層，測試直接呼叫對帳、出場管理時照樣交錯——跟 r39「開倉函式本身也要擋」同一個道理。
       改成裝飾器套在函式本身。
     - **框架重設的探針測試**（照 gold-scalper r45）：往 14 個模組層級狀態各塞一個探針值，`fresh()` 之後必須全部消失；
       也驗證過拿掉還原那一行時確實會失敗。靜態檢查只能確認「有列在清單裡」，探針測試才能確認「真的還原了」。
     - **靜態檢查認不出 f-string 描述的前提**：`check("W3", f"（前提）{why}：…", …)` 的描述不是純字串，檢查器當成空字串，
       「前提」兩個字認不出來——從 r21 做這個檢查起就有這個洞，只是之前沒有 f-string 的前提。改成取 f-string 的固定部分，金絲雀加一組驗證。
     - **這輪測試自己的錯**：W3 那組第一個情境（沒帶表單）就把實盤覆蓋清掉了，後面每個情境一開始覆蓋就已經是空的，
       「沒被清掉」全部被拖累失敗、`act=reset` 那項則空跑通過；改成每個情境各自設好覆蓋，並補前提。
     - **舊版程式**：`tests/legacy/` 改留 r40、r44。r44 上 10 項斷言失敗、r40 上 36 項，測試本身崩掉都是 0。
   - **gold-scalper 對照 r48 時的幾點**（r49 新增）：
     - **f-string 寫的前提描述**：gold-scalper 的靜態檢查看的是斷言函式與引數的結構（有沒有正向斷言、索引前有沒有確認），**不讀描述文字**，所以不適用。
     - **框架重設涵蓋的模組改成自動列舉**：原本列了 7 個模組，改成 `app/` 底下所有模組（`pkgutil`），探針測試同樣掃全部——沒列進清單的模組就不會被還原。
     - **真的打網頁端點要有 HTTP 客戶端**：FastAPI 的測試客戶端需要 `httpx`，沙盒沒裝時那項測試是 `ERROR`、沒有真的跑。
       改成列在 `requirements-dev.txt`（只給測試用、部署不需要）。
     - **舊版程式**：`tests/legacy/` 改留 r42、r46。r46 上 3 項斷言失敗、r42 上 13 項，測試本身崩掉都是 0。
   - **crypto-screener 對照 r49 時的幾點**（r50 新增）：
     - **f-string 寫的前提描述**：靜態檢查看的是 `need(...)` 呼叫本身、不讀描述文字（r22 起「看斷言本身，不看描述」），**不適用**。
     - **交錯測試要驗證「真的是鎖擋下的」**：把裝飾器裡拿鎖那段換成直接呼叫，三項交錯測試都失敗；「函式都套了鎖」那項照樣通過（它只看標記）——
       只看「有沒有套」的測試證明不了鎖有效，要兩種都有。
     - **框架重設涵蓋的模組改成自動列舉**（照 gold-scalper r49）：`backend/` 底下每個模組自動列出；每個情境重新載入的（trader、main）不用管，
       其他模組不能有模組層級的可變狀態，常數表與寫了理由的豁免除外。
     - **舊版程式**：`tests/legacy/` 改留 r43、r47。r47 上 8 項斷言失敗、r43 上 12 項，測試本身崩掉都是 0。
   - **pump-dump-hunter 對照 r50 時的幾點**（r51 新增）：
     - **交錯測試的對照組寫進測試裡**（照 crypto-screener r50）：用一個什麼都不擋的假鎖換掉引擎鎖，同樣的情境必須出事——兩張進場單、同一筆結兩次帳；
       有鎖時不出事。兩組並列，才證明那幾項是鎖擋下的。「三個函式都套了鎖」（看 `__wrapped__`）另外一項，只看標記證明不了鎖有效。
     - **「呼叫新介面前先加存在性前提」又漏一次**：對照組直接讀 `manager.ENGINE_LOCK`，在舊版 r44 上崩掉（r44 還沒有引擎鎖）。
       r38 寫進清單的規則，這是第三次靠存在專案裡的舊版才抓到——舊版存進專案（r39）這件事本身一直在發揮作用。
     - **前提要在動作之前檢查**：「同一檔有一筆 pending」寫在呼叫 `place()` 之後，那時 pending 已經被處理掉了，前提失敗。
     - **框架重設涵蓋的模組**：靜態檢查一直是掃 `app/*.py` 全部模組（r44），新模組自動納入，**本來就對**。
     - **舊版程式**：`tests/legacy/` 改留 r44、r48。r48 上 6 項斷言失敗、r44 上 18 項，測試本身崩掉都是 0。
     - **gold-scalper「估算」那幾筆有沒有算進斷路器**：這輪只收到 pump-dump-hunter，要在 gold-scalper 自己對照。
   - **gold-scalper 對照 r51 時的幾點**（r52 新增）：
     - **對照組停錯地方就證明不了鎖**：第一版讓第一條執行緒停在「查交易所」，換上假鎖後照樣只送一張——
       因為開倉函式在查交易所**之前**就把部位記上了，第二條一進來「拿到鎖後的再檢查」自己就擋下，有沒有鎖都一樣。
       鎖真正保護的是「再檢查」與「記上部位」之間那段 I/O（這裡是寫資料庫）：讓第一條停在那裡，有鎖送一張、假鎖送兩張，兩組才有差別。
       → 寫對照組前先找出「檢查」到「記帳」之間有哪些會卡住的步驟，停在那裡；對照組沒出事時，先懷疑停的位置，不是先改預期值。
     - **對照組的模擬環境也要完整**：手動平倉的對照組第一次沒送單，是因為它去讀最近成交拿價格、測試環境沒有成交資料，
       在「拿不到最新價格」就回了——跟鎖無關。前提只寫「背景停在查交易所」不夠，還要確認另一條真的走到了送單那一步。
     - **呼叫新介面的測試在舊版上**：這輪新測試都先加了「程式有引擎鎖」的前提，舊版 r46、r49 上框架／測試崩掉都是 0。
     - **舊版程式**：`tests/legacy/` 改留 r46、r49。r49 上 5 項斷言失敗、r46 上 10 項，測試本身崩掉都是 0。
   - **crypto-screener 對照 r52 時的幾點**（r53 新增）：
     - **對照組寫進測試**（照 pump-dump-hunter r51、gold-scalper r52）：三個交錯情境各跑兩次，真的引擎鎖、以及什麼都不擋的假鎖；
       假鎖那組必須出事（兩次結帳、兩張進場單、忙碌時照樣送單），並確認另一條執行緒真的走到了查交易所／送單。r50 只手動拿掉鎖跑過一次、沒寫進測試。
     - **「檢查」到「記帳」之間的 I/O 可能只在某個分支才有**：手動平倉成交後到記帳之間，平常沒有任何 I/O（回應帶均價、直接記帳），
       只有「回應沒均價、用單號查成交明細」那個分支才有——對照組要用模擬交易所拿掉均價，才停得到那裡。
       開倉則是「同檔檢查後、寫 pending 前」的查帳戶，r50 的測試本來就停在那裡，**本來就對**。
       r50 的「對帳停在查交易所時手動平倉不能送單」停在檢查**之前**，驗的是互斥本身、不是出事，所以另外加上面那組。
     - **舊測試直接拿不在帳上的部位呼叫函式**：移損加了「確認是帳上那一筆」之後，一致性測試與情境測試共 5 處改成先把部位放進帳本——
       那些測試原本就是在「帳上沒有這筆」的狀態下測移損，只是以前沒人檢查。
     - **舊版程式**：`tests/legacy/` 改留 r47、r50。r50 上 1 項斷言失敗、r47 上 11 項（另 6 項前提不成立：r47 還沒有引擎鎖），測試本身崩掉都是 0。
   - **pump-dump-hunter 對照 r53 時的幾點**（r54 新增）：
     - **兩道防線疊在一起，對照組就證明不了其中一道**：加了均價比對之後，「拿掉鎖」的對照組不再出事——舊部位 1.0、重開在 1.2，
       均價比對自己就擋下了。照 gold-scalper r52「對照組沒出事先懷疑情境」：改成**同一個價格**重開，均價比對分不出新舊、只剩鎖，
       對照組才又出事。→ 有好幾道防線時，每一道的對照組都要讓其他防線失效。
     - **前提量到了被測的結果（兩處）**：「交易所上現在是別人的 100」「帳上是新開的那一筆」寫在動作之後——而動作正是會把它們改掉的東西
       （別人的部位被減碼、帳上被舊的蓋掉）。改成在放行之前記下；帳上那項改看「第二張進場單送出了」。
     - **程式變嚴格之後，豁免會過期；多寫的豁免檢查器也會抓**：1R 減碼送單前改成先確認部位，`test_r30` 那兩項在突變下從「通過」變成「明確失敗」，
       豁免過期、刪掉。這輪新測試我一開始多豁免了兩條前提（它們在突變下本來就會失敗），檢查器報「清單過期」。
     - **`apply()` 這輪擋下兩次**：同一段字串在函式裡出現兩次（送單前、送單後），拆成兩個「恰好一處」的編輯；
       以及比對字串是我前面已經改掉的舊內容——兩次都一個檔都沒寫。另一次是套用腳本從 `/tmp` 執行、匯入路徑不含專案，腳本根本沒跑起來。
     - **簡化模擬又一次跟不上**：`test_lessons` 的模擬 `binance` 模組沒有「逐幣部位列」（部位確認改用它比對均價），6 項明確失敗、不是空跑。
     - **舊版程式**：`tests/legacy/` 改留 r48、r51。r51 上 7 項斷言失敗、r48 上 13 項，測試本身崩掉都是 0。
   - **gold-scalper 對照 r54 時的幾點**（r55 新增）：
     - **新防線讓 20 項舊測試失敗，先分類再改**：暫時把新的均價比對關掉重跑——還失敗的 9 項是「部位不在帳上」（直接拿沒放進帳本的部位呼叫，
       r53 的新檢查正確地不動；同 crypto-screener r53 那 5 處），關掉就通過的 11 項是「**測試資料的均價對不上**」：模擬部位表預設均價 4391.5、
       測試的帳上成交價寫 4391.0（以前沒人比對）。前者把部位放進帳本；後者讓預設均價跟最常用的帳上成交價一致（pump-dump-hunter r30「測試資料要自洽」）。
       → 「關掉新防線重跑」能把「測試用法不對」跟「測試資料不自洽」分開，不用逐項猜。
     - **新加的查詢讓突變檢查多出 8 項「空跑」**：平倉送單前的確認讓 8 項平倉相關的測試都經過被突變的查詢，而「查不到照樣送」本來就是設計，
       它們照樣通過。它們測的是送單之後的事；列成對照組豁免，引用「送單前確認」本身那一項（突變下明確失敗），由檢查器驗證。
     - **這輪 `apply()` 前的腳本崩掉一次**：用語法樹找類別時名稱猜錯，腳本在 `apply()` 之前就停了，一個檔都沒寫。
     - **舊版程式**：`tests/legacy/` 改留 r49、r52。r52 上 7 項斷言失敗、r49 上 12 項，測試本身崩掉都是 0。
   - **crypto-screener 對照 r55 時的幾點**（r56 新增）：
     - **對照組用「同價重開」讓均價比對失效**（照 pump-dump-hunter r54）：交易所端的事鎖本來就擋不到，五條路徑只剩均價比對一道，
       同價重開時五條都必須出事；另加兩組保護誤判的：均價有出入但沒有平倉成交（照常管理、手動平倉照送）、成交明細查不到（不送、不結帳）。
     - **新防線沒讓任何舊測試失敗**：因為要兩個證據才判定，舊測試裡帳上成交價跟模擬部位表有出入的地方（以前沒人比對）都沒有平倉成交，照常通過——
       這本身就是「只看均價會誤判」的證據：換成只看均價，那些測試會跟 gold-scalper r55 一樣成片失敗，而實單持倉也會。
     - **案例不能用迴圈產生**：靜態檢查逐一數 `@case`，迴圈產生的 8 項它只數到 1 個，檢查器報「解析到的案例數對不上」，一個檔都沒放行。展開成逐項寫。
     - **舊版程式**：`tests/legacy/` 改留 r50、r53。r53 上 6 項斷言失敗、r50 上 7 項，測試本身崩掉都是 0。
   - **pump-dump-hunter 對照 r56 時的幾點**（r57 新增）：
     - **「前提之後的動作不能讓測試崩掉」連兩輪都犯**：`test_r53`、`test_r56` 都在突變下去觸發一個根本沒開成的部位的停損，測試本身崩掉。
       都是突變檢查器的「測試本身崩掉」報出來的。寫「讓交易所端發生某事」的輔助動作時，先確認那個東西存在。
     - **測試的準備步驟憑空造出帳上紀錄**：`mismatched()` 開倉後改帳上成交價，開倉沒成功時照樣寫——寫出一筆只有成交價的假紀錄，
       「帳上還有這筆」就成立了（空跑）。準備步驟只改確實存在的東西。
     - **兩條爛前提**：「手動平倉之前部位還在」寫成幾乎一定成立的條件；守衛那條寫在動作之後。都改成動作之前記下。
     - **舊版程式**：`tests/legacy/` 改留 r51、r54。r54 上 8 項斷言失敗、r51 上 12 項，測試本身崩掉都是 0。
   - **gold-scalper 對照 r57 時的幾點**（r58 新增）：
     - **「應該送單」的測試，在「查不到就照樣送」的設計下會空跑**：「均價有出入但沒有平倉成交 → 手動平倉要送單」這項，突變讓部位查詢查不到時，
       平倉照樣送單（查不到照送是原本的設計），測試照樣通過——分不出送單是因為「判定是同一筆」還是「查不到」。突變檢查報出來的。
       → 補結果前提（第 16 種）：送單前真的比對了均價、查了成交明細。查不到就不送的專案（pump-dump-hunter r16）這項在突變下自然會失敗，不會踩到。
     - **前提不成立時測試本身崩掉，這輪自己的測試又兩處**：開倉後直接讀 `eng._position.get(...)`（沒開成時是 None）、
       手動平倉後直接讀 `self.manual_result[0]`。前者是舊測試照 r57 補的；後者是這輪新寫的，靜態檢查抓到。
       另外靜態檢查只認得 `len(x)` 這種直接寫法，寫成 `len(getattr(...) or ())` 它認不出來——確認的寫法要簡單直接。
     - **舊版程式**：`tests/legacy/` 改留 r52、r55。r55 上 6 項斷言失敗、r52 上 12 項，測試本身崩掉都是 0。
   - **pump-dump-hunter 對照 r58 時的幾點**（r59 新增）：
     - **「查不到就不送」要直接驗證，不能只看突變結果**：「均價有出入但沒被平過 → 手動平倉照送」在突變下確實明確失敗，
       但細看原因是突變讓**開倉**的基準查詢也查不到、部位根本沒開成——失敗在準備階段，沒走到「平倉前查不到要不要送」。
       另外補一組：部位正常開好、只在平倉那一刻讓逐幣查詢查不到——不送單、不結帳、部位與停損都保留。確認 r16 的「查不到就不送」，
       所以「應該送單」的測試只有真的比對過才會通過，gold-scalper r58 那個坑不會踩到。
     - **測試的預期要照這個專案的設計，不是照清單上別的專案的流程寫**：第一版假設手動平倉遇到「停損已觸發」會自己照成交明細結帳（gold-scalper 的流程），
       pump-dump-hunter 是交給對帳結帳（r16）——前提失敗、改成檢查回應講明不送單、帳上那筆留給對帳。
     - **舊版程式**：`tests/legacy/` 改留 r54、r57。r57 上 1 項斷言失敗、r54 上 9 項，測試本身崩掉都是 0。
   - **gold-scalper 對照 r59 時的幾點**（r60 新增）：
     - **把突變下的失敗分成「失敗在前提」與「失敗在要測的行為」**：gold-scalper 突變下明確失敗的 56 項裡，29 項失敗在前提——
       最常見的是突變把部位查詢整個換掉，測試裡模擬的查詢根本沒被呼叫，前提「真的查了」就不成立。這些項目在突變下失敗，
       只證明它們對部位查詢敏感，**不證明「那一步查不到時該怎樣」有被驗證**（r59 那點的樣子）。
       → 用失敗訊息開頭是不是「前提」把突變結果分兩類；再把每個「某一步查不到」的設計逐一對應到直接測試（部位正常在帳上、只在那一步查不到）。
       gold-scalper 七個步驟裡有兩個沒有直接測試（平倉送單前、數量比對），補上後兩項在修改前的程式上就通過——行為本來就對，只是沒被直接驗證過。
     - **直接測試要照自己專案的設計**：gold-scalper 平倉送單前「部位查不到」是**照送**（停損要執行，r55），「均價不同、成交明細查不到」才是不送（r56）；
       pump-dump-hunter 兩種都不送（r16）。三個專案這一點不一樣，直接測試的預期也不一樣。
     - **舊版程式**：`tests/legacy/` 改留 r55、r58。r58 上 2 項斷言失敗、r55 上 8 項，測試本身崩掉都是 0。
   - **crypto-screener 對照 r60 時的幾點**（r61 新增）：
     - **突變結果分兩類**（照 gold-scalper r60）：突變檢查器改成照失敗訊息開頭把「失敗在前提」與「失敗在行為」分開列。結果 137 項明確失敗裡 134 項失敗在前提、只有 3 項失敗在行為——
       幾乎全部只證明「對逐幣查詢敏感」。
     - **「某一步查不到」逐一直接測試**：模擬交易所加「只讓某一個查詢在這一刻回 500」的注入，部位正常開好、只在那一步查不到，並用結果前提確認注入真的在那一步觸發。
       14 個步驟（平倉送單前、待平倉重試、守衛查掛單、守衛補掛前、移損前、對帳全量表、對帳逐幣補查、出場價成交明細、開倉前基準、開倉前帳戶、
       成交後等部位、開倉查單、平倉查單、撤孤兒單前）在修改前的程式上**全部本來就對**——一律不送、不結帳、不算失敗。
     - **直接測試在突變下必然通過，要列豁免、引用同一步的正常路徑**：突變本身就是「逐幣查不到」，跟直接測試注入的情況一樣，預期結果也一樣，
       突變檢查器會報「命中卻通過、可能在空跑」。豁免成「對照組」，引用同一步**查得到時照常運作**的那一項（突變下必須失敗，證明突變確實打到這一步）；
       撤孤兒單原本沒有「查得到就撤」的正常路徑測試，另補一項當被引用的對照。
     - **舊版程式**：`tests/legacy/` 改留 r56、r58。r58 上 2 項斷言失敗、r56 上 5 項，測試本身崩掉都是 0。
   - **pump-dump-hunter 對照 r61 時的幾點**（r62 新增）：
     - **「某一步查不到」逐一對照直接測試**：列出 14 個步驟，6 個沒有直接測試——平倉送出後確認、守衛查掛單、1R 減碼前確認、開倉後查單、
       手動平倉讀部位表、自檢孤兒單逐幣確認（這一項另補「查得到就列成孤兒」的正常路徑）。補上後 6 個在修改前的程式上**全部通過**：
       一律不送、不結帳、不算失敗（pump-dump-hunter 查不到就不送，r16）。
     - **突變結果分兩類**：突變檢查器照失敗項目的描述是不是「（前提」「（對照組」開頭分開列，附一組人造資料自我驗證。
       209 項明確失敗裡，失敗在前提 95、失敗在行為 114。
     - **直接測試在突變下必然通過，列對照組**（照 crypto-screener r61）：守衛查掛單那兩項引用 `test_r56`「守衛照樣補掛停損」；
       手動平倉讀部位表的前提引用 `test_r58`「真的送出平倉單」——兩項都是同一步「查得到時照常運作」，突變下必須失敗，由檢查器驗證。
     - **舊版程式**：`tests/legacy/` 改留 r57、r59。r59 上 5 項斷言失敗、r57 上 6 項，測試本身崩掉都是 0。
   - **gold-scalper 對照 r62 時的幾點**（r63 新增）：
     - **直接測試在突變下是通過還是失敗，要看突變打在哪一層**：crypto-screener、pump-dump-hunter 的突變注入在模擬交易所那一層，
       跟「某一步查不到」的直接測試是同一件事，所以直接測試在突變下必然通過、要列豁免（r61）。gold-scalper 的突變是把引擎的部位查詢函式整個換掉，
       測試裡模擬的交易所查詢根本沒被呼叫——直接測試在突變下**全部失敗在前提**「真的查了」，不需要豁免。
       兩種都對，但突變結果的解讀不同：先確認自己的突變打在哪一層，再決定直接測試該通過還是失敗。
     - **第 20 種四次了，做成靜態檢查**：這輪通知格式的測試又放進了把通知 mock 掉的共用框架（前三次：通知格式、資料庫 SQL、通知格式）。
       靜態檢查改成：繼承共用框架的測試類別裡，不能呼叫框架 mock 掉的東西（通知格式、真的 SQL），附三組人造資料自我驗證。
       → 同一種錯犯到第三次，就該從「記得」改成「檢查器擋下」。
     - **舊版程式**：`tests/legacy/` 改留 r58、r60。r60 上 3 項斷言失敗、r58 上 5 項，測試本身崩掉都是 0。
   - **crypto-screener 對照 r63 時的幾點**（r64 新增）：
     - **犯到第三次的錯都已經有靜態檢查，而且每次都是它抓到的**：「先索引沒先確認」r42、r46、r49 三輪（`check_indexing`，r35 起）；
       「第一個動作不是 fresh()」r49、r52（第 14 種）；「案例數對不上」r55（第 19 種）。沒有靜態檢查而重複的錯目前都只有一次
       （r43 測試把整個 notify_telegram 換掉、r58 猜錯自檢結果的欄位名、r49 輔助函式定義在用到之後）。
     - **框架 mock 掉的東西，照 gold-scalper r63 做成靜態檢查（第 26 種）**：`fresh()` 把 `save_state` 換成不寫檔的假函式；案例若檢查狀態檔在磁碟上的結果
       （`STATE_FILE`、`.unloaded`、明確呼叫 `save_state()`）卻沒用 `fresh(keep_save=True)`，靜態檢查擋下；附三組人造資料自我驗證。現有 212 個案例 0 處。
     - **`apply()` 整批中止後，重跑時要重跑整批**：這輪一批四個檔的編輯因為一個錨點比對不到而整批中止（正確），我改了錨點卻只重跑了後半的編輯，
       前兩個檔的編輯就掉了——寫測試時「注入沒有走到」的結果前提抓到（模擬交易所的新掛勾根本沒加進去）。中止後的重跑要用原本的整批清單。
     - **舊版程式**：`tests/legacy/` 改留 r58、r61。r61 上 1 項斷言失敗、r58 上 5 項，測試本身崩掉都是 0。
   - **pump-dump-hunter 對照 r64 時的幾點**（r65 新增）：
     - **犯到第三次的錯，做成靜態檢查**（照 gold-scalper r63）：逐條數自己犯過的——「前提之後的動作讓測試本身崩掉」4 次（r36、r44、r54、r57），
       「呼叫新介面前先加存在性前提」3 次（r38、r44、r51），「前提寫在動作之後」3 次（r51、r54、r57）。前兩種做成 `tests/check_repeat.py`：
       A 類——`fx.trigger`／`fx._reduce`／`os.remove` 所在的敘述或包住它的 if 必須含對應的確認（包在 `run(lambda: …)` 裡、前一句是 `fx.open(`、
       `os.listdir` 迴圈裡也算）；B 類——測試用到的模組屬性在 `tests/legacy/` 任一版不存在時，檔案裡必須有 `hasattr`／`getattr` 守護。
       第一次掃出 13 處 A 類，其中 9 處其實有守護但寫法檢查器不認得（`run(`、`fx.open` 之後）、4 處是真的沒守護——只是還沒被踩到。
       附 17 組人造資料自我驗證。**第三種靜態分不出**「前提要求動作發生了」（合法，例如「平倉單真的送出」）跟「前提量到被測的結果」，
       只能靠突變檢查與舊版重跑在執行期抓；寫進檢查器的說明裡，不假裝有擋。
     - **`apply()` 加上「中止後重跑整批」**（r64 那點）：整批中止時記下這批每一處的指紋（路徑＋新字串，改錨點重跑時新字串不變才對得上）；
       下一次只帶了這批的一部分就擋下，整批成功寫入後忘掉。自我驗證第一版兩次寫錯——指紋先用（路徑＋舊字串），改錨點重跑時舊字串不同、對不上；
       人造資料裡中止那批的新字串跟重跑的不一樣。自我驗證抓到，共 13 組。
     - **舊版程式**：`tests/legacy/` 改留 r59、r62。r62 上 0 項失敗（這輪程式沒改）、r59 上 8 項，測試本身崩掉都是 0。
   - **gold-scalper 對照 r65 時的幾點**（r66 新增；r64 沒有新的第 8 條，這輪程式沒改）：
     - **逐條數自己犯過的測試錯誤**（用對話紀錄數，清單裡的文字比對數不準）：第 20 種 4 次（r26、r45×2、r63）→ r63 已做成靜態檢查；
       「先索引沒先確認」3 次（r34、r37、r58）→ r38 起 `check_indexing`，r58 那次就是它抓到的；第 14 種 3 次（r38、r41、r44）→ r44 起框架自動還原＋探針測試；
       **「呼叫新介面前沒加存在性前提、在舊版上崩掉」3 次（r41、r44、r58）→ 這輪做成 `tests/check_newapi.py`**：測試用到的模組屬性、引擎屬性
       在 `tests/legacy/` 任一版的原始碼裡不存在（語法樹找頂層定義、匯入、`PaperTradingEngine` 與 `TelegramNotifier` 的方法與 `self.X`），
       方法或 setUp 裡就必須有 `hasattr`／`getattr` 守護。第一版誤報 120 處：匯入的名稱沒算進模組屬性、通知是實例要看類別、
       測試框架自己掛在引擎上的屬性（`eng.alerts`）不是程式介面；修完 0 處，用真的不存在的名稱探測抓到 3／3。附 7 組人造資料自我驗證。
     - **靜態分不出的，寫進檢查器說明、不假裝有擋**：「前提量到被測的結果」（第 13 種）、「前提只看有查沒看結果」（第 16 種）——
       前提「真的送單了」跟「結果是 X」語法上一樣合法，差別在斷言的是不是被測那一步的產物，只能靠突變檢查（結果在被測步驟之前就存在時
       突變下照樣通過 → 報空跑）與舊版重跑在執行期抓。
     - **`apply()` 加「中止後重跑整批」**（照 pump-dump-hunter r65：指紋＝路徑＋新字串）：中止時存成檔案，下一批沒涵蓋就擋下、整批成功就忘掉。
       **改寫工具自己也會壞**：這輪用 `apply()` 改 `patch.py`，新程式用到 `os` 卻沒匯入——寫入前的 `compile()` 只抓語法、抓不到執行期的名稱錯誤，
       工具寫完自己就崩、連自我驗證都跑不了，只能手動補匯入。→ 改寫工具改完先跑 pyflakes（會報 undefined name）再跑自我驗證；自我驗證裡故意中止的案例
       會留下「待重跑批次」，後面的案例要先清掉，否則整個自我驗證死在中途（第一次就是這樣）。自我驗證共 10 項。
     - **舊版程式**：`tests/legacy/` 改留 r60、r63。r63 上 0 項失敗（這輪程式沒改）、r60 上 3 項，測試本身崩掉都是 0。
   - **crypto-screener 對照 r66 時的幾點**（r67 新增；r64～r66 沒有新的第 8 條，這輪程式沒改）：
     - **逐條數自己犯過的測試錯誤**（用對話紀錄數）：「先索引沒先確認」3 輪（r42、r46、r49）→ r35 起 `check_indexing`，三次都是它抓到的；
       「第一個動作不是 fresh()」2 輪（r49、r52）→ 第 14 種；「案例數對不上（迴圈產生）」1 次（r55）→ 第 19 種；「全否定句沒前提」1 次（r49）→ 已有。
       沒有靜態檢查的都只有一次：r43 測試把整個 notify_telegram 換掉、r49 輔助函式定義在用到之後、r53 舊測試拿不在帳上的部位呼叫函式、
       r58 猜錯自檢結果的欄位名（測試崩掉）、r61 直接測試在突變下命中卻通過沒豁免、r64 apply 中止後只重跑一部分。犯到第三次的目前沒有還缺檢查器的。
     - **靜態分不出的寫進 `check_tests.py` 說明、不假裝有擋**：「前提量到被測的結果」（第 13 種）、「前提只看有查沒看結果」（第 16 種）靠突變檢查與舊版重跑；
       「前提之後的動作讓測試崩掉」靠舊版重跑的崩掉計數與突變檢查的 CRASHES；「把被測那段 mock 掉」靠人看（第 26 種只擋框架自己 mock 的 save_state）。
     - **`apply()` 加「中止後重跑整批」**（照 pump-dump-hunter r65：指紋＝路徑＋新字串，存成 `scripts/.patch_pending.json`）：只帶一部分就中止、一個檔都不寫；
       改了錨點但新字串相同的整批放行；整批成功後刪掉。自我驗證把待重跑檔改指到暫存目錄（不碰真的），故意中止的案例之後 `clear_pending()`（r66）。
       改完 `patch.py` 先跑 pyflakes 再跑自我驗證（r66）：verify.sh 的 pyflakes 步驟本來就掃 scripts/、排在自我驗證之前，**本來就對**。
     - **舊版程式**：`tests/legacy/` 改留 r61、r64。r64 上 0 項失敗（這輪程式沒改）、r61 上 1 項，測試本身崩掉都是 0。
   - **pump-dump-hunter 對照 r67 時的幾點**（r68 新增；r66、r67 沒有新的第 8 條，這輪程式沒改）：
     - **改寫工具改完自己有沒有先跑 pyflakes：以前沒有，而且同一種錯早在 r47 就踩過**（用 `apply()` 加引擎鎖時 `manager.py` 用到 `threading` 沒匯入，
       是之後手動跑 pyflakes 才抓到，`compile()` 過了）。r65 改 `patch.py` 時運氣好、匯入在同一批裡。這輪不靠記得：`apply()` 寫入前
       除了 `compile()`，再用 pyflakes 的 API 在記憶體裡掃每個改過的 .py，有「undefined name」就整批中止、一個檔都不寫；pyflakes 沒裝時印警告、不擋。
       自我驗證加「用了沒匯入的名稱 → 中止、檔案沒被改動」。這樣「先跑 pyflakes 再自我驗證」的順序由工具保證，不是流程。
     - **自我驗證的故意中止會不會擋到後面的案例：會，而且更糟——它寫的是真的那份待重跑檔**。r65 的待重跑檔放在 `/tmp`，
       自我驗證裡故意中止的案例寫進去、後面成功的案例又把它刪掉：自我驗證前若真的有一批中止中，跑一次自我驗證就把它忘了；
       反過來真的中止中的批次也可能擋到自我驗證的案例（r65 沒出事是因為指紋剛好不同）。改成：待重跑檔放專案內 `scripts/.patch_pending.json`
       （`/tmp` 會跟別的專案共用；加進 .gitignore）；自我驗證期間改指到暫存目錄、結束還原；故意中止的案例之後 `clear_pending()`；
       加兩項自我驗證確認「故意中止寫的是暫存那份、真的那份沒被動」「clear_pending() 之後消失」。自我驗證共 17 項。
     - **verify 的 pyflakes 步驟**：`scripts/verify.py` 第一步就是 pyflakes、掃 `app/ tests/ scripts/`，排在改寫工具的自我驗證之前，**本來就對**。
     - **舊版程式**：`tests/legacy/` 改留 r62、r65。兩版都 0 項失敗（r64、r65 兩輪程式都沒改），測試本身崩掉都是 0。
   - **gold-scalper 對照 r68 時的幾點**（r69 新增；r67、r68 沒有新的第 8 條，這輪程式沒改）：
     - **pyflakes 放進工具裡**（照 pump-dump-hunter r68）：`apply()` 寫入前除了 `compile()`，用 pyflakes 的 API 在記憶體裡掃每個改過的 .py，
       有 undefined name 就整批中止、一個檔都不寫；沒裝時印警告、不擋。r66 那次「工具改完自己就崩」用這條就擋得住。
     - **自我驗證寫的是真的那份待重跑檔——r66 就是這樣**：待重跑檔 r66 起就在專案內（沒放 /tmp），但自我驗證的 `reset()` 直接刪真的那份；
       自我驗證前若真的有一批中止中，跑一次自我驗證就把它忘了。改成自我驗證期間把路徑改指到暫存目錄、結束還原，`finally` 裡比對真的那份
       內容有沒有變（變了就拋錯）；實測先放一份假的待重跑檔再跑自我驗證，結束後原封不動。加三項：用了沒匯入的名稱時中止、故意中止寫的是暫存那份、
       `clear_pending()` 後消失；共 13 項。
     - **verify 的 pyflakes 步驟**（r67 那點）：第一步就掃 `app/ tests/ scripts/`，排在自我驗證之前，**本來就對**。
     - **舊版程式**：`tests/legacy/` 改留 r63、r66。兩版都 0 項失敗（r66、r68 兩輪程式都沒改），測試本身崩掉都是 0。
   - **pump-dump-hunter 對照 r69 時的幾點**（r70 新增；r68、r69 沒有新的第 8 條，這輪程式沒改）：
     - r69 gold-scalper 那點照做：自我驗證期間改指暫存目錄的還原改進 `finally`；結束時比對真的待重跑檔**內容**有沒有變（r68 只看存在與否），
       變了就拋錯；實測先放一份假的待重跑檔再跑自我驗證，結束後原封不動、假的那份也不擋真的批次（指紋不同）。自我驗證共 17 項。
     - **舊版程式**：`tests/legacy/` 改留 r65、r68。兩版都 0 項失敗（連續三輪程式沒改），測試本身崩掉都是 0。
