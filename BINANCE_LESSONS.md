# 幣安踩坑共用清單

**版本 2026-09-21r31（r9 作廢，未採用）。這份檔案在 crypto-screener、gold-scalper、pump-dump-hunter 三個專案裡內容相同。**
發現新坑 → 在這裡加一條 → 三個 repo 都更新一份。開新對話時把這份檔案一起丟給 Claude，
它就不必重新踩一次。每一條都寫「症狀 / 原因 / 處理 / 誰踩到的」，不寫抽象原則。

---

## 1. 條件單搬到 Algo 端點（2025-12-09）

- **症狀**：下單成功，但停損單回 `-4120 Order type not supported for this endpoint. Please use the Algo Order API endpoints instead.`，倉位裸奔。
- **原因**：幣安把 STOP_MARKET / TAKE_PROFIT_MARKET / TRAILING_STOP_MARKET 全部移到 Algo 服務，舊的 `/fapi/v1/order` 直接拒絕。
- **處理**：
  - 掛單 `POST /fapi/v1/algoOrder`，要帶 `algoType=CONDITIONAL`，`stopPrice` 改叫 `triggerPrice`，`activationPrice` 改叫 `activatePrice`，回傳的是 `algoId` 不是 `orderId`。
  - 撤單 `DELETE /fapi/v1/algoOrder` 帶 `algoId`；查掛單 `GET /fapi/v1/openAlgoOrders`。
  - 查回來的型別欄位叫 `orderType`（舊端點叫 `type`），兩個都要看。
  - 先打新端點，**只有 404 或未知端點才退回舊寫法**；參數錯（-1111、-2021 之類）不能誤判成端點不存在。
- **誰踩到**：crypto-screener 先踩並修好，pump-dump-hunter 沒同步，2026-09-20 NIL、09-21 SAGA 各裸奔一次。
- **退回不能是永久的**（r12 新增）：「端點不支援」的判斷若寫進一個全程序共用的旗標，一次誤判就會讓之後**所有**條件單改走舊端點、
  全部 `-4120`，直到重啟——停損、補掛、移損全部失效。只有 HTTP 404 才能當成「端點不存在」。
  - **誰踩到（crypto-screener）**：把 `-1000`（幣安的「未知錯誤」，多半是暫時性的伺服器問題）與 `-1013`（價格／數量過濾器，參數錯）
    都判成端點不存在，並永久切換。2026-09-21 對照 r11 時由測試帶出，改成只有 404 才退回。
  - **只有 404 還不夠，退回也要有期限、要能自己改回來**（r13 新增）：
    404 也可能是一時的（閘道、維護）。退回舊端點要設期限（例如 10 分鐘後重新試 Algo）；
    期限內舊端點若回 `-4120`，代表交易所明確說要用 Algo——**立刻改回 Algo 重送這一張**，不要等期限到。
    退回期間兩個端點都可能有自己的單，「查停損還在不在」要兩邊都查；Algo 那邊查詢失敗，只有在「目前確實走舊端點」時才能忽略。
  - **誰踩到（pump-dump-hunter）**：退回條件包含 `-1013` 與字串比對 `Unknown`，並永久切換。`-1000` 碰巧沒被判中
    （幣安訊息是小寫 unknown），但原因不對。2026-09-21 對照 r12 時改成上述做法，測試在修改前 3 項失敗。
  - **退回期間 Algo 查詢失敗，停損守衛不能整個跳過**（r15 新增）：守衛通常寫成「查詢失敗就跳過這輪」（第 2 條原則 1），
    但退回期間 Algo 端點一直回 404，每一輪都「查詢失敗」，守衛就**永遠不檢查**——舊端點的停損不見了也沒人補。
    退回期間要改用舊端點的查詢結果判斷；只有兩邊都查不到才跳過。
  - **「退回期間」要怎麼知道**（r16 新增）：如果只有「掛停損單時收到 404」才進入退回，服務**重啟後**退回旗標歸零，
    在還沒掛過新停損之前，守衛查 Algo 一直 404、判定「查詢失敗、跳過」——上面那一點又發生了，只是換了觸發條件。
    → 查停損時 Algo 回 404，也要跟掛單一樣進入退回期。
  - **誰踩到（pump-dump-hunter）**（r16 新增）：r13 的判斷式本身是對的（退回期間用舊端點結果），但只有掛單會設定退回旗標。
    2026-09-21 對照 r15 時，測試模擬「沒有 Algo 服務的環境、服務剛重啟、舊端點的停損不見了」，守衛 3 輪都沒補掛。
  - **誰踩到（crypto-screener）**（r18 新增）：同上，只有掛單會進入退回；而且帳上記的端點是 algo 時連舊端點都不查。
    2026-09-21 對照 r17 時改成查詢 404 也進入退回。
  - **誰踩到（crypto-screener）**（r15 新增）：2026-09-21 對照 r14 時，404 後永久改走舊端點、舊端點回 `-4120` 也不改回，
    以及上面「守衛整個跳過」。改成 10 分鐘後重試 Algo、遇 `-4120` 立刻改回重送、退回期間兩個端點都查。

## 2. 「查不到」不等於「不存在」

- **症狀**：誤判停損單消失 → 觸發保護動作 → 誤平倉。
- **原因**：查詢 API 失敗（超時、限流、回傳格式非預期）被當成「查到空清單」。
- **處理**：查詢函式回傳 `(orders, ok)`，`ok=False` 一律跳過不動作。要動作前還要：
  1. 連續 3 輪都確認不在才動手；
  2. 補掛被拒、原因含 existing / already → 視為誤判，不動作；
  3. 預設只補掛、不強制平倉。強制平倉是事故的放大器。
- **誰踩到**：crypto-screener 誤平倉一次，教訓寫在它的 `guard_positions` 註解裡。
- **查到空清單也不一定是「沒有」**（r15 新增）：`GET /fapi/v2/positionRisk` 不帶 symbol 時回傳全部幣，
  但維護或閘門異常時可能回 200 加空清單。對帳若據此判定「全部部位都平了」，會一次把所有部位記成平倉、**所有停損一起撤掉**。
  全量表裡找不到某個部位時，先帶 symbol 逐幣再查一次；逐幣查不到（失敗）就這輪不動。
  crypto-screener 2026-09-21 對照 r14 時補上，尚未出事。
  - **不只對帳會用到全量表**（r16 新增）：凡是用「查部位」下結論的地方都要檢查——
    平倉後確認「這一側沒了」、pending 到期前確認「交易所上沒有」、送單前記基準。這些地方直接帶 symbol 逐幣查，
    不要從全量表裡找。
  - **誰踩到（pump-dump-hunter）**（r16 新增）：對帳用全量表判平倉（同 crypto-screener）；更嚴重的是**平倉確認**也用全量表——
    平倉單被拒、確認查詢又剛好回空清單，就把部位記成已平倉、撤掉停損，交易所上留下沒有停損的倉。
    pending 到期判定「未成交」也用全量表。2026-09-21 對照 r15 時全部改成逐幣查詢。
  - **逐幣查詢回空清單也是異常**（r17 新增）：帶 symbol 查 positionRisk 時，交易所一定會回這個幣的列
    （單向 1 列 `BOTH`、雙向 `LONG`/`SHORT` 2 列，數量 0 也會回）。回 200 加空清單＝查詢異常，要當成「查不到」，
    不能當成「數量 0」。改用逐幣查詢不等於安全了——判斷式要檢查「有沒有這個幣的列」，不是只看加總數量。
  - **誰踩到（gold-scalper）**（r17 新增）：本來就一律逐幣查，但查詢函式把「沒有這個幣的列」當成數量 0。
    平倉單逾時、確認查詢剛好回空清單時，會判成「這一側已經沒了」、撤掉停損結帳；pending 到期也會判成未成交。
    2026-09-21 對照 r16 時改成沒有該幣的列就回「查不到」。
  - **「沒有部位才能做 X」的判斷也一樣**（r18 新增）：撤孤兒單、刪紀錄之類的動作，前提常寫成
    `any(數量 > 0 for 列 in 回傳)` 為假就放行——空清單時 `any()` 也是假，**判成「沒有部位」就動手了**。
    同樣要先確認回傳裡有這個幣的列。
  - **誰踩到（crypto-screener）**（r18 新增）：查詢函式把沒有該幣的列當成 0（同 gold-scalper），影響平倉前確認、
    平倉後確認、送單前基準三處；撤孤兒單用上面的 `any()` 寫法，空清單時會撤。另外 pending 到期判定「未成交」用全量表（同 pump-dump-hunter）。
    2026-09-21 對照 r17 時全部修正。
  - **誰踩到（pump-dump-hunter）**（r19 新增）：r16 才把平倉前後確認、對帳、pending 到期、送單前基準四處從全量表改成逐幣查詢，
    但查詢函式在回傳前就把數量 0 的列濾掉——空清單和「真的沒部位」變得無法分辨，四處全中（正是 r17 說的「改用逐幣查詢不等於安全」）。
    2026-09-21 對照 r18 時改成：沒有該幣的列就拋例外，呼叫端一律走「查不到、這輪不動」。
  - **守衛補掛前也要確認部位還在**（r19 新增）：停損不見了就補掛，看起來天經地義；但如果部位其實已經被平掉
    （例如這一輪對帳出錯、還沒偵測到），補上去的就是一張孤兒 reduce-only 單，單向共用帳號裡還可能平到別人同側的倉（第 7、13 條）。
    → 補掛前逐幣確認自己的部位（扣基準）：查不到就這輪不動、確認沒了就不補（交給對帳）、還在才補，數量取「交易所−基準」與帳上的小者。
    這一點在「對帳跟守衛各自 try」（第 8 條 r18）之後特別重要——對帳出錯時守衛照樣會跑。
  - **不只守衛補掛，任何「掛一張新停損」都要先確認**（r20 新增）：第一次掛、**移損時掛新的那一張**、補掛，
    都是送出一張新的 reduce-only 單。移損尤其容易漏——它看起來是「更新」既有的停損，實際上是新掛一張。
    部位已被平掉、只是還沒偵測到時，價格一動就移損，就掛出孤兒單。確認要寫在「送出停損單」那一個共用的地方，不是守衛裡。
  - **誰踩到（gold-scalper）**（r20 新增）：第一次掛、移損、守衛補掛共用一個函式，但這個函式掛單前完全不查部位；
    數量用帳上的、沒扣基準。2026-09-21 對照 r19 時在這個共用函式裡補上確認：查不到這輪不動（不算掛單失敗）、
    扣基準後沒了就不掛（交給數量比對）、數量取小。啟動對帳遇空清單原本會回報「一致（空手）」，改成回報「查不到」。
  - **有基準時停損不能用 `closePosition`**（r21 新增）：`closePosition=true` 是「平掉這一側全部」，不看數量。
    同側有別人的部位（基準 > 0）時，這張停損一觸發就把**別人的部位一起平掉**——平倉單扣了基準（第 7 條 r15），停損沒扣，一樣會出事。
    → 有基準時停損改用「自己的數量＋reduce-only」（數量取交易所−基準與帳上的小者）；沒有基準時可以維持 `closePosition`
    （好處是部分出場後自動涵蓋剩餘）。這個判斷也放在「送出停損單」的共用地方。
  - **自檢／啟動對帳遇空清單的另一個後果**（r21 新增）：用全量表判斷「哪些幣沒有部位」再找孤兒條件單時，
    空清單會讓**所有部位的停損都被列成孤兒單**，畫面上旁邊還有撤單按鈕。全量表裡找不到時要逐幣確認，查不到的幣不列孤兒。
  - **誰踩到（crypto-screener）**（r21 新增）：2026-09-21 對照 r20 時——第一次掛、認領、守衛補掛、移損掛新、移損失敗掛回共 5 處
    各自直接送單，全部不確認部位、全部用 `closePosition`（有基準時會平到別人的）；自檢遇空清單把自己部位的停損列成孤兒。
    改成全部走一個 `place_stop()`：確認部位（扣基準）、查不到回「unknown」這輪不動不算失敗、沒了回「gone」交給對帳、
    有基準用數量。移損在**撤舊之前**就先確認（否則撤了才發現查不到，就裸倉了）。
    認領時用全量表那一列當確認（這一列本身就是「部位在」的正向證據；危險的是空清單被當成「沒有」，不是反過來）。
  - **誰踩到（pump-dump-hunter）**（r22 新增）：5 處各自送停損單，只有守衛補掛（r19 才加）有確認；**移損掛新的那一張完全沒確認**，
    數量也沒跟「交易所−基準」取小。沒有 `closePosition`（r3 起就是 reduce-only 帶數量），有基準時帳上數量本來就是自己的。
    2026-09-21 對照 r21 時改成一個共用的 `place_stop()`，回傳 ok／unknown／gone；手上有正向證據（剛成交的回應、交易所那一列）的
    呼叫端直接傳已知數量，不重查。移損遇 unknown 時**不算失敗**、想要的停損價留著，下一輪重試。
    自檢孤兒單 r19 已逐幣確認、查不到的列「無法確認」不列孤兒，這點本來就對。
  - **誰踩到（gold-scalper）**（r23 新增）：r20 把確認放進共用函式後，**開倉成交、認領之後掛停損也會重查**——
    手上已有成交回應／交易所那一列，卻又查一次；交易所剛成交時偶爾還沒反映，重查拿到 0 就判「部位不在」而**不掛停損**。
    2026-09-21 對照 r22 時改成這兩處直接傳已知數量；共用函式改成回傳 unknown／gone／placed 等結果，測試的前提直接檢查結果（第 16 種）。
    停損一律 reduce-only 帶數量、從沒用過 `closePosition`，r21 那點本來就對。
  - **誰踩到（crypto-screener）**（r24 新增）：2026-09-21 對照 r23 時，開倉與認領 r21 起就傳已知數量、不重查，這點本來就對。
    但移損與守衛遇 unknown／gone 時**什麼都不回傳**，測試分不出「查不到」和「沒了」（第 16 種）；改成回傳略過的原因。
  - **誰踩到（pump-dump-hunter）**（r25 新增）：開倉、認領、手動認領 r22 起就傳已知數量，本來就對；移損 r22 起會回傳 unknown／gone。
    **停損守衛**的每個出口都回 None（查詢失敗、停損還在、還沒到第 3 輪、查不到、沒了、已存在誤判、補掛失敗），
    2026-09-21 對照 r24 時改成每個出口都回傳原因，並用語法樹確認函式裡每一個 `return` 都帶值。
  - **誰踩到（gold-scalper）**（r26 新增）：掛停損、數量比對 r23 已回傳原因，但**停損守衛** 4 個出口都回 None，補掛那個出口也沒把掛單結果傳回來。
    2026-09-21 對照 r25 時改成回傳 skip／unknown／present／missing／replaced:（掛單結果）。語法樹檢查除了「每個 `return` 帶值」，
    還要檢查**函式最後不會掉出去**（最後一行不是 return 時，Python 一樣回 None）。
  - **誰踩到（crypto-screener）**（r27 新增）：2026-09-21 對照 r26 時，停損守衛 8 個出口都是裸 `return`、最後一句是 `if` 會掉出函式；
    移損「還沒到」「已移過」「待平倉」三個出口回 None。改成每個出口回傳原因，並用語法樹檢查
    （`scripts/check_returns.py`：每個 return 帶值、明寫 `return None` 也算沒帶、最後不能掉出去；附 6 組人造函式自我驗證）。
  - **誰踩到（pump-dump-hunter）**（r28 新增）：r25 只用語法樹檢查了守衛「每個 return 帶值」，沒檢查會不會掉出函式，也只檢查了守衛一個。
    2026-09-21 對照 r27 時擴大到守衛、移損、重試移損、送停損、重試平倉五個：移損**成功時**最後一句是 `if`，掉出函式回 None；
    重試移損有兩個裸 `return`、最後的 try 也會掉出去；重試平倉有一個裸 `return`。守衛與送停損本來就對。
    改成每個出口回傳原因（`scripts/check_returns.py`，照 crypto-screener 的做法附 6 組人造函式自我驗證）。
  - **誰踩到（gold-scalper）**（r29 新增）：r26 的語法樹檢查用 `n.value is None` 判斷「不帶值」，**明寫的 `return None` 抓不到**
    （它的 value 是一個值為 None 的常數節點）；也只檢查了守衛、掛停損、數量比對三個。2026-09-21 對照 r28 時補上 `return None`、
    「最後一句是沒有 else 的 if」算掉出去，並擴大到認領、重試平倉（兩者都還有裸 `return`）；附 6 組人造函式自我驗證。
    檢查本身也要有前提：每個函式真的解析到多個 `return`（第 19 種）——這項前提是這輪的測試靜態檢查抓到它缺的。
  - **誰踩到（crypto-screener）**（r30 新增）：2026-09-21 對照 r29 時，r27 的檢查本來就把明寫的 `return None` 算成不帶值、
    沒有 else 的 if 算掉出去；但前提只看**總數**（「return 總數 ≥ 函式數」），一個函式只有 1 個 return 會被別的函式撐過去。
    改成每個函式至少 2 個，並把新增的出場價函式、手動平倉、撤孤兒單納入（共 10 個函式）。
  - **誰踩到（pump-dump-hunter）**（r31 新增）：r28 的檢查本來就把明寫的 `return None` 算成不帶值、沒有 else 的 if 算掉出去，
    但前提只看「掃了幾個函式」，沒看每個函式有幾個 return。2026-09-21 對照 r30 時改成每個函式至少 2 個，
    並把手動平倉、認領、網頁交易入口納入（共 8 個函式）；這三個 r28 本來就每個出口都回傳值。

## 3. 下單成功但沒記到帳 → 孤兒倉

- **症狀**：交易所有倉，程式的持倉清單是空的；之後該幣的訊號被自己擋掉，還誤判成「其他專案的倉」。
- **原因**：市價單送出後、記帳前如果拋例外（例如接著掛停損失敗），整段被外層 try 吞掉，什麼都沒寫。
- **處理**：順序必須是 **送單前先寫 pending → 成交後立刻記帳 → 最後才掛停損**。
  **送單回應逾時或 5xx 時結果不明**（r12 新增）：單可能已經成交，不能當成送單失敗而清掉 pending——要保留給下一輪對帳確認。
  只有交易所明確拒絕（4xx 帶錯誤碼）才可以清掉。crypto-screener 在 r11 時把逾時（狀態 0）當失敗並清掉 pending，已修正。下一輪對帳時，pending 裡還在交易所的倉要認領回來。另外做一個手動認領/平倉的介面，處理修正前留下的孤兒倉。
- **保留下來的 pending 要有期限，而且期間要擋同幣進場**（r13 新增）：
  1. 對帳「這一輪沒看到部位」不能立刻清掉 pending——交易所可能還沒反映。給一個期限（例如 180 秒），過了還沒有才判定未成交並通知。
     否則逾時之後，同一輪迴圈裡下一個訊號觸發的強制對帳，就會在交易所反映前把 pending 清掉。
  2. pending 期間同一個幣不能再下新單，本策略的持倉數也要把 pending 算進去。
- **認領時數量、均價一律拿交易所那一列的，並立刻掛停損**（r13 新增）：
  pending 是送單**前**寫的，裡面只有訊號資訊，沒有成交數量與均價。直接把 pending 紀錄當成部位，
  之後掛停損、移損、平倉用到數量時會出錯——而這些都在守衛的 try 裡，錯誤被吞掉、**部位永遠沒有停損**。
  另外認領回來的部位一定還沒掛停損（停損是記帳之後才掛的），不能等守衛「連續 3 輪確認不在」，要當場掛。
  - **誰踩到（pump-dump-hunter）**：認領直接複製 pending 紀錄，沒有數量；守衛補掛時 `KeyError: 'qty'` 被接住，只推播一則
    「補掛失敗：'qty'」、錯誤區沒有紀錄。2026-09-21 對照 r12 時由測試帶出（測試第一版只看錯誤區，碰巧通過，見用法第 5 點）。
- **送單前先記下這一側原有的數量**（r14 新增）：認領時「交易所那一列」可能包含送單前就有的部位
  （共用帳號、之前的孤兒倉、同側加碼）。送單前查一次、記下基準，認領數量＝現在減基準；有基準部位時均價是合併過的，
  要在通知裡講明，不能當成這張單的成交價。
  - **誰踩到（gold-scalper）**（r14 新增）：開倉回應逾時一律當送單失敗，還把「是否真的開倉」記成否——
    之後出場照規則不送真實平倉單，若其實成交就留下孤兒倉。沒有 pending 機制。2026-09-21 對照 r13 時補上：
    回應不明先查交易所，看到就認領（數量均價取交易所、當場掛停損），沒看到就記成「不明」保留 180 秒每輪確認。
    gold-scalper 每個引擎一個帳戶、一次一個部位，pending 期間引擎已有帳面部位，本來就不會再開新倉。
  - **基準查不到就不送單**（r16 新增）：送單前的基準查詢失敗時，不能當成 0 繼續送——之後認領、平倉都會把別人的部位算成自己的。
    這次不下單、記錄原因，下一根 K 棒再說。
  - **基準要跟著部位走完整個生命週期**（r16 新增）：不只認領時扣，**對帳判斷平倉、偵測數量減少、平倉前確認剩多少、
    平倉後確認沒了**，全部都要扣基準（見第 7 條 r15）。只在認領時扣，其他地方照樣會把別人的部位算進來。
  - **基準的限制**：只涵蓋「送單前就有」的部位。單向共用帳號裡，別的專案在你進場**之後**才在同一側加碼，
    交易所上是合併成一列，程式無從分辨。真正的解法是第 7 條的「上主網前每個專案開獨立子帳號」。
  - **誰踩到（pump-dump-hunter）**（r16 新增）：沒有基準。2026-09-21 對照 r15 時補上：送單前逐幣查、記進 pending 與部位紀錄，
    認領數量＝交易所−基準、有基準時用基準均價反推這張單的成交價並在通知裡講明是估計值。
  - **即時損益也要扣基準**（r18 新增）：畫面上的未實現損益、以及拿它算 R 倍數的地方（例如反向訊號時判斷持倉是否已賺到 1R），
    若直接用交易所那一列的 `unRealizedProfit`，同側有別人的部位時會把別人的損益算進來。改用「自己的數量 ×（標記價 − 自己的進場價）」。
  - **誰踩到（crypto-screener）**（r18 新增）：2026-09-21 對照 r17 時——基準查不到時以 0 計繼續送單（r14 時的寫法）；
    認領沒有用基準均價反推；即時損益沒扣基準。「全生命週期扣基準」的其他地方（對帳判平倉、偵測數量減少、平倉前後確認）r14 已做。
  - **誰踩到（pump-dump-hunter）**（r19 新增）：儀表板「帳號全部持倉」表中本策略那一列，損益直接用交易所整列的 `unRealizedProfit`。
    沒有拿即時損益算 R 的地方（R 一律用 K 棒價格與自己的進場價算）。2026-09-21 對照 r18 時補上「本策略自己的損益」欄，
    原本那欄保留並註明是整列（含同側別人的部位）。
- **注意**：對帳迴圈如果只在「帳上有持倉」時才跑，崩潰後帳上是空的、只剩 pending，認領就永遠不會發生。觸發條件要是「有持倉 **或** 有 pending」。
- **誰踩到**：pump-dump-hunter，2026-09-20。crypto-screener 2026-09-21 檢查時發現同樣的空窗（市價單到記帳之間約百行、五六次 API 呼叫），尚未出事即補上。
- **認領要在「判斷哪些部位還在」之前做**（r15 新增）：對帳常見的寫法是先從部位表算出「還在場的部位」，
  再處理 pending 認領，最後把「帳上有、但不在場」的記成平倉。剛認領進帳的部位不在先前算好的清單裡，
  **同一輪就被判成已平倉**，平倉流程還會把剛掛上的停損撤掉——交易所上留下一個沒有停損、帳上也沒有的倉。
  → 先認領，再算「還在場」。測試要走完整的對帳流程，不能只單獨呼叫認領函式（單獨呼叫測不到順序）。
- **誰踩到（crypto-screener）**（r15 新增）：2026-09-21 對照 r14 時，r13、r14 的每一點都有——pending 120 秒就靜靜丟掉、
  pending 期間同幣仍可下單且不算持倉數、送單前沒記基準數量（成交確認看到部位就當成這張單的），
  以及上面「認領後同一輪被判平倉」。最後一項是測試走完整對帳流程時才發現：上一輪的認領測試直接呼叫認領函式，所以一直通過。

## 4. 數量與價格必須照交易所精度

- **症狀**：`-1111 Precision is over the maximum defined for this asset`。
- **原因**：`round(qty)` 直接取整、停損價固定小數位，沒照 `exchangeInfo` 的 `stepSize` / `tickSize`。
- **處理**：開機讀一次 `exchangeInfo` 快取；數量無條件捨去到 `stepSize`、價格四捨五入到 `tickSize`，都用字串送出避免科學記號；低於 `minQty` / `minNotional` 提早擋下。
- **延伸**：出場位階（R、目標、移動停利啟動價）要用**取整後、實際掛出去**的停損價算。用取整前的算，R 會差一個跳動點，績效的 R 倍數跟著偏（crypto-screener 的一致性測試抓到）。

## 5. 槓桿設不上會沉默失敗

- **症狀**：保證金佔用與強平距離跟預期不同。
- **原因**：新子帳戶常被限制在 5x，小市值幣的分層本身也可能低於 10x，設定被拒卻沒處理。
- **處理**：設不上就查 `leverageBracket` 取上限、改用上限，並依實際槓桿縮量；回傳實際值與原因，不要沉默吞掉。**換子帳號上主網前一定要重測這項。**
- **順序**：設槓桿要在計算部位大小**之前**——名目上限與保證金上限都要用實際生效的倍數。crypto-screener 曾把兩者順序寫反，變數在賦值前被使用，正式網第一次下單直接拋 `UnboundLocalError`（送單前就中止，沒有留倉）。

## 6. positionRisk 權重高，打太兇會被限流

- **症狀**：`418 I'm a teapot`（429 累積後的 IP 封鎖），對帳整段失效。
- **原因**：每產生一個訊號就對帳一次。
- **處理**：對帳結果快取 20 秒，只有真的要下單前才強制重抓。查掛單優先帶 symbol（權重 1），不帶 symbol 是 40。
- **延伸**：會打高權重查詢的端點如果不需要登入（例如網頁上的自檢按鈕），要加冷卻，不能讓它被反覆觸發。

## 7. 共用模擬帳號會互相干擾

- **症狀**：A 專案的持倉讓 B 專案不敢下單；或 B 的殘留 reduce-only 單平掉 A 的倉。
- **原因**：三個專案共用同一個 demo 帳號。
- **處理**：持倉數只算自己開的，每輪跟交易所對帳；撤單一律用 `orderId` / `algoId` 精準撤，**不要用 `cancel all open orders by symbol`**；本策略的倉位上限獨立計算（例如 pump-dump-hunter 把本金上限壓在 1500 U）。上主網前每個專案開獨立子帳號。
- **雙向模式**：共用帳號若切成雙向持倉，每張單要帶 `positionSide`、且不能帶 `reduceOnly`；讀 positionRisk 時只認自己那一側。模式可能被別的專案切換，遇到 `-4061` / `-1106` 要強制重新偵測模式並重送一次。
  - 對帳也要用「幣＋方向」比對，不能只看幣名：我的多單已被停損、別的專案剛好有同幣空單時，只看幣名會誤以為自己還在場。
    **單向模式也要做**：單向時 `positionSide` 一律是 `BOTH`，方向要看 `positionAmt` 的正負；只在雙向模式過濾 `positionSide` 不夠。
  - 持倉模式若有快取，上面的「強制重新偵測」要清掉快取，否則快取期間會一直送錯參數。
    重新偵測本身也可能失敗（逾時、限流）：這時要反轉**這張被拒的單送出時的假設**——帶了 `positionSide` 就是當雙向送的，重送改單向寫法，反之亦然——
    再用結果更新快取。**不能拿快取來反轉**：快取可能從沒偵測成功過（空值），也可能在送單後被別的執行緒改過，
    用它反轉會拿同一個錯誤假設再送一次。（r8 更正：r5–r7 這裡寫「直接把快取反轉」，會誤導。）
  - **誰踩到（反轉快取）**：crypto-screener 在 r5 照原文實作成「反轉快取」，快取為空時沒有值可反轉，
    就用同一個假設重送、再被拒。2026-09-21 對照 r7 時改成依被拒那張單的參數反轉，並用測試確認修改前會失敗。
- **守衛程式只能碰自己記錄過 id 的單**：「確認停損還在」、「清掉多餘的停損單」這類守衛，不能把同幣的所有條件單都當成自己的。
  共用帳號裡別的專案同幣的停損會被當成殘留撤掉；反過來，自己的停損不見了、只剩別人的單時，也會誤判成「停損還在」。
  正確做法：只認部位上記錄過的 `algoId` / `orderId`；沒記錄 id 的舊部位才退而用方向判斷。
  - **移動停利不能算「停損還在」**：它要到啟動價才生效、通常只涵蓋部分數量。把它算進去，真正的停損不見時會被它蓋過去。
  - **守衛補掛成功要把新 id 換上帳、拿掉舊 id**。沒換的話，改成「只認 id」之後，帳上那個已不存在的舊 id 會讓每一輪都誤判停損不在。
  - **誰踩到**：pump-dump-hunter，2026-09-21 對照 r2 檢查時在自己的 `ensure_stop` 發現（前一版才引入），尚未出事即修正。
  - **誰踩到（對帳）**：gold-scalper 啟動對帳把帳戶所有 `positionAmt` 加總成淨額再比，雙向模式多空各一張會誤判空手、只剩別人的反向單會誤判仍在場。2026-09-21 對照 r3 時改成依方向比對。
  - **誰踩到（crypto-screener）**：2026-09-21 對照 r4 檢查時，四點都有：
    對帳只在雙向模式過濾方向（單向漏了正負號）；模式重新偵測失敗時快取不變；
    守衛把同幣任何 STOP_MARKET 或 TRAILING_STOP_MARKET 都當成自己的停損；補掛成功沒記新 id。均已修正，尚未出事。
  - **誰踩到（偵測失敗就放棄）**：gold-scalper 沒有持倉模式快取、每次即時查，但收到 `-4061` / `-1106` 後重新偵測若失敗，就**直接放棄重送**——效果跟「快取不變」一樣，這張單就沒送出去。2026-09-21 對照 r6 時改成：偵測失敗就把原本的假設反轉（原本帶 `positionSide` 改成單向寫法，反之亦然）再送一次。**沒有快取的專案也要檢查這個分支。**
  - **有快取的專案，第一次送單也是這個分支**（r10 新增）：快取過期後，下一張單送出前會先重新偵測；
    偵測被限流或逾時就直接拋錯，單根本沒送出去——不需要先被 `-4061` 拒絕。以 5 分鐘快取為例，
    每 5 分鐘就有一個時間窗，偵測 API 一失敗，掛停損、移損都會失敗。
    處理：送單用的偵測失敗時不能放棄——有舊值就用舊值，從沒偵測成功過就先假設單向；假設錯了會被拒，再依被拒單反轉。
    自檢那類「就是要知道現在模式」的查詢才該把偵測失敗當錯誤。
  - **「再用結果更新快取」的精確意思**（r10 補充）：重送**成功**才把快取寫成成功的那個假設；
    重送也失敗就清掉快取，不能留一個沒驗證過的值給下一張單用。
  - **誰踩到（pump-dump-hunter）**：2026-09-21 對照 r8 時全部都有——照 r5 原文反轉快取；快取為空時根本不反轉、
    重送又去偵測又失敗；重送前就把反轉值寫進快取；服務有背景迴圈與網頁手動操作兩條執行緒，錯誤發生後才讀快取；
    以及上面「第一次送單」那個分支。已修正，測試在修改前的程式上確認會失敗。
  - **誰踩到（gold-scalper，偵測失敗時平倉單沒送出）**（r11 新增）：沒有快取、每次即時偵測，但偵測失敗時用呼叫端給的預設值——
    預設是雙向。gold-live 是單向帳戶，平倉時就去找 LONG 那一列、找不到部位，回「沒有部位可以平」，**平倉單根本沒送出去**，
    真實部位留在交易所，程式當成已出場。連 `-4061` 都不會發生，反轉重送那段救不到。開倉那邊偵測失敗則直接假設「設定想要的模式」。
    2026-09-21 對照 r10 時改成：偵測失敗有舊值用舊值、沒有就假設單向；另外**平倉前本來就會查 positionRisk，它的列本身就說明了模式**
    （單向只有 `BOTH` 列，雙向是 `LONG`/`SHORT` 兩列），平倉改成以它為準，不依賴另外偵測。
    **另外兩邊請檢查**：平倉時如果是「先決定模式、再去部位表找對應那一列」，模式猜錯會不會變成「找不到部位」而不送單。
  - **確認成交的查詢也要用「幣＋方向」**（r12 新增）：送單後輪詢部位表確認成交時，單向模式同樣要看 `positionAmt` 的正負號。
    共用帳號裡別的專案同幣反向的部位，不能被當成自己的單已成交（否則會拿錯數量與均價去掛停損）。
  - **手動平倉也一樣：找不到自己那一側就不動作，不能退而取第一列**（r13 新增）：
    帳上有紀錄時只認「同一側」；那一側已經不在（可能剛被停損），就回錯誤、不送單、不動帳上紀錄，交給對帳。
    帳上沒紀錄的孤兒倉，雙向模式同幣兩側都有部位時分不出哪一側是孤兒，也不能自己挑。
  - **誰踩到（pump-dump-hunter）**：手動平倉找不到自己那一側時取 `cands[0]`。測試中帳上沒紀錄、雙向同幣兩側都有部位時，
    **真的把第一列（別的專案的 SHORT）平掉了**並回報「已平倉」；帳上有紀錄但那一側已不在時，會用別人那一列的數量送出平倉單。
    2026-09-21 對照 r12 時修正。
  - **誰踩到（crypto-screener，r10 兩點）**：2026-09-21 對照 r11 時——重送**之前**就把反轉值寫進快取、重送失敗也不清；
    以及上面「確認成交」那一點（r5 修對帳方向時漏了這個函式）。「第一次送單」分支沒有問題：網路錯誤回狀態 0 而不拋例外，
    偵測失敗時有舊值用舊值、沒有就單向。平倉不查部位表、直接送單，不會因模式猜錯而「找不到部位」。
  - **誰踩到（撤全部）**：gold-scalper 每次真實開倉切持倉模式被 `-4067` 擋下時，會撤掉**整個帳戶所有幣**的掛單再重試——當初 crypto-screener 的 KAS 條件單就是這樣被撤掉的。2026-09-21 改成不撤、直接沿用帳戶現有模式下單。
  - **不取第一列，但也不能「不查就送」**（r15 新增）：平倉不查部位表、直接送 reduceOnly 單，看起來避開了「取錯列」，
    但自己那一側已被停損時，單向共用帳號裡同側若還有別的專案的部位，這張 reduceOnly 單會**把別人的平掉**。
    送平倉單前要確認「這一側扣掉基準數量（第 3 條 r14）後，還有自己的部位」；沒有就不送、不動帳，交給對帳。
    平倉數量也要用「交易所這一側 − 基準」與帳上數量取小的。
  - **誰踩到（crypto-screener）**（r15 新增）：r12 時寫「平倉不查部位表，不會找不到部位」，只看了一半——
    2026-09-21 對照 r14 時由測試帶出上面的情況。
  - **誰踩到（pump-dump-hunter）**（r16 新增）：r13 已不取第一列，但「自己那一側」直接用交易所整列的數量——
    單向帳號裡交易所 140（自己 100＋別人 40）時手動平倉平掉 140；自己的 100 已被停損、只剩別人 40 時仍送出平倉單；
    對帳把「只剩別人的 40」判成「自己的數量從 100 減少到 40」。時間出場的平倉重試也會用交易所剩餘數量重送，
    把別人的部位一起平掉。2026-09-21 對照 r15 時全部改成扣基準。

## 8. 同方向的 closePosition 停損只能有一張

- **症狀**：移動停損時「先掛新的再撤舊的」被拒。
- **原因**：`closePosition=true` 的條件單同方向同時只能存在一張。
- **處理**：兩種做法二選一：
  - 用 `reduceOnly` + `quantity`（可以並存）→ 先掛新、再撤舊，沒有裸倉窗口。**建議這個。**
  - 仍用 `closePosition` → 只能先撤舊再掛新，掛不上要立刻把舊的掛回去，裸倉窗口約一秒。
- **誰踩到**：crypto-screener 原本「先掛新再撤舊」，新單被拒、舊單留著，移損到成本安靜失效好幾天
  （只寫進日誌）。改成第二種做法；保留 `closePosition` 是因為第一目標出一半後，它能自動涵蓋剩下的數量。
- **兩種做法都要**：新停損掛不上時，**記下「想要的停損價」並每輪重試直到成功**，只發一次告警不夠——
  下一根 K 棒不會再觸發同一個移損條件，錯過就永遠錯過。價格若已穿過想要的停損（`-2021`），直接出場。
  撤舊單失敗也要把舊 id 記下來，平倉時一起撤（接第 13 條）。
  pump-dump-hunter 2026-09-21 對照 r2 時發現同一個模式（走 reduceOnly 做法也會發生），已補上。
  crypto-screener 2026-09-21 對照 r4 時發現：移損失敗會把舊停損掛回去，但沒記「想要的停損價」，
  只有價格還在 1R 以上時下一輪才會再試；另外撤舊停損的結果完全沒檢查，撤不掉時仍去掛新的。
  已改成：觸發後記下想要的停損、之後每輪不看價格直接重試；撤舊失敗這輪不動、舊 id 留在帳上；
  `-2021` 直接市價出場；告警只在開始失敗與恢復時各發一次。
  gold-scalper 的 backstop（第 12 條）也是同一個模式：搬移掛不上只記警告，停損沒再移動就不會再試。2026-09-21 改成每輪對齊「想要的停損價」直到成功，告警只在開始失敗與恢復時各發一次。
- **告警節奏（r6 定案，三個專案統一）**：重試失敗期間要**定期提醒直到恢復**，不能只在開始失敗時發一次。
  停損卡住沒移動是持續累積風險的狀態，只發一次的告警很容易被後續訊息淹沒、被當成已處理。
  - 節奏：失敗第 1、5、30 次各發一次，之後每 120 次發一次（以每分鐘一輪計，約每 2 小時）；恢復時再發一次「已補上」。
    「之後每 120 次」精確定義為**第 30 次之後每隔 120 次**：150、270、390…（r8 補充：原文可讀成 120、240、360，三個專案要一致）。
    判斷式：`n in (1, 5, 30) or (n > 30 and (n - 30) % 120 == 0)`。
  - 每則提醒要帶：幣別、想要的停損價、目前實際停損價、第幾次、最後一次的錯誤訊息——看一則就知道現況，不用翻前面。
  - 這個節奏同樣適用於「沒有停損單且補掛失敗」（第 2 條）與「平倉後殘留單撤不掉」（第 13 條）這類需要人處理的持續狀態。
  - crypto-screener 與 gold-scalper 在 r5 時是「開始與恢復各一次」，**要改**。
    （r10 更正：r6 這裡原本寫「pump-dump-hunter 原本就是這個節奏」，是錯的。它當時算的是 `n % 120 == 0`，
    落點 120、240、360。2026-09-21 對照 r8 時改正。）
    crypto-screener 2026-09-21 已改（r8）：移損失敗、補掛失敗（第 2 條）、殘留單撤不掉（第 13 條）三處都套用，
    殘留單同樣放進引擎層級的待撤清單。其中補掛失敗原本是反方向的問題——**每輪都推**，持續失敗時洗版。
    gold-scalper 2026-09-21 已改（r7）：backstop 掛單/搬移失敗、以及平倉後殘留單撤不掉都套這個節奏。
    殘留單撤不掉時部位已經平了，重試狀態沒地方放在部位上，改放在引擎層級的「待撤清單」每輪重試。
  - **次數不等於時間**：節奏是以「失敗次數」計，實際間隔看各專案的重試迴圈多快。gold-scalper 每 15 秒一輪，
    120 次約 30 分鐘，不是 2 小時。各專案回報時請寫明自己的迴圈間隔，比較告警頻率時才不會誤會。
    crypto-screener 有部位、待認領送單或殘留單時每 20 秒一輪（`POSITION_POLL`），120 次約 40 分鐘；否則 60 秒一輪。
    pump-dump-hunter 固定每 60 秒一輪（`POLL_SEC`），120 次約 2 小時，第 150 次約在開始失敗後 2.5 小時。
  - **恢復通知不能用「失敗超過 1 次」判斷**（r8 新增）：第 1 次失敗已經告警過，若下一輪重試就成功，
    失敗次數仍是 1，用 `> 1` 當條件會讓「已補上」永遠不發——使用者收到「失敗」卻等不到「恢復」。
    正確條件是「曾經告警過」（第 1 次一定會告警，所以等於「失敗次數 ≥ 1」）。
    crypto-screener 的殘留單恢復通知在 r7 實作時犯了這個錯，對照 r7 重新檢查時發現並修正。
  - **判斷式對了還是會錯：計數和恢復放錯位置**（r10 新增）。檢查的不是條件式，而是「失敗在哪裡發生、狀態在哪裡被清掉」：
    1. **第一次失敗在計數之外**：例如移損的第一次失敗發生在出場判斷裡、只發一則通用告警，
       計數寫在重試函式裡——之後告警的「第 1 次」其實是第 2 次；殘留單在平倉當下撤不掉，計數從 0 開始又不告警，
       下一輪就撤掉時恢復條件（≥ 1）不成立，使用者從頭到尾不知道出過事。
       → 計數與告警放在**失敗發生的那個函式裡**（例如「掛停損」本身），不管誰呼叫、第幾次呼叫都走同一段。
    2. **恢復只寫在重試函式裡**：失敗狀態可能從別的路徑被清掉——移損失敗後，下一根 K 棒的追蹤停損從出場判斷那條路
       移損成功，失敗次數被默默歸零；補掛失敗後，下一輪發現停損其實在（上次逾時但交易所端成功），
       或補掛回報「已存在」，都直接返回。三種情況都收到了失敗告警、等不到恢復。
       → 恢復通知放在**失敗狀態被清掉的每一個地方**，最簡單的做法是跟計數放在同一個函式裡。
  - **已成交的動作先通知，再做後續可能失敗的步驟**（r10 新增）：例如「到 1R 減碼一半、停損移到成本」，
    減碼是市價單、已經成交；如果通知寫在移損之後，移損一失敗通知就不會發，部位少了一半你卻不知道。
  - **成交後的例外處理不能改寫「已成交」**（r11 新增，上一點的延伸）：送單和後續步驟（記錄滑點、寫資料庫、掛停損）
    包在同一個 `try` 裡時，後續任何一步丟例外，`except` 會把整筆當成送單失敗處理——通知變成「下單失敗」，
    更嚴重的是若 `except` 裡會把「是否真的開倉」記成否，之後出場時就不會送真實平倉單，**交易所留下孤兒倉（第 3 條）**。
    → 送單成功就立刻記一個「已成交」旗標，`except` 先看旗標：已成交只記錄錯誤、不改成交紀錄。
  - **誰踩到（pump-dump-hunter）**：2026-09-21 對照 r8 時，節奏、上面兩種錯法、減碼通知全部都有，已修正。
  - **誰踩到（gold-scalper）**（r11 新增）：對照 r10 時：節奏落點是 `n % 120`（120、240）；掛 backstop 時丟例外不計數；
    搬移時舊單撤不掉要等平倉才開始計數；backstop 失敗中部位被平掉，失敗狀態消失卻沒有收尾通知。
    以及上面「成交後的例外處理」——開倉那段 `except` 會把 `real_open_executed` 改成否，是真的會留孤兒倉的寫法。
    已修正，`tests/test_lessons.py` 在修改前的程式上 12 項有 9 項失敗（另外 3 項是前幾版已修好的回歸項）。
  - **平倉單送出後一定要看結果**（r12 新增，上一點的反面）：上一點是「已成交被當成失敗」，這一點是「失敗被當成已成交」。
    平倉市價單被拒（保證金、數量、模式參數、網路）時若照樣記成已平倉，接著撤掉這個部位的停損與停利——
    真實部位留在交易所，而且**沒有任何保護**。
    → 平倉單回應非成功時，再查一次部位：交易所上確實沒有了（例如同時被停損觸發）才記帳；還在就保留部位與停損、告警。
    數量不符被拒（出一部分後帳上沒更新、雙向模式不能超量）時，改用交易所上的實際數量重送一次。
    **補充（r13）**：
    - 「再查一次部位」本身也會失敗，查不到不能當成「已經沒了」（第 2 條）→ 當成沒平掉。
    - 回應逾時／5xx 但交易所其實成交了——這是最常見的「看起來失敗、其實成功」，查部位就會知道，不能直接告警「平倉失敗」。
    - **帳上紀錄要等確認平掉之後才移除**。先從帳上拿掉再送平倉單，被拒時部位就從程式眼中消失——停損沒撤、但也沒人管了。
    - 沒平掉的部位記一個「待平倉」旗標，**每輪重試**，照第 8 條的節奏告警；等待期間停損守衛照常運作。
      不能只靠「下一根 K 棒再觸發一次出場條件」——5 分 K 的策略要 5 分鐘才會再試。
    - **交易所端的停損單也要等確認平掉之後才撤**（r14 新增）：跟帳上紀錄同一個道理。先撤停損、再送平倉單，
      被拒時部位還在、停損已經沒了——剛好是最需要保護的時候。順序是：送平倉單 → 確認這一側沒了 → 撤停損 → 結帳。
      「平倉前先撤停損，順便看它是不是已經觸發」的寫法要改成：平倉單失敗時查部位，沒了才去撤（撤單回「不存在」＝已觸發）。
    - **高頻觸發的出場路徑在待平倉期間要停下來**（r14 新增）：用即時報價（bookTicker 等）觸發停損的專案，
      平倉失敗後若不擋，每一筆報價都會再送一次平倉單。待平倉期間只讓「每輪重試」那條路送單。
  - **誰踩到（pump-dump-hunter）**（r13 新增）：手動平倉先把部位從帳上移除再送單；時間出場／穿價出場／停損掛不上的緊急平倉
    都不看成交數量；緊急平倉成功時連平倉紀錄都沒寫。另外撤舊停損失敗時只記進清單、要等平倉才開始計數告警（同 gold-scalper r11），
    部位平倉時失敗狀態無聲消失、補掛連續次數以幣名為鍵跨部位殘留（同 crypto-screener r12），
    成交後寫紀錄丟例外會一路拋到迴圈外層、「✅下單」通知消失。2026-09-21 對照 r12 時修正，
    `tests/test_r12.py` 在修改前的程式上 26 項失敗，另外 5 項逐一確認：4 項程式本來就對，1 項（-1000）碰巧對、原因不對。
  - **減碼由交易所端的停利單執行時**（r12 新增）：程式不會收到成交回報，只能在對帳時發現「數量變少了」。
    不偵測的話，不只沒有通知，**損益也會算錯**——平倉時用「全部數量 × 最後出場價」：
    出一半後追蹤到更高價出場會高估，出一半後打平出場會低估。
    → 對帳時比對交易所數量與帳上數量，變少就記一筆部分出場、立即通知、更新帳上數量；平倉損益改成各次出場加總。
    **沒有交易所停利單的專案也要做**（r13 補充）：在交易所 App 手動減碼、或 ADL 自動減倉，一樣會讓帳實不符；
    不偵測的話，之後的減碼單、停損單都用帳上的舊數量送出。pump-dump-hunter 沒有交易所停利單，2026-09-21 仍補上偵測。
  - **誰踩到（crypto-screener）**（r12 新增）：2026-09-21 對照 r11 時，r10、r11 的每一點都有——
    反向訊號路徑呼叫移損失敗時第 1 次被計數但沒告警；殘留單第 1 次告警只附在背景對帳的平倉通知裡，
    手動平倉、守衛強平、移損穿價出場三條路徑都收不到；補掛回報「已存在」時沒發恢復也沒歸零；
    部位平倉時移損與補掛的失敗狀態消失、沒有收尾通知，補掛次數還留在記憶體裡，同幣下次進場會接著數；
    進場成交後任何步驟丟例外，通知都寫「下單失敗」。另外**五張平倉市價單都不看結果**（上面「平倉單送出後一定要看結果」），
    以及交易所端「2R 出一半」從未被偵測（上面「減碼由交易所端執行」）——正式網第一筆 AVAX 帳上 +3.32R，實際約 +2.61R。
    改成所有通知都在失敗發生的函式裡產生、背景迴圈統一送出。`tests/test_r11.py` 在修改前的程式上 23 項有 18 項失敗；
    另外 5 項逐一確認過是程式本來就對。**已記錄的歷史交易無法自動更正**，累積的期望值帶有上述偏差。
  - **誰踩到（gold-scalper）**（r14 新增）：2026-09-21 對照 r13 時，平倉順序完全倒過來——先把帳上紀錄結掉、
    從記憶體移除、撤掉交易所停損，**最後才送平倉單**；被拒時只發一則「同步平倉失敗」，部位從程式眼中消失、而且沒有停損。
    改成上面的順序，平倉失敗保留部位與停損、每輪重試、照節奏告警；手動強制平倉被拒時回報失敗而不是「已平倉」。
    另外補上交易所數量比對（每 4 輪約 60 秒一次，第 6 條限流）：變少記部分出場並通知，連續 3 輪為 0 走平倉流程確認。
    `tests/test_lessons.py` 在修改前的程式上 25 項有 8 項失敗。
  - **「先撤停損」不一定寫在平倉函式裡**（r15 新增）：移損的流程本身就是「撤舊 → 掛新」（第 8 條第二種做法）。
    掛新停損被拒 `-2021`（價格已穿過）時若直接送平倉單，舊停損已經為了「移動」撤掉了——送單那一刻交易所上沒有任何停損。
    → `-2021` 時先把舊停損掛回去，再送平倉單；平掉了才撤，沒平掉就記待平倉。
    檢查時要看**所有會送平倉單的路徑**在送單那一刻交易所上有沒有停損，不只看「平倉函式」的順序。
  - **誰踩到（crypto-screener）**（r15 新增）：2026-09-21 對照 r14 時——平倉失敗只告警一次、沒有每輪重試；
    上面移損 `-2021` 的路徑；待平倉期間守衛強平與移損出場仍可能各自送單。手動平倉的順序本來就對。
    `tests/test_r14.py` 在修改前的程式上 19 項有 16 項失敗，另外 3 項逐一確認是程式本來就對。
  - **停損守衛補掛遇 `-2021` 也是一條出場路徑**（r16 新增）：停損不見了、補掛時價格已穿過停損價——停損本來就該觸發了。
    只告警「補掛失敗」的話，部位沒有停損、價格又在錯的那一邊，要等人處理。→ 直接走平倉流程（一樣看結果、失敗就待平倉）。
    這條路徑送單那一刻交易所上**本來就沒有停損**（正是因為不見了才要補），沒辦法先掛回去；能做的是盡快出場。
  - **誰踩到（pump-dump-hunter）**（r16 新增）：守衛補掛遇 `-2021` 只告警。另外逐條檢查了 r14、r15 的平倉順序：
    交易所停損在確認平掉之後才撤、待平倉期間只有每輪重試那條路送單、移損是「先掛新再撤舊」所以 `-2021` 時舊停損還在，
    這三項 r13 就是對的。
  - **誰踩到（gold-scalper）**（r17 新增）：**根本沒有停損守衛**——交易所上的停損（backstop）在 App 被撤掉、或因其他原因消失，
    程式完全不會發現，只有在停損價要移動時才會碰到它。2026-09-21 對照 r16 時補上守衛：每 4 輪（約 60 秒）
    帶 symbol 查掛單（權重 1），查詢失敗不動作、連續 3 輪確認不在才當場補掛，補掛遇 `-2021` 直接走平倉流程。
    Algo 查詢回 404 時改查舊端點（每次查詢當下判斷、不存旗標，所以重啟後也一樣）。
    **另外兩邊請檢查**：守衛是不是每個部位都會跑到，還是只在某些條件下（例如移損時）才碰到停損單。
  - **守衛有寫、但不一定每輪都跑到**（r18 新增）：背景迴圈常寫成一個 `try` 包住「對帳 → 移損 → 守衛 → 重試 → 送出告警」。
    前面任何一步丟例外，**這一輪的守衛就不跑、告警也送不出去**；如果是某筆部位資料造成的持續性例外，守衛**每一輪都不跑**。
    守衛自己的迴圈也一樣：某個部位丟例外，排在後面的部位整輪不檢查。
    → 迴圈的每一步各自 `try`；守衛對每個部位各自 `try`，出錯的部位照節奏告警（第 14 條：不能被 try 吞掉）；
    送出告警放在 `finally`。測試時讓前面的步驟丟例外，確認守衛與告警照樣執行；讓出錯的部位**排在前面**。
  - **誰踩到（crypto-screener）**（r18 新增）：2026-09-21 對照 r17 時——上面兩處都是單一 `try`；守衛補掛遇 `-2021` 只告警
    （同 pump-dump-hunter r16）。守衛本身每輪對所有部位都跑，沒有 gold-scalper 那種「只有移損時才碰到」的問題。
    `tests/test_r17.py` 在修改前的程式上 12 項全部失敗。
  - **「每個部位各自 try」還不夠，同一個部位的每一步也要各自 try**（r19 新增）：守衛迴圈常寫成
    對每個部位 `try: 重試平倉 → 重試移損 → 補掛停損 → 出場判斷`。部位之間互不影響了，但同一個部位裡，
    前面的重試一出錯，**這個部位的停損守衛就跳過**——偏偏會出錯的部位最需要守衛。→ 每一步各自 try。
  - **出錯次數也要跟著部位結束清掉**：跟補掛連續次數（第 8 條 r12）同一個道理，以幣名為鍵的出錯計數不清，
    同幣下一筆進場會接著數、節奏錯位。
  - **誰踩到（pump-dump-hunter）**（r19 新增）：2026-09-21 對照 r18 時——背景迴圈外層一個大 try（掃描器出錯，整輪對帳和守衛都不跑），
    對帳和出場管理又共用一個 try；守衛迴圈每個部位各自 try，但部位內四步共用一個，而且錯誤只寫錯誤區、不推播。
    改成背景迴圈拆出可單獨呼叫的一輪 `tick()`、四步各自 try；部位內三段各自 try；出錯照節奏推播、恢復時通知。
    `tests/test_r18.py` 在修改前的程式上 16 項失敗。
  - **維護步驟出錯，也不能讓出場判斷跳過**（r20 新增）：上面講的是「前面出錯、守衛跳過」；反過來也一樣——
    如果每輪流程是「維護（對帳、守衛）→ 訊號與出場判斷」，維護丟例外時**整輪中止，程式內的停損判斷也不跑**。
    程式內停損是最主要的保護，比守衛更不能被擋。→ 維護整段包 try，出錯照節奏推播，流程繼續走到出場判斷。
  - **誰踩到（gold-scalper）**（r20 新增）：每輪開頭「撤殘留單 → 維護（認領 → 平倉重試 → 數量比對 → 守衛）」沒有任何 try，
    任何一步丟例外，後面的守衛跳過、而且整輪 tick 中止，連出場判斷都不跑。2026-09-21 對照 r19 時改成每一步各自 try，
    出錯照節奏推播、恢復時通知；以步驟名為鍵的出錯次數在部位結束時清掉並發收尾通知。
    `tests/test_lessons.py` 在修改前的程式上 44 項有 9 項失敗。
  - **通知也是一步**（r21 新增）：「對帳記了平倉 → 推播平倉通知」這種寫在對帳後面、沒有自己 try 的通知，
    一出錯（格式化時某個欄位是 None、網路例外）就中斷整輪——後面的守衛與待平倉重試（出場）都不跑。通知要跟其他步驟一樣各自 try。
  - **錯誤收進「事件」也算吞掉**：迴圈逐部位 `try`，但 except 只把錯誤包成一筆事件回傳、呼叫端只寫一行錯誤區——
    隔離做對了，錯誤卻沒人看到（第 14 條）。出錯要照節奏推播。
  - **誰踩到（crypto-screener）**（r21 新增）：2026-09-21 對照 r20 時——平倉通知與認領通知沒有自己的 try（平倉通知出錯 → 該輪守衛、
    待平倉重試全部跳過）；移損逐部位 try 但錯誤只進事件；待平倉重試、殘留單重試、對帳的兩個逐部位迴圈沒有 try
    （殘留單那個還會在出錯時把該張從清單丟掉）；背景迴圈步驟與守衛的出錯次數恢復時不歸零、部位結束時不清。
    改成共用的「部位步驟出錯」計數（步驟＋幣為鍵、照節奏推播、恢復通知、部位結束時清掉並收尾）。
    `tests/test_r20.py` 在修改前的程式上 11 項全部失敗。
  - **包 try 的時候，except 不能把「已經做完的動作」當成沒做**（r22 新增）：對帳逐部位包 try 之後，如果一筆的流程是
    「結帳（寫平倉紀錄、撤停損）→ 發通知」，通知出錯被同一個 try 接住，except 照「保守保留在帳上」處理——
    部位已經結帳、停損已經撤了，卻又被放回帳上，下一輪再結一次帳。→ 通知要自己包 try（讓組字串也在 try 裡），
    不能跟不可逆的動作共用一個 except。
  - **except 裡組告警不能再用壞掉的那筆資料**（r22 新增）：逐筆重試的 except 寫 `f"{x['symbol']} 撤不掉"`，
    剛好就是 `x['symbol']` 壞掉時進到這裡——except 裡再拋一次 `KeyError`，整段中止，後面每一筆都不處理。
    告警訊息一律用 `.get()` 組；壞掉的那筆留在清單、照節奏推播，不能被丟掉。
  - **誰踩到（pump-dump-hunter）**（r22 新增）：2026-09-21 對照 r21 時——對帳的兩個逐部位迴圈沒有 try（同 crypto-screener）；
    `telegram.send` 本身有 try，但訊息是呼叫前就組好的，格式化出錯照樣拋；殘留單重撤是上面「except 裡再拋一次」；
    整段重撤出錯只寫日誌、不推播。背景迴圈、守衛逐步驟 try 與出錯次數的清理 r19 已做。
    `tests/test_r21.py` 在修改前的程式上 15 項失敗。
  - **反過來也一樣：結帳之前出錯，不能把部位弄丟**（r23 新增）：常見寫法是先把部位從「持倉中」取走（避免兩條路同時平倉），
    再送平倉單、確認、結帳。取走之後、結帳之前任何一步丟例外（例如確認查詢本身拋錯），例外往外傳、部位也沒放回——
    **程式就忘了這個部位**，交易所上的倉沒人管，之後還可能再開新倉。
    → 以「結帳」為界包一層：界線之前出錯＝放回帳上、記待平倉、每輪重試；界線之後出錯＝不可逆、只推播。
    用「外層包裝函式＋原函式改名」做，原函式內容不用重新縮排（第 14 條 r22）。
  - **背景執行緒裡的例外沒人接**（r23 新增）：「另開執行緒送平倉單」這類 fire-and-forget 寫法，執行緒裡丟的例外
    不會回到主迴圈，不計數、不推播，只在標準錯誤印一行。執行緒的進入點要包一層，跟其他步驟一樣照節奏推播。
  - **誰踩到（gold-scalper）**（r23 新增）：2026-09-21 對照 r22 時——平倉沒有外層保護，確認前出錯部位就被弄丟、
    確認後出錯例外往外拋；快速停損（即時報價觸發）在背景執行緒送平倉，出錯沒人接；殘留單重撤沒有逐筆 try，
    告警用 `item['symbol']`（r22 說的「except 裡再拋一次」的前身）。`exit_price` 缺值時算損益會拋錯，改成用最新價補。
    `tests/test_lessons.py` 在修改前的程式上 51 項有 7 項失敗（另有 4 項只因為新增的「回傳結果」而失敗，
    拿掉那條斷言後在修改前的程式上成立）。
  - **結帳本身要排在最前面，算損益不能丟例外**（r24 新增，r22 那點的另一種樣子）：結帳函式常寫成
    「撤剩下的條件單 → 發收尾通知 → 算損益 → 寫紀錄、移出帳」。撤單是不可逆的；後面的通知或算損益一丟例外
    （某個欄位缺了、格式化時型別不對），條件單已撤、部位卻還在帳上——下一輪對帳又結一次、又丟一次，**這筆永遠結不了帳**。
    → 順序改成「算平倉紀錄（只用 `.get()`，缺欄位時損益記為未知）→ 寫紀錄、移出帳、存檔（界線）→ 撤單、通知、統計各自 try」。
  - **網頁請求也是另一條執行緒**（r24 新增，r23「背景執行緒」的另一種樣子）：手動開倉、平倉從網頁送出時，
    請求處理本身就在另一條執行緒；例外穿出去，網頁只看到連線中斷，沒有推播、使用者要平倉的意圖也沒留下。
    交易操作的入口要包一層：回錯誤給網頁、照節奏推播；平倉在結帳前出錯就記待平倉。
  - **誰踩到（crypto-screener）**（r24 新增）：2026-09-21 對照 r23 時——結帳是上面「撤單在前、結帳在後」的順序；
    手動平倉沒有外層保護（crypto-screener 不是「先取走部位」的寫法，部位不會弄丟，但例外往外傳、待平倉沒記）；
    監控執行緒（會自動下單）出錯只寫錯誤區不推播；暖機執行緒沒包；網頁交易操作沒包。except 裡組訊息有一處用 `o["type"]`。
    `tests/test_r23.py` 在修改前的程式上 12 項有 8 項失敗，另外 4 項確認是程式本來就對。
  - **誰踩到（pump-dump-hunter）**（r25 新增）：2026-09-21 對照 r24 時——
    結帳是「撤停損 → 收尾通知（`:.6g` 格式化）→ 算損益 → 寫紀錄」，資料型別不對時通知一拋，停損撤了、紀錄沒寫；
    對帳逐部位的 try（r22）接住後把它「保守保留在帳上」，下一輪再結、再拋——正是 r24 說的「永遠結不了帳」。
    平倉函式在交易所已經平掉之後用 `pos["entry"]` 算損益，缺欄位就拋。背景執行緒是 `while True: tick()`，
    `tick()` 裡每一步都有 try，但 `tick()` 本身出錯時整條執行緒結束、機器人停止運作，只在標準錯誤印一行。
    網頁的手動平倉有 try，但只把錯誤回給網頁：不推播、結帳前出錯也不記待平倉。
    pump-dump-hunter 不是「先取走部位」的寫法（平倉期間部位一直留在帳上，確認平掉才移出），r23 那點本來就對。
    修正：結帳改成「算紀錄（只用 `.get()`）→ 寫紀錄、移出帳（界線）→ 撤單、通知各自 try」；
    背景執行緒與網頁交易入口都照「外層包裝＋原函式改名」包一層。`tests/test_r24.py` 在修改前的程式上 17 項失敗。
  - **通知的格式化，測試常常根本沒跑到**（r26 新增）：交易流程的測試通常把「送通知」整個 mock 掉，
    所以「損益是 None 時 `:.2f` 會拋」這種錯，所有測試都通過、上線才炸。→ 另寫一項不 mock 通知本身、只攔最底層送出的測試，
    餵缺欄位的資料，確認訊息真的組得出來。
  - **誰踩到（gold-scalper）**（r26 新增）：2026-09-21 對照 r25 時——確認平掉之後是「撤停損 → 出錯計數收尾 → 算損益 → 寫紀錄 → 移出帳」，
    撤單在前、算損益會拋；網頁的手動測試下單、測試平倉、設定槓桿、取消掛單四個入口沒有保護（強制平倉走引擎的平倉包裝，本來就有）；
    背景迴圈 `tick()` 出錯不會讓執行緒結束，但只寫日誌不推播；平倉通知在損益未知時格式化會拋（上一點）。
    改成「算紀錄（失敗時損益記未知）→ 寫紀錄、移出帳（界線）→ 撤停損、收尾各自 try」；網頁入口用裝飾器包一層
    （`functools.wraps` 保留參數，FastAPI 讀得到）。`tests/test_lessons.py` 在修改前的程式上 58 項有 7 項失敗。
  - **`.get(鍵, 預設值)` 擋不住「鍵存在、值是 None」**（r27 新增）：r22 說「組告警用 `.get()`」，但 `t.get("opened", 0)`
    在紀錄裡有 `"opened": None` 時回的是 None，不是 0——接著相減照樣拋。r24 起結帳遇缺欄位會把值記成 None，這種紀錄一定會出現。
    → 數字先確認型別（`isinstance(v, (int, float))`）再計算或格式化；`.get(k) or 預設` 只適用於 0 也當成缺值的欄位。
  - **統計也要處理「損益未知」**（r27 新增）：勝率、期望值常寫成 `(x["pnl"] or 0) > 0` 分勝負——損益未知被算成**虧損**，
    勝率被拉低、期望值偏差。→ 損益未知的交易不算勝負，另外計數，並在通知與畫面上講明「另有 N 筆損益未知」。
  - **誰踩到（crypto-screener）**（r27 新增）：2026-09-21 對照 r26 時——平倉通知用 `t.get("closed", 0) - t.get("opened", 0)`
    （上面那點）、損益未知時標題寫「虧損平倉」；結帳時資料壞掉的端到端情境裡，使用者收到的是「⚠ 平倉通知出錯」而不是平倉通知。
    統計與分版本統計都把損益未知算成虧損；網頁把損益未知顯示成「+0.00 U」。背景迴圈外層出錯只寫錯誤區（r26 gold-scalper 同）。
    `tests/test_r26.py` 在修改前的程式上 9 項有 6 項失敗（另 3 項測的是測試基礎設施本身）。
  - **「估算」也不能把未知包裝成已知**（r28 新增）：成交價查不到時，常見寫法是退而用訊號價估算損益——
    算出來的是一個看起來確定的數字，但訊號價不是這張單的實際成交價，那個「損益」是假的已知。
    → 估算只用實際成交價；查不到就記未知，實際損益交給交易所的成交明細。成交明細查得到時以它為準，那是對的。
  - **`or 預設` 在時間戳、計數這類欄位上**（r28 補充 r27）：改寫 `.get(k, 預設)` 時很容易順手寫成 `.get(k) or 預設`——
    0 被當成缺值。pump-dump-hunter 對照 r27 時，讀完這條、改程式時照樣犯了一次（K 棒時間戳 `last_t` 是 0 時被換成預設值），
    是舊測試失敗才發現。→ 值是 None 才用預設，一律明寫 `if v is None`。
  - **誰踩到（pump-dump-hunter）**（r28 新增）：2026-09-21 對照 r27 時——偵測到數量減少時用 `rec.get("fill", rec["entry"])` 算損益，
    成交價是 None 時照樣拿 None 相減，**整個對帳那一筆出錯**；出場判斷用 `pos.get("last_t", start)`，`last_t` 是 None 時
    **每根 K 棒都拋錯、這個部位的出場判斷整個停擺**；部分出場算不出損益時寫 `pnl=est or 0`，最後整筆損益用估算加總時
    未知那段被當成 0，通知寫「損益 +0.00 U」（crypto-screener 網頁顯示的同一個症狀）。沒有勝率、期望值這類即時統計；
    已平倉表遇到未知原本顯示空白，改成顯示「未知」並在標題講明筆數。通知本來就是攔在最底層的 `telegram.send`
    （它只收組好的字串），格式化在測試裡一直有執行到，這點本來就對。`tests/test_r27.py` 在修改前的程式上 14 項失敗。
  - **風控遇到損益未知**（r29 新增）：每日虧損上限、連續虧損上限這類斷路器，把未知當 0 會低估虧損、當虧損會誤觸發。
    兩者都不對——不算進去，但**把筆數放進風控狀態**（網頁與推播看得到「另有 N 筆損益未知」），交給人判斷。
    這表示斷路器在有未知筆數時可能該觸發而沒觸發；未知筆數多時要人工查交易所成交明細。
  - **誰踩到（gold-scalper）**（r29 新增）：2026-09-21 對照 r28 時——r26 起結帳遇缺欄位會把損益記成未知，但下游全都沒跟上：
    勝率分母包含未知（等於算成虧損）；風控每日損益 `or 0.0` 當 0、連續虧損當虧損；**網頁成交清單對未知呼叫 `toFixed`，整份清單畫不出來**；
    平倉通知在真實成交價查不到時用「模擬點數 × 張數」寫成「≈ X USDT」；**網頁總損益的 USDT 是全部模擬點數 × 張數**，
    連沒真的下過單的模擬交易都算進去；App 手動減碼／ADL 用標記價估損益。全部改成只用真實成交價、查不到記未知並講明筆數。
    `tests/test_lessons.py` 在修改前的程式上 65 項有 6 項失敗。
  - **出場價「標記價，查不到再用進場價」＝損益剛好 0**（r30 新增，r28「估算」的更隱蔽版本）：它不是寫在「估算」分支裡，
    而是每一條出場路徑的一行 `px = mark_price(sym) or pos["entry"]`。標記價是偵測當下的價格、不是成交價；
    標記價查不到時退回進場價，損益就算成 0——通知寫「+0.00 U」、統計算成打平，完全看不出是假的。
    → 出場價只用實際成交價：自己送的平倉單看回應裡的成交均價；交易所端觸發的停損停利查成交明細（`userTrades`）；都沒有就記未知。
    部分出場也一樣（停利**觸發價**也不是成交價）。有基準部位（同側有別人的倉）時，成交明細分不出哪幾筆是自己的，記未知。
  - **成交明細的時間界線要用「已採用的最後一筆」，不是「偵測當下」**（r30 新增）：部分出場後，之後的出場價只該看那之後的成交。
    用偵測當下的時間當界線，前一段那筆成交的時間戳可能落在同一毫秒內，最後出場時又被算進去——出場均價被拉偏。
  - **風控遇到損益未知的另一個地方：冷卻**（r30 補充 r29）：「獲利後短冷卻、虧損後長冷卻」這類規則，未知被記成「沒贏」就套了
    虧損的冷卻、訊息寫「剛虧損平倉」。crypto-screener 的選擇：未知套較長的冷卻（只是等久一點，不影響統計），但訊息寫「損益未知」。
  - **誰踩到（crypto-screener）**（r30 新增）：2026-09-21 對照 r29 時——7 條出場路徑全部是上面那行（手動平倉、待平倉重試、
    對帳偵測、守衛兩條、移損穿價）；部分出場用停利觸發價或標記價，查不到時算出一個數字；結帳把部分出場的未知損益 `or 0` 加總；
    每日虧損上限本來就不加未知（R 倍數是 None），但沒有記筆數；冷卻把未知當虧損。自檢的 pending 逾時用 `p.get("ts") or now`，
    時間戳是 0 時被當成「剛送出」，逾時的 pending 被藏起來。`tests/test_r29.py` 在修改前的程式上 9 項有 7 項失敗
    （另 2 項：通知格式化 r26 起就沒被 mock、錯誤攔截在 `sys.stderr` 這一層涵蓋所有模組，本來就對）。
  - **平倉成交不能用 `realizedPnl ≠ 0` 篩**（r31 新增）：從成交明細挑「這筆是不是平倉」時，寫成 `realizedPnl != 0` 很直覺——
    開倉那筆的 realizedPnl 是 0。但**打平出場**（例如移到成本後在成本價被停損）那筆平倉的 realizedPnl 也剛好是 0，整筆被濾掉，
    出場價與損益變成未知、或退到估算。→ 用方向判斷（多單的平倉是 SELL），不看 realizedPnl。
  - **成交明細的界線用成交 id**（r31 補充 r30）：「已採用的最後一筆」用 id 記，不用時間——同一毫秒內可能有好幾筆成交。
  - **誰踩到（pump-dump-hunter）**（r31 新增）：2026-09-21 對照 r30 時——完整平倉的六條路徑（手動、時間、兩種穿價、緊急平倉、待平倉重試）
    都用平倉單回應的成交均價、查不到記未知，**本來就對**；交易所停損觸發那條走成交明細，也對。但：
    偵測到數量減少那條**正是 r30 那一行** `px = markPrice or entryPrice`（r28 只改了參考價那一端）；1R 減碼是程式自己送的市價單，
    回應沒讀、也沒記成部分出場，成交明細查不到時最後的估算只算剩下那一半，卻呈現成已知；成交明細用上面那個 `realizedPnl ≠ 0` 篩；
    界線是開倉時間，最後的出場價被前一段拉偏；有基準部位時也沒記未知。pending 時間戳是 None 時每輪拋錯、永遠卡著。
    沒有依輸贏調整的冷卻，也沒有每日虧損、連續虧損這類斷路器。改成共用的 `adopt_partial()`／`close_info()`：
    界線用最後採用的成交 id、方向判斷平倉成交、有基準記未知、各段損益加總（任一段未知整筆未知）。
    `tests/test_r30.py` 在修改前的程式上 10 項失敗。

## 9. K 線最後一根是「進行中」的

- **症狀**：實盤訊號跟回測對不上；條件在剛開盤幾秒的空棒上判斷，量能類條件幾乎不可能成立。
- **原因**：`/fapi/v1/klines` 回傳的最後一根是未收盤的棒。
- **處理**：過濾掉 `開盤時間 + 週期 > 現在` 的棒；輪詢對齊整分後 2 秒，棒一收完就判斷。
- **不適用的情況**：crypto-screener 不用幣安 klines，訊號來自 CoinGecko `market_chart` 的時間點快照，量能是 24 小時滾動量，沒有「空棒」問題。但它的 60 分均線把最後一個即時價當一根算，三刀流狀態可能在一小時內翻動——這是既有行為，改動等於換策略，要走策略版本分桶。

## 10. 伺服器時間是 UTC，判讀會差 8 小時

- **症狀**：以為一筆單掛了 8 小時，其實才 2 小時。
- **原因**：容器預設 UTC。
- **處理**：程式啟動最前面 `os.environ["TZ"]="CST-8"; time.tzset()`（POSIX 寫法，不需要 tzdata）。例外：日 K 的日期是 UTC 切日，那個欄位保持 UTC 才不會標錯天。
- **先判斷架構**：如果後端一律存 epoch 毫秒或帶時區的 ISO 字串、前端用 `toLocale*` 轉本地時間（gold-scalper、crypto-screener 都是），就不需要設 `TZ`。這種架構下殘留的 `gmtime()` 多半是刻意的 UTC 切日（例如每日下單上限），不要改。

## 11. 回測有的出場規則，實盤要一條不漏

- **症狀**：實盤只掛初始停損，減碼、保本、追蹤、時間出場、冷卻全沒做，實盤結果跟回測完全不是同一套策略。
- **處理**：實盤出場管理逐根收盤棒重現回測邏輯，並寫一支一致性測試：同一組 K 線丟給回測和實盤管理，**出場原因與出場根數必須完全一致**。改其中一邊就跑這支測試。
- **沒有回測引擎的專案**：一致性測試改鎖「同一組數字被算了好幾次」的地方。crypto-screener 的影子追蹤曾把目標寫死成 2R，實盤用的是可調的 `tp1R`——改參數後兩邊就會脫鉤。
- **誰踩到**：pump-dump-hunter，2026-09-21 才補上。

## 12. 沒掛交易所停損時，服務中斷 = 倉位裸奔

- **症狀**：策略本身邏輯正常（回測、實盤判斷都對），但停損/移動停損完全是「程式內比對現價」實作，沒有在交易所掛任何條件單。服務重啟、斷線、或主機掛掉的那段時間，部位在交易所端沒有任何保護，可能大幅虧損才被發現。
- **原因**：一開始圖簡單，出場邏輯只靠程式自己算 `sl_price` 再比對現價、觸發後送市價單；這樣做行情判斷靈活（可以疊 ATR 動態停損、追蹤停損、結構停利），但沒有交易所端的備援。
- **處理**：真實開倉成功後，額外在交易所掛一張 `reduceOnly` 的 `STOP_MARKET` 條件單當「backstop」，觸發價用當下的 `sl_price`；程式內的判斷仍是主要出場依據，backstop 只在服務掛掉時墊底。三個時機都要處理：
  1. **移動停損更新時**：backstop 要跟著搬——用第 8 條建議的「先掛新的、再撤舊的」，不要先撤再掛，不然搬動的瞬間裸奔。
  2. **平倉時**（不管出場原因是程式判斷、手動平倉、還是任何路徑）：一定要撤掉這張 backstop，否則留下一個沒有對應部位的孤兒 reduceOnly 掛單。
  3. **平倉前要先確認 backstop 是否已經自己觸發**：撤單時如果發現這張單已經不存在（查無此單），代表它已經先一步把部位平掉了（例如服務中斷過一段時間）——這時不能再送一次真實平倉單（交易所根本沒部位可平），改成查這張單的實際成交價，讓損益紀錄準確。
  掛不上 backstop 不該讓這筆交易整個失敗——它是保險，不是主要防線，掛不上就記警告、繼續走原本流程。
- **誰踩到**：gold-scalper 原本完全沒有交易所停損，2026-09-21 補上。
- **不適用**：crypto-screener 的主要出場本來就是交易所條件單（停損、停利、移動停利），不需要另掛 backstop；但它踩到的是這條第 2 點的反面，見第 13 條。

## 13. 一張條件單觸發平倉後，同一部位的其他條件單會留下來

- **症狀**：部位已經平了，交易所上還掛著它的停利、移動停利或停損。下次同一個幣再進場時，
  新的 `closePosition` 停損被拒（第 8 條：同方向只能一張），接著觸發「沒有停損就不留倉」而立刻平倉；
  共用帳號時，殘留的 reduce-only 單還可能動到別的專案同一個幣的倉（第 7 條）。
- **原因**：出場是交易所端的條件單自己觸發的（停損、停利、移動停利），程式只在對帳時發現「部位不見了」，
  記了帳卻沒去撤剩下的單。
- **哪一型會留下（實測，未經幣安文件確認）**：`closePosition=true` 的停損在部位歸零後**查不到**，
  推測幣安會自動取消；帶 `quantity` 的 `reduceOnly` 單（部分停利、移動停利）則會留下。
  所以停損觸發後，停利與移動停利是主要的孤兒來源。不要依賴自動取消，一律自己撤。
- **處理**：
  - 每張條件單的 `algoId` / `orderId` 都記在部位上；**任何**平倉路徑（交易所觸發、手動、守衛）
    記帳時一律用記錄的 id 精準撤剩下的單。
  - 撤單回 `-2011 Unknown order`（已觸發或已撤）視為正常，不是錯誤；其他失敗要推播，請人手動處理。
  - 自檢加一項「孤兒條件單」：查全部掛單（不帶 symbol，權重 40，只在自檢時查），列出沒有對應部位的。
  - 第 12 條第 2 點是同一件事的另一面：那條講「程式平倉要撤 backstop」，這條講「交易所平倉要撤其他單」。
- **誰踩到**：crypto-screener，2026-09-21 自檢在模擬網查到 2 張沒有對應部位的條件單（共用帳號，來源未確認），已撤。
  正式網第一筆 AVAX 移動停利出場後，原本推測成本價停損會殘留，自檢查證**沒有**——這也是上面「closePosition 會被自動取消」推測的依據。
- **其他兩個專案要檢查**：pump-dump-hunter 若同時掛了停損與停利，停損觸發後停利是否有撤；
  gold-scalper 的 backstop 若自己觸發（第 12 條第 3 點），同一部位是否還有其他交易所掛單。

## 14. 改寫一個函式時，把旁邊的東西一起刪掉

- **症狀**：某個保護機制（撤單、停損守衛）看起來在運作，實際上每次都拋 `NameError`，被外層 try 吞掉，
  變成「撤單失敗」「補掛失敗」這種看起來像交易所問題的告警，或只在錯誤區留一行。
- **原因**：用「從 A 函式開頭取代到 B 函式開頭」的方式整段改寫時，夾在中間的小函式或模組層級宣告被一起刪掉。
  守衛類程式通常包在 try 裡（這是對的），所以程式錯誤跟交易所錯誤被一視同仁地處理。
- **處理**：
  - 改寫後、部署前跑靜態檢查：`python -m pyflakes app/`（或該專案的程式目錄）。**有 `undefined name` 就不能部署。**
  - 行為測試的斷言要檢查「失敗的原因」，不只是「有沒有失敗」：例如測「撤單失敗會告警」，要確認告警裡的錯誤是注入的那一個，
    不是 `NameError`。
- **誰踩到（pump-dump-hunter）**：2026-09-21 兩次。
  1. 對照 r8 時（作廢的 r9 版本）刪掉了 `_missing = {}`，停損守衛整個失效。
  2. 對照 r12 時改寫 Algo 退回邏輯，刪掉了判斷「已撤單」的 `gone()`，**所有撤單都會失敗**。
     測「移損時撤舊單失敗要告警」的測試照樣通過——因為撤單確實失敗了，只是原因是 `NameError` 而不是注入的 503。
     是看到告警內容寫著 `name 'gone' is not defined` 才發現。兩次都在部署前抓到。
- **另一種形式：改寫沒有生效，也沒有報錯**（r15 新增）：用字串取代（`str.replace`、`sed`）修改程式時，
  比對字串的縮排或內容跟檔案差一點，取代就**什麼都不做、也不報錯**。常見於被改的程式先前被包進 `try` 多了一層縮排。
  pyflakes 抓不到（程式本身沒有錯，只是沒改到）。
  - **處理**：取代一律用「必須恰好命中 N 次，否則中止」的函式（crypto-screener 的 `scripts/patch.py`）；
    一輪修改結束後，逐條用搜尋確認每一處預期的修改都在程式裡，不能只靠「測試通過」推論。
  - **誰踩到（crypto-screener）**：2026-09-21 對照 r14 時，開倉兩條平倉路徑與部位紀錄共 3 處取代沒有生效，
    是基準數量的測試失敗才發現。
  - **pump-dump-hunter 檢查自己的改寫方式**（r16 新增）：r13 以前用的取代函式只 `assert` 字串存在，**不檢查命中幾次**
    （`str.replace` 會把所有出現處都換掉）；改測試檔、HANDOFF 用的是完全沒檢查的 `.replace`，改版本號用 `sed -i`——
    都會靜靜略過。回頭用搜尋逐條確認 r13 那輪 17 處預期修改都在程式裡，沒有漏。
    這輪起改用 `scripts/patch.py`（必須恰好命中 N 次，否則中止），每輪結束逐條搜尋確認。
    2026-09-21 對照 r18 時實際擋下一次：儀表板那一處的比對字串多寫了 `${...}`，工具中止並列出比對字串，
    改正後重跑。換成 `str.replace` 的話，後端改好了、畫面卻沒有新欄位，測試也不會發現（測試不看畫面）。
  - **整段縮排包 try 之後，要比對改動只有縮排和新增的 try**（r22 新增）：把一大段程式縮排包進 try，
    手動改很容易漏一行縮排（改變控制流程）或改到內容。pump-dump-hunter 對照 r21 時用程式比對前後：
    被改的 55 行全部只差縮排，新增的只有 try／except 那幾行。
    crypto-screener 對照 r23 時，手動平倉的外層保護照 r23 的「外層包裝＋原函式改名」做，原內容沒有重新縮排。

---

## 怎麼用這份清單

1. **開新對話時**：把這份檔案連同專案 zip 一起上傳，並說「先看 BINANCE_LESSONS.md」。
2. **發現新坑時**：加一條（症狀 / 原因 / 處理 / 誰踩到），更新最上面的版本日期，三個 repo 各覆蓋一份。
3. **API 可能又改時**：每個專案都有 `preflight.py`（交易所相容性自檢），開機跑一次、網頁可手動重跑。
   它檢查的每一項都對應上面的某一條。新增一條清單，就順手加一項自檢。
4. **改動下單相關程式後**：跑該專案的一致性測試，pump-dump-hunter 與 crypto-screener 都是 `python -m tests.test_parity`
   （crypto-screener 沒有回測引擎，它的測試鎖的是「同一組出場位階在程式裡被算了好幾次」的地方，
   例如送單參數、移損觸發點、影子追蹤、對帳認領必須都等於 `plan_exits`）。
5. **對照新版清單的做法**（r10 新增）：
   - 先把新舊兩版清單做逐段差異，**每一段都拆成可以對程式檢查的項目**，列成表再逐項查。
     只照轉達的重點去查，會漏掉差異裡其他段落——pump-dump-hunter 對照 r8 時第一次就是這樣漏掉的。
   - 驗證寫成專案裡的正式測試檔，**先在修改前的程式上跑**，確認每一項都會失敗，再修改、再跑到全部通過。
   - 修改前就通過的項目要回頭看：多半是測試寫錯了（只檢查告警編號、沒檢查實際計數之類），不是程式本來就對。
   - pump-dump-hunter 的 `tests/test_lessons.py` 是這個做法的範例，涵蓋第 7、8 條 r6→r8 的所有差異。
   - gold-scalper 也有 `tests/test_lessons.py`（`python -m unittest tests.test_lessons -v`），涵蓋 r7→r11 第 7、8 條。
   - **測試不能 mock 掉被測的那一段**：例如要測「偵測成功會記住模式」，就不能把偵測函式整個換掉，要 mock 更底層的 API 回應，
     否則測試會因為繞過了記錄的那行而失敗（或更糟：誤判通過）。gold-scalper 寫測試時踩到一次。
   - **模擬交易所的回應要跟真實幣安一致**（r12 新增）：至少要做到——條件單送到舊端點 `/fapi/v1/order` 回 `-4120`；
     單向模式的空單 `positionAmt` 是負數、雙向模式回 `LONG`/`SHORT` 兩列；雙向模式下平倉超量要被拒。
     crypto-screener 對照 r11 時有 2 項測試在修改前「通過」，追查發現是模擬交易所對舊端點條件單回 200，
     程式退回舊端點「掛成功」，測試根本沒走到要測的分支；另一支測試一律回正數的 `BOTH` 列，程式改成依方向比對後才露出來。
     pump-dump-hunter 對照 r12 時把模擬交易所做成獨立的 `tests/fake_exchange.py`，在 HTTP 層攔截，讓真的 `binance.py` 跑過；
     另外可以注入逾時、5xx、指定錯誤碼、「成交了但回應丟失」。舊的簡易模擬交易所也改成回 -4120，並檢查停損確實打到 Algo 端點。
   - **修改前就通過的項目，常見的測錯方式**（r13 新增，pump-dump-hunter 對照 r12 時一次遇到 6 個）：
     1. 準備資料時兩個來源設成同一個值（訊號價與成交均價都是 1.2），程式從哪邊拿都會過。
     2. 只跑一輪，守衛還沒到「連 3 輪」就結束，根本沒碰到要測的那行。
     3. 只檢查錯誤區，但錯誤被守衛的 try 接住後變成推播——錯誤區與推播都要看。
     4. 檢查結果碰巧對（別人的部位沒被平掉），但其實程式已經送出了錯誤的單、只是被交易所拒絕——要檢查「有沒有送單」。
     5. 用 0 當時間戳之類的「假值」，被程式的 `or` 預設值蓋掉，情境根本沒成立。
     6. 斷言只看有沒有失敗，沒看失敗原因（第 14 條的 `gone()`）。
     gold-scalper 對照 r13 時用這 6 種回頭檢查既有測試：4 項告警測試只看「第 1 次」、沒看告警裡的錯誤是不是注入的那個（第 6 種），
     1 項訊號價與成交均價同值（第 1 種），都已改緊；改緊後在現行程式上仍通過，程式本身沒問題。
   - **通知那段通常包在 try 裡**，裡面丟 `NameError` 會被吞掉、測試照樣通過（第 14 條的另一個位置）。
     行為測試跑完要確認「通知真的有送出、內容對」，並攔下程式的錯誤紀錄，確認裡面只有測試注入的錯誤。
   - **行為改了，一致性測試的模擬環境也要跟著補**：平倉改成「送單後再查部位確認」後，
     回測一致性測試的模擬交易所沒有部位表，時間出場全被判「無法確認」。要補的是模擬環境，不是放寬程式。
   - **測錯方式第 7～9 種**（r15 新增，crypto-screener 對照 r14 時遇到）：
     7. 用「最近 N 次呼叫裡有沒有送出 X」判斷，算到了測試準備階段送出的那一次——只算測試動作開始之後的呼叫。
     8. 被測程式在失敗時**整個跳過**，所以「沒有做出錯的事」——要另外確認它真的做了該做的檢查（例如真的查了舊端點）。
     9. 單獨呼叫流程中的某一個函式，測不到函式之間的**順序**問題（第 3 條「認領在判斷還在場之前」）——要走完整流程。
   - **共用的模擬交易所放獨立檔案**（r15 新增）：一個測試檔從另一個測試檔匯入模擬交易所時，
     對方的測試案例會在匯入時整個跑一遍（案例寫在模組層級時），輸出混在一起、還可能互相影響狀態。
     crypto-screener 抽成 `tests/fake_exchange.py`，同 pump-dump-hunter 的做法。
   - **測錯方式第 10 種**（r16 新增，pump-dump-hunter 對照 r15 時遇到）：
     10. 包裝（攔截）某個物件的方法時，被包的原方法是在**前一個情境**抓的——閉包綁到了上一個模擬交易所。
         新情境的平倉單被送進舊的交易所，量到的也是舊交易所的狀態。一個情境「通過」、另一個莫名失敗。
         → 包裝寫成函式，每個情境對「這個情境的物件」各包一次。
     發現後用修正過的測試**重跑修改前的程式**，確認原本判定「程式本來就對」的項目仍然成立——
     錯的測試得出的「本來就對」不能沿用。
   - **新規則會讓舊測試走不到要測的地方**（r17 新增，測錯方式第 8 種的變形）：加了「基準查不到就不送單」之後，
     一支舊測試沒有模擬部位表，基準查詢失敗、單沒送出，根本走不到它要測的「成交後出錯」。這次是因為斷言剛好會失敗才發現；
     斷言寫得寬一點（例如只看「沒有報錯」）就會空跑通過。→ 每支測試都加一條前提斷言（「單真的送出去了」），
     新增前置條件的規則時，舊測試才會明確失敗、而不是空跑。gold-scalper 2026-09-21 對照 r16 時遇到。
   - pump-dump-hunter 對照 r15 時用第 7～9 種回頭看 `tests/test_r12.py`：2 處數呼叫次數改成只算動作之後（第 7 種）；
     4 處補上「前提」檢查——對帳真的跑完、平倉單真的送出（第 8 種）；收尾通知補一個走完整對帳的版本（第 9 種）；
     時間出場告警補檢查原因是注入的錯誤（第 6 種）。收緊後在現行程式上全部通過。
   - **用突變驗證前提斷言**（r18 新增）：補完前提斷言後，故意把新規則的前置條件弄壞再跑一次
     （例如讓逐幣部位查詢一律回空清單 → 基準查不到 → 不送單）。會送單的測試必須全部明確失敗；
     **還通過的就是在空跑**。crypto-screener 對照 r17 時這樣找到 2 支（「進場瞬間穿價、平倉被拒」與「停損掛不上、平倉被拒」）：
     斷言是「交易所上有部位、且帳上沒記」，單沒送出時交易所上沒部位，條件不成立就「通過」了。
     這個突變檢查可以放進驗證流程，每次都跑。
     pump-dump-hunter 對照 r18 時做成 `tests/mutation_check.py`（模擬交易所的 `PDH_MUTATE=no_base` 開關）：
     依情境切開測試檔，列出每個會送進場單的情境在突變下失敗與仍通過的項目；**本來就在測突變條件的情境**要標「突變豁免」，
     但必須另有對照組（同樣的呼叫在沒注入時確實會送單），否則「沒送單」可能是別的原因。
     `tests/test_r12.py` 的結果：4 個會送進場單的情境都有項目明確失敗，但其中「逾時 → 通知不能寫下單失敗」單項空跑
     （單沒送出，通知自然沒有那幾個字）；4 個情境都補上「進場單真的送出」的前提。
   - **哪些斷言需要前提**（r19 新增）：正向斷言（「認領數量＝100」「停損有掛上」）在程式什麼都沒做時本來就會失敗，不會空跑；
     **否定句斷言**（「不能 X」「沒有 Y」「部位仍在」「停損沒被撤」）程式什麼都沒做也成立——這類一定要有前提。
     pump-dump-hunter 三支測試共 43 個情境，逐一分類後補了 7 個否定句情境的前提。
   - **前提斷言也可能是錯的，它失敗時先懷疑預期值**：pump-dump-hunter 對照 r18 時兩次——
     整列未實現損益的預期值算錯（漏算別人那 40 的損益）；「孤兒單無法確認」的預期不成立（注入只影響全量表，逐幣查詢正常，程式正確判定不是孤兒）。
     兩次都是前提先失敗、才沒拿錯的預期去判斷主斷言。
   - **測錯方式第 11、12 種**（r18 新增，crypto-screener 對照 r17 時遇到）：
     11. 測「某一項出錯不影響其他項」時，出錯的那一項排在**後面**——前面的早就處理完了，看起來沒影響。要讓出錯的排在前面。
     12. 前提斷言只檢查「做了查詢」，沒檢查要測的那一步**之前**的動作真的發生（例如測「平倉被拒後的確認查詢」，
         卻讓平倉前的確認就先失敗，平倉單根本沒送）。前提要涵蓋走到被測那一步所需的每一個動作。
   - **測錯方式第 13 種**（r19 新增，pump-dump-hunter 對照 r18 時遇到）：
     13. 斷言看的「結果」在被測那一步**之前**就已經存在。例如測「下一輪確認部位還在 → 補掛停損」，
         斷言是「交易所上有這個幣的停損」——修改前的程式在**上一輪**就補掛了，這一輪什麼都沒做也成立。
         → 斷言要看「這一步之後新發生的事」（只算這一步之後的呼叫、比對前後差異），不是看最終狀態。
   - **測錯方式第 14 種**（r20 新增，gold-scalper 對照 r19 時遇到）：
     14. 被測物件是**模組層級的共用物件**（例如程式啟動時就建好的引擎），前一個測試留在它身上的狀態（出錯次數、計數器）
         帶進下一個測試。斷言「部位結束後出錯次數清空」失敗，看到的是上一個測試留下的另一個鍵。
         → 取得共用物件的函式要把每個會被測試改到的狀態重設；或每個測試建新物件。跟第 10 種（閉包綁到舊物件）是同一類。
   - gold-scalper 對照 r19 時做了突變驗證（`tests/mutation_check.py`，讓逐幣部位查詢一律「查不到」）：
     會送進場單的 4 項、會掛停損的 9 項全部明確失敗；豁免的 4 項（本來就在測「查不到」）在突變下也失敗，
     因為它們的前提斷言「真的查了部位」抓到了突變——前提寫得夠細時，突變豁免的項目也會失敗，這是好事。
     兩個對照組在沒有突變時通過。
   - **測錯方式第 15 種**（r21 新增，crypto-screener 對照 r20 時遇到）：
     15. **否定句斷言藏在正向的描述裡**。例如「確認成交時要用幣＋方向」，實際斷言是「不能把別人的空單當成自己的已成交」——
         照描述分類會被當成正向、不補前提；突變（查詢回空清單）下什麼都找不到，斷言自然成立，就空跑了。
         → 分類看**斷言本身**（`if 某事發生: 失敗`＝否定句），不看描述；或直接用突變逐項比對來抓。
   - **突變檢查要逐項比對，不是「有失敗就好」**：crypto-screener r18 的突變檢查只要求每支測試「至少一項明確失敗」，
     抓不到上面那種單項空跑。改成逐項：突變下仍通過的每一項都必須列在豁免清單並寫理由，不在清單的就是空跑。
     本來就在測「查不到」的項目不豁免，而是補對照組，讓突變也打到它（`tests/mutation_check.py`）。
     **豁免的理由也要驗證**（r22 新增，pump-dump-hunter 的做法）：豁免分三種——「無關」（不經過被突變的查詢）、
     「前提」（同情境有前提在突變下失敗）、「對照組」（另有一項證明正常時那條路走得通）。後兩種要寫出引用的是哪一項，
     檢查器確認**被引用的那一項在突變下真的失敗**，否則豁免不成立；豁免清單裡已經不再通過的項目也要報出來刪掉（清單過期會蓋住將來的空跑）。
     檢查器本身也要驗證：故意拿掉一項豁免、把一項引用改成突變下會通過的項目，確認兩個都會報錯。
     **自我驗證要用固定的人造資料，不能拿當下的豁免清單來弄壞**（r23 新增，gold-scalper 遇到）：清單裡的項目可能本身就過期
     （突變下已經會失敗），檢查器對它根本不看引用，「把引用改掉」就測不出來——第一版自我驗證就這樣只抓到一半。
     另外每次補完前提斷言都要重跑檢查器：gold-scalper 原本列的 3 項豁免，都因為前提「真的查了部位」抓到了突變而過期。
     「無關」可以自動判定：記錄被突變的函式在這項測試裡被呼叫幾次，0 次就是無關，不用靠人寫理由。
     **命中次數要算「被呼叫」，不是「突變分支被執行」**（r25 新增，pump-dump-hunter 遇到）：第一版只在突變實際回空清單時計數，
     但很多情境自己就注入了「逐幣查詢回空」——注入先回了，突變分支沒執行，計數 0，**明明呼叫了被突變的函式卻被自動判成無關**。
     剛好是最該檢查的那一組（本來就在測「查不到」的項目）。改成只要突變開著、被突變的查詢被呼叫就算命中。
     **命中次數是「這個情境到目前為止」，全域檢查會繼承上一個情境的**：放在最後的「所有情境沒有程式錯誤」、
     放在某情境裡的靜態檢查，都帶著上一個情境的命中次數被判成要人工豁免。→ 這類檢查自成一個情境（先 `fresh()`）。
     pump-dump-hunter 修完後：突變下仍通過 92 項，命中 0 次自動判定無關 49 項、人工豁免 43 項（全部是「前提」或「對照組」，
     沒有人工寫的「無關」）。檢查器的判定邏輯抽成純函式，用 13 組固定人造資料自我驗證（`tests/mutation_selftest.py`）。
     pump-dump-hunter 對照 r21 時，逐項比對找出 3 項真的空跑（見第 16 種），突變下仍通過的其他 74 項全部分類並寫理由。
   - **測試本身也可以做靜態檢查**：crypto-screener 的 `tests/check_tests.py` 檢查每個案例的第一個動作是不是重建乾淨狀態
     （第 14 種）、描述是否定句的案例有沒有 `need(...)` 前提（r19）。靜態檢查抓得到大部分，第 15 種要靠逐項突變。
     pump-dump-hunter 對照 r21 時也做了 `tests/check_tests.py`，用語法樹判斷斷言本身（不看描述）：
     條件是 `not …`、`… not in …`、`== 0`、`is None` 的算否定句。**組合條件要注意**：`A and B` 只要有一項是正向，
     程式什麼都沒做時整體就會失敗、不會空跑，所以要**全部**都否定才算否定；`A or B` 則是**任一**否定就算。
     第一版寫反了，報出一堆假問題。
   - **第 14 種在 pump-dump-hunter 的實際樣子**（r22 新增）：不只計數器，**被換掉的模組層級函式**
     （模擬 K 線 `B.klines`、模擬時鐘 `manager._now`）在三支測試裡換掉後從沒還原，後面的情境全都用著前一個情境的假 K 線和假時鐘。
     另外幾處在情境內還原了，但情境中途出錯時走不到還原那行。→ 一律由 `fresh()` 重設所有會被換掉的東西。
   - **測錯方式第 16、17 種**（r22 新增，pump-dump-hunter 對照 r21 時遇到）：
     16. 否定句斷言在**兩種不同原因**下都成立，前提只檢查「有去查」、沒檢查「查到了什麼」。例如「部位已沒了 → 移損不掛新停損」：
         突變下是「查不到」而不是「沒了」，結果一樣是不掛；前提「移損有去確認部位」也照樣成立。
         → 前提要檢查查詢的**結果**（「確認的結果是『沒了』，不是『查不到』」）。逐項突變抓出 3 項這種。
     17. 新規則讓舊測試**走了另一條路、結果剛好一樣**。例如「雙向超量被拒 → 用實際數量重送」：加了「平倉前先確認剩多少」之後，
         程式一開始就送實際數量，根本不會再超量被拒；最後「平掉了」的主斷言照樣通過，測試描述的那條路已經不存在。
         是補「平倉單至少兩張」的前提時才發現。→ 前提寫出要走的路徑（第幾張、數量多少），不只寫結果。
   - **測錯方式第 18 種**（r24 新增，crypto-screener 對照 r23 時遇到）：
     18. **模擬環境的注入條件太寬，在被測那一步之前就生效**。例如「平倉單送出之後，查部位就失敗」寫成
         「任何市價單送出之後」——進場那張也算，平倉前的確認就先失敗、程式判「查不到、不送單」，
         兩條否定句斷言自然成立。這一項從加進來起就在空跑；突變檢查也抓不到，因為它不經過被突變的查詢。
         是用語法樹判斷否定句、補前提「平倉單有送出」時才發現。→ 注入條件要精確到被測的那一步；前提寫出那一步真的發生了。
   - **看斷言本身判斷否定句時，要配合自己的斷言寫法**（r24 新增）：pump-dump-hunter 的規則（`not …`、`== 0` 算否定）
     適用於 `assert 條件` 的寫法。crypto-screener 的寫法是 `if 條件: return 失敗`——條件成立＝失敗，方向剛好相反：
     `if not X` 是正向（要求 X 發生）、`if X` 是否定；組合條件也反過來，`if A or B` 要全部否定才算否定、`if A and B` 任一否定就算。
     直接套別人的規則會全部判反。檢查器本身也用固定人造資料自我驗證（11 組）。
   - **「無關」自動判定後，人工豁免可能歸零**：crypto-screener 對照 r23 時照 r23 的做法記錄每一項的突變命中次數，
     先前人工寫理由的 13 項全部命中 0 次，自動判定無關；人工豁免清單歸零，也就沒有過期豁免的問題。
   - **怎麼確認沒有第 18 種**（r25 新增，pump-dump-hunter 的做法）：讓模擬交易所在每次注入觸發時印出「是第幾個請求、哪一張單」
     （`PDH_INJECT_LOG=1`），跑一遍全部測試，逐一核對每個注入都是在被測那一步觸發。pump-dump-hunter 對照 r24 時全部正確；
     但有一處是用「第幾次查詢」計數的寫法（「第一次逐幣查詢正常、之後都回空」），碰巧正確，平倉前只要多一次查詢就會錯位——
     改成「平倉單送出之後才回空」，綁在被測那一步上。
     gold-scalper 對照 r25 時有 4 項開倉／認領測試是「第 1 次查部位回基準、第 2 次回成交後」的寫法，
     改成「開倉單送出之前回基準、之後回成交後」，並在注入裡記錄每次是在送單前還是送單後觸發，斷言觸發順序。
   - **測錯方式第 19 種**（r26 新增，gold-scalper 對照 r25 時遇到）：
     19. **全域／靜態檢查本身也會空跑**：「pyflakes 沒有報 undefined name」這種斷言，pyflakes 根本沒裝時輸出是空的、斷言成立。
         同樣的還有「錯誤紀錄裡沒有程式錯誤」（攔截沒生效時紀錄是空的）、「靜態檢查沒有報問題」（掃描路徑錯了時什麼都沒掃到）。
         → 這類檢查一律要有前提：工具真的執行了、真的掃到了東西（例如輸出裡有一條已知的誤報、掃到的測試數量大於 N）。
         是用「每個測試至少要有一條正向斷言」的靜態檢查（`tests/check_tests.py`）找到的；同時抓到一項「偵測失敗不假設想要的模式」
         只有 `assertFalse`，補上「有舊值時要回舊值」這條正向斷言。
   - **patch 工具在腳本中途中止時，前面的修改已經生效**（r25 新增）：一個腳本依序改好幾個檔，第三處比對不到而中止——
     前兩處已經寫進檔案，後面的都沒改。重跑前要先確認哪些已生效，否則重跑會在已改過的地方比對不到（或重複修改）。
     pump-dump-hunter 對照 r24 時遇到一次（全域檢查那段的原文跟預期不同），逐檔確認後只重跑沒生效的部分。
     **更好的做法：先全部比對、全部命中才一次寫入**（r26 新增，gold-scalper 的 `scripts/patch.py` 的 `apply()`）：
     一批修改（可跨多個檔、同一個檔多處依序套用）先在記憶體裡逐一比對，任何一處對不上就中止、**一個檔都不寫**，
     就不會有「改了一半」要去確認的狀態。工具本身要驗證一次：故意讓第二處比對不到，確認第一處的檔案沒被改動。
     **但只保護得到「同一次 apply」裡的修改**：gold-scalper 對照 r25 時，清單的 apply 中止了（一個檔都沒寫），
     同一個指令後面另外用 `sed` 改的 VERSION 卻照樣生效，差點交出「清單 r25、VERSION r26」的不一致版本。
     → 版本號也放進同一次 apply，或者每一步檢查上一步的結果再往下走。
     另外兩個小坑：拿「從檔案擷取的一段原文」當比對字串時，替換內容要保留原文結尾的換行，否則下一行會黏上來（py_compile 抓得到）；
     逐條搜尋確認修改時，預期次數要算上同一字串在**其他函式**裡的出現次數，搜尋結果跟預期不同時先懷疑預期值。
     crypto-screener 對照 r26 時照做：`apply()` 附自我驗證（第二處比對不到時第一個檔不被動到），這輪中止兩次
     （一次是預期的上下文錯了、一次是同一段在另一個函式也有），兩次都一個檔都沒寫；清單與版本號放在同一次 apply。
     pump-dump-hunter 對照 r27 時照做（`scripts/patch.py` 的 `apply()`，5 組自我驗證：第二處對不到時第一個檔不動、
     結尾換行不一致時中止）；這輪清單、版本號、HANDOFF 放在同一次 apply。第一次套用就中止了（清單有三處比對字串的縮排
     是照「前面印出來的樣子」抄的，但那次輸出前面還有自己加的縮排），**三個檔都沒寫**、版本號也沒被單獨改掉；
     改成從檔案讀出整行當比對字串後重新套用。
   - **第 19 種在 crypto-screener 的樣子**（r27 新增）：
     - **某一支測試的攔截根本沒裝**：5 支測試檔各自複製了一份案例框架，其中 test_r11 那份沒有攔截錯誤輸出——
       「沒有程式錯誤被吞掉」在它的 23 項上從來沒檢查過。→ 案例框架收成一份共用的（`tests/harness.py`），並用人造案例自我驗證。
     - `verify.sh` 的 pyflakes 沒裝時只印警告、照樣通過；區域變數檢查只印「通過」、看不出掃了幾個函式。
       → 沒裝就失敗；先掃一個已知有錯的檔（金絲雀），必須報出來；印出並檢查掃到的數量。
   - **測試基礎設施本身的測試，不該被突變檢查當成「程式的測試」**（r27 新增）：測「注入綁定」「命中計數」這類模擬交易所本身的項目，
     會自己送被突變的查詢，命中大於 0 又通過，被判成空跑。→ 這類項目執行時關掉突變環境、或事後清掉自己的命中紀錄，
     讓它們自動歸為「無關」；不要為了它們放寬檢查器。
   - **「讀模擬交易所」不等於「測基礎設施」**（r28 新增，pump-dump-hunter 對照 r27 時）：43 項人工豁免逐項看，
     條件有讀模擬交易所狀態的不少（例如「別人的 40 留著」），但那個狀態是**程式送的單造成的**，測的是程式的行為。
     真正只確認測試環境本身的只有 1 項。做法：斷言加 `infra=True` 標記，框架印〔命中0〕自動歸為無關；
     靜態檢查確認 infra 項的條件只讀模擬交易所、不碰程式，防止拿這個標記繞過檢查。
   - **收成共用框架時，別用 `import *`**（r28 新增，第 19 種）：pump-dump-hunter 把五支測試收成 `tests/harness.py` 時
     先用了 `from tests.harness import *`——pyflakes 在這五個檔上**完全無法偵測未定義名稱**（它自己會說 unable to detect）。
     靜態檢查靜靜失效。改成明列名稱後，pyflakes 馬上抓到一個潛伏的錯：某支測試用舊框架的 `ORIG["record_close"]`，
     新框架的鍵格式不同，那行執行時一定 `KeyError`，只是那條分支沒被走到。驗證腳本也把「unable to detect」明確算成失敗。
   - **共用框架收起來之後，漏掉的那份才會現形**：pump-dump-hunter 的 test_r12（50 多項）結尾**根本沒有**
     「沒有程式錯誤被吞掉」的全域檢查（同 crypto-screener 的 test_r11）；收進框架、由 `finish()` 統一做之後才補上。
     遷移前後逐支比對斷言數，確認只多了這 2 項全域檢查、其他一項沒少。
   - **框架與檢查器的自我驗證，第一次跑就抓到它們自己的錯**（r28 新增）：框架的全域前提寫成「斷言數 > 1」，
     真的測試都有幾十項所以一直通過，只有一項斷言的人造案例才露出來（應為 ≥ 1）。突變檢查器加了「至少解析到 5 項」的新規則後，
     它自己的 3 組人造資料全部被判「解析太少」——r17 說的「新規則讓舊測試走不到」，發生在檢查器自己身上；門檻改成參數，
     自我驗證另外補一組驗證這條新規則。
   - **程式變穩之後，舊測試的前提可能不成立了**（r28 新增，r17 的另一種樣子）：一項測試靠「餵 `exit="n/a"` 讓通知格式化出錯」
     來驗證通知出錯被隔離；程式改成先檢查型別後，壞資料不再讓格式化出錯，這項就測不到它要測的東西了。
     改成讓通知**送出**時失敗，並補前提「通知真的失敗了」。
     gold-scalper 對照 r28 時逐項看過：靠壞資料觸發例外的只有「待撤清單有一筆欄位缺漏」，補了前提「那一筆真的走到例外處理」
     （不是走一般的撤單失敗）；其他都是直接讓被 mock 的函式丟例外，不受程式變穩影響。
   - **測錯方式第 20 種**（r29 新增，gold-scalper 對照 r28 時遇到）：
     20. **共用框架的 setUp 把被測的東西 mock 掉了**：「平倉通知不能用模擬點數估 USDT」這項放進了交易流程的共用框架，
         而那個框架在 setUp 裡把整個 `notify_trade_event` 換成收集器——通知的格式化根本沒執行。
         斷言「通知裡沒有 -0.20 USDT」會空跑通過；是前提「通知真的組出來、送出了」失敗才發現。
         → 測格式化、測某個函式內部行為的項目，放在不 mock 那個函式的情境；前提要檢查「那個函式真的執行了」。
   - **錯誤掃描只攔了一個模組的 logger**（r29 新增，第 19 種在 gold-scalper 的樣子）：「沒有程式錯誤被吞掉」的全域檢查
     只攔引擎的 logger，網頁入口、下單層寫的錯誤從來沒被掃過。改成攔所有 logger（`logging.Logger.error`），
     前提是「至少攔到兩個不同模組的錯誤」。gold-scalper 的完整驗證收成 `scripts/verify.py`，第一次跑就報出一項靜態檢查失敗
     （上面那個回傳原因檢查缺前提）。
   - **彼此獨立的檢查不要塞在同一個情境裡**：命中次數是「這個情境到目前為止」，放在移損情境後面的兩個「沒有待辦時回傳原因」
     繼承了移損的命中次數，被突變檢查判成空跑。它們本來就不查部位，移到自己的情境就自動歸為無關。
   - **完整驗證收成一個指令**：pump-dump-hunter 的 `scripts/verify.py` 依序跑 pyflakes（沒裝就失敗、先掃金絲雀）、
     所有行為測試（每支要印「全部通過」且斷言數達標）、框架／靜態檢查／回傳原因／改寫工具／突變檢查器各自的自我驗證、逐項突變檢查。
     第一次跑就如實報出三件事（未修的程式、上面那個 import *、一個計數方式不適用的測試），不會因為某一步沒執行而印「通過」。
   - **crypto-screener 對照 r29 時的幾點**（r30 新增）：
     - **模擬交易所要讓「成交價」和「標記價」不一樣**：市價單成交價加一點滑價、部位均價＝成交價。兩者一樣時，
       測試分不出程式用的是哪一個——「出場價用成交價」這條斷言會空跑通過。
     - **程式變穩之後失效的舊測試**（r28）：r23 有兩項靠「`wantStop` 是字串 → 收尾通知格式化時拋錯」來測「結帳之後出錯」，
       r24 起通知先檢查型別、不再拋錯，兩項照樣通過但測不到它描述的那條路。改成讓結帳之後的一步（每日統計）確實失敗，
       並補前提「那一步真的出錯了」。其他靠壞資料觸發例外的 5 項逐一確認仍會觸發、且都有斷言證明例外發生。
     - **沒有用 logging 的專案**：錯誤是各模組直接寫 `sys.stderr`，攔截點是框架裡的 `sys.stderr`，本來就涵蓋所有模組；
       前提照 r29 做——在框架底下讓 trader 與 main 各走一次真的出錯路徑，確認兩個都攔得到（`harness.selftest_modules()`）。
     - **基礎設施標記**（r28）：`case(..., infra=True)`，框架執行後清掉它的突變命中；靜態檢查確認 infra 項在 `fresh()` 之後
       不碰 `T.`、`M.`（附人造資料自我驗證）。取代了 r27 用的「關環境變數、手動清命中」兩個權宜寫法。
   - **pump-dump-hunter 對照 r30 時的幾點**（r31 新增）：
     - **模擬交易所讓成交價 ≠ 標記價之後，一項舊測試「通過的原因」露出來了**：r27 的「成交明細查不到時講明損益未知」一直通過，
       是因為舊的模擬交易所平倉損益剛好 0，而程式用 `realizedPnl ≠ 0` 篩平倉成交把它濾掉了——**兩個錯湊成對的結果**。
       改完模擬交易所後這項失敗，才發現它根本沒讓成交明細查不到。→ 模擬環境的預設值不要是退化值（0、成交價＝標記價、手續費 0），
       退化值會讓程式的錯剛好被抵銷。這次同時加了滑價、加權均價、開倉成交、手續費、遞增成交 id。
     - **測試資料要自洽**：一項「打平出場」測試把模擬交易所的均價改成 1.0，程式帳上記的成交價卻是 1.001；
       程式濾掉那筆成交後退到估算，拿 1.001 算出一個負數，碰巧滿足「損益 < 0」。改成開倉時就不加滑價，
       並直接比對成交明細算出的值（-0.04，估算會是 0.0）。
   - **測錯方式第 21 種**（r31 新增）：21. **在一堆輸出裡找關鍵字，被別的輸出滿足**。「平倉通知講明損益未知」寫成
     「任何一則通知含『損益未知』」——這輪新增的「數量減少」通知也寫「這段損益未知」，平倉通知沒講明也照樣通過。
     是逐項突變檢查報它空跑才發現。→ 斷言指定是哪一則（開頭符號、類型），不在全部輸出裡找字。
   - **錯誤掃描的前提放進每支測試**：r29 說前提是「至少攔到兩個不同模組的錯誤」。pump-dump-hunter 的做法是框架的 `finish()`
     每次都先讓 manager、main、presets 各走一次真的出錯路徑，確認攔得到才做全域掃描；掃描範圍加上 `sys.stderr`（traceback 都在那）。
     找到三處只 `print` 的錯誤：套用實盤參數失敗、Telegram 送出失敗、網頁請求出錯，錯誤區與推播都看不到，已補上。
   - **`apply()` 這輪擋下一次**：對帳那段在 r22 被整段縮排包進 try，照舊縮排寫的比對字串對不上，六處修改一處都沒寫。
     改成用一個小函式從檔案讀出連續幾行原文當比對字串（`exact()`），不再手抄縮排。
